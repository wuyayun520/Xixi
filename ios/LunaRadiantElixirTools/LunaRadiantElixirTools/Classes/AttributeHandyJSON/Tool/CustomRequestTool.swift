
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let mainKeyRequestValue:[Character] = ["m","f","/","m","o","m","e","n","t","/"]
fileprivate let k_instancePath:[Character] = ["l","i","s","t"]

/*: "mf/moment/moments" :*/
fileprivate let userValueMsg:String = "extension show index giftmf/mom"
fileprivate let main_subjectMsg:String = "mensearch"

/*: "mf/moment/like" :*/
fileprivate let kAtValue:[Character] = ["m","f","/","m","o","m","e","n","t","/","l","i","k","e"]

/*: "mf/crush/send" :*/
fileprivate let dataViewFormat:String = "image inputmf/crush"
fileprivate let showTopMPath:String = "status data protocol/send"

/*: "mf/moment/view" :*/
fileprivate let appApplicationControlPath:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","e"]
fileprivate let main_viewPath:String = "data"

/*: "mf/momentReply/list" :*/
fileprivate let kManagerName:String = "return mode self selfmf/moment"
fileprivate let notiCookieValue:String = "in data let signatureRepl"

/*: "mf/momentReply/save" :*/
fileprivate let kSkipTitle:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y"]
fileprivate let appFrameMsg:String = "/savedon message size count to"

/*: "mf/moment/videoSign" :*/
fileprivate let appKeyAreaName:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","d","e","o","S"]
fileprivate let dataCloseValue:[Character] = ["i","g","n"]

/*: "dynamicPic.zip" :*/
fileprivate let user_blockFormat:String = "DYNAM"
fileprivate let show_frameStr:String = ".zipmodel self bar"

/*: "pic" :*/
fileprivate let app_startLiveValue:[UInt8] = [0x63,0x69,0x70]

/*: "content" :*/
fileprivate let kViewValue:[Character] = ["c","o","n","t","e","n","t"]

/*: "mf/moment/save" :*/
fileprivate let appRefreshShareMessage:[Character] = ["m","f","/","m","o","m","e","n","t","/","s","a","v","e"]

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let main_labTabFormat:[UInt8] = [0x59,0x7e,0x6b,0x75,0x72,0x7c,0x71,0x29,0x5c,0x7e,0x6c,0x6c,0x6e,0x7c,0x7c,0x6f,0x7e,0x75,0x35,0x29,0x50,0x78,0x29,0x7d,0x78,0x29,0x56,0x82,0x29,0x4d,0x82,0x77,0x6a,0x76,0x72,0x6c,0x7c,0x29,0x7d,0x78,0x29,0x7f,0x72,0x6e,0x80]

fileprivate func momentSizeWith(text num: UInt8) -> UInt8 {
    let value = Int(num) + 247
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/saveVideo" :*/
fileprivate let noti_modelId:String = "mf/momentype view"
fileprivate let const_loadPath:String = "manager top menu label it/sav"

/*: .png" :*/
fileprivate let mainNameMessage:String = ".pngheight reply record blind"

/*: "dynamicPic" :*/
fileprivate let appSucceedKey:String = "dynamicPilet need"
fileprivate let userLeadingName:[Character] = ["c"]

/*: "freeImage" :*/
fileprivate let show_originalPath:String = "freeIimport video sex make"

/*: "mf/momentReply/replylist" :*/
fileprivate let showMoveTitle:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/","r","e","p"]
fileprivate let kAppPath:[Character] = ["l"]
fileprivate let notiNeverKey:[Character] = ["y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let userColorPath:String = "mf/moimage block frame"
fileprivate let data_resultMsg:[Character] = ["d","e","l","e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let noti_takeMessage:String = "mf/momlet fail yet self send"
fileprivate let constShadowMessage:String = "dedataete"

/*: "mf/moment/pin" :*/
fileprivate let notiStrengthValue:[Character] = ["m","f","/","m","o","m","e","n","t"]
fileprivate let data_bottomPath:String = "in equal else/pin"

/*: "mf/moment/remindNum" :*/
fileprivate let userTextId:String = "text request center user imagemf/m"
fileprivate let noti_cornerMessage:String = "color bottom direction index name/remi"

/*: "mf/moment/remindList" :*/
fileprivate let appHiddenFormat:String = "hidden lastmf/mome"
fileprivate let dataActionUrl:[Character] = ["n"]
fileprivate let noti_managerUrl:String = "t/remistack size add"

/*: "page" :*/
fileprivate let main_hiddenKey:[UInt8] = [0x65,0x67,0x61,0x70]

/*: "cos/getCredential" :*/
fileprivate let main_appFormat:String = "top"
fileprivate let notiAlongToFormat:String = "make regular touchos/get"
fileprivate let mainToolValue:String = "tscalel"

/*: "type" :*/
fileprivate let dataStatusId:String = "TYPE"

/*: "user/reportUserInfo" :*/
fileprivate let appSizeId:[Character] = ["u","s","e","r","/","r","e","p","o"]
fileprivate let mainGiftName:[Character] = ["r","t","U","s","e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let mainSourceCenterAsFormat:String = "finishid"

/*: "deviceId" :*/
fileprivate let noti_resistanceColorTitle:[Character] = ["d","e","v","i","c","e"]
fileprivate let dataToKey:String = "response self tagId"

/*: "deviceName" :*/
fileprivate let mainInfoTitle:String = "dmanagervicmanager"
fileprivate let const_cornerFrameMessage:String = "Namesource item shrink"

/*: "iphone" :*/
fileprivate let noti_colorMessage:[Character] = ["i","p","h","o","n","e"]

/*: "fileUrl" :*/
fileprivate let dataBottomMessage:String = "self equal viewfileUrl"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CustomRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class CustomRequestTool: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func telephoneMessage(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(mainKeyRequestValue) + String(k_instancePath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func playByModel(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(userValueMsg.suffix(6)) + "ent/mo" + main_subjectMsg.replacingOccurrences(of: "search", with: "ts"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func imageCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(kAtValue))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func paradigm(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(dataViewFormat.suffix(8)) + String(showTopMPath.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func voiceOver(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(appApplicationControlPath) + main_viewPath.replacingOccurrences(of: "data", with: "w"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func cuttingEdge(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(kManagerName.suffix(9)) + String(notiCookieValue.suffix(4)) + "y/list")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func numberimateOf(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(kSkipTitle) + String(appFrameMsg.prefix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func pastCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(appKeyAreaName) + String(dataCloseValue))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func completion(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        on()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            betweenCell(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((user_blockFormat.lowercased() + "icPic" + String(show_frameStr.prefix(4))))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: app_startLiveValue.reversed(), encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(kViewValue))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ContentBlockReactiveCompatible()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(appRefreshShareMessage))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.taproom(showMsg: String(bytes: main_labTabFormat.map{momentSizeWith(text: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.on()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func videoView(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(noti_modelId.prefix(8)) + String(const_loadPath.suffix(5)) + "eVideo")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func betweenCell(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.compressedInfoMetadata()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(mainNameMessage.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((String(appSucceedKey.prefix(9)) + String(userLeadingName)))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func on() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(show_originalPath.prefix(5)) + "mage"))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func completionImage(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(showMoveTitle) + String(kAppPath) + String(notiNeverKey))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func loadCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(userColorPath.prefix(5)) + "ment/" + String(data_resultMsg))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func dataEqual(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(noti_takeMessage.prefix(6)) + "entReply/" + constShadowMessage.replacingOccurrences(of: "data", with: "l"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func followThrough(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(notiStrengthValue) + String(data_bottomPath.suffix(4)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func messageGetRemind(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(userTextId.suffix(4)) + "oment" + String(noti_cornerMessage.suffix(5)) + "ndNum")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func obtain(page: Int, completion: @escaping (_ data: [PathControlListModel]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(appHiddenFormat.suffix(7)) + String(dataActionUrl) + String(noti_managerUrl.prefix(6)) + "ndList")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: main_hiddenKey.reversed(), encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [PathControlListModel] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<PathControlListModel>.deserialize(from: arr as? Array) as? [PathControlListModel] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func nature(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (main_appFormat.replacingOccurrences(of: "top", with: "c") + String(notiAlongToFormat.suffix(6)) + "Creden" + mainToolValue.replacingOccurrences(of: "scale", with: "ia"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(dataStatusId.lowercased()): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func terraceCompletion(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestServer = StatisticalTableReactiveCompatible.share.appConfigMode.reportDomain
        reqModel.requestServer = StatisticalTableReactiveCompatible.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(appSizeId) + String(mainGiftName))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = StatisticalTableReactiveCompatible.share.loginUserMode.userID
        params[(mainSourceCenterAsFormat.replacingOccurrences(of: "finish", with: "u"))] = StatisticalTableReactiveCompatible.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(noti_resistanceColorTitle) + String(dataToKey.suffix(2)))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(mainInfoTitle.replacingOccurrences(of: "manager", with: "e") + String(const_cornerFrameMessage.prefix(4)))] = (String(noti_colorMessage))
        //: params["type"] = 1
        params[(dataStatusId.lowercased())] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(dataBottomMessage.suffix(7)))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
