
//: Declare String Begin

/*: "http://" :*/
fileprivate let const_showName:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let notiDetailKey:String = "hviewviewps"
fileprivate let appColorName:String = "://"

/*: "http:// :*/
fileprivate let main_hiddenId:[Character] = ["h","t","t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let constInnerKey:String = "sarrayve"
fileprivate let kTextUrl:String = "ctIndtop effect"
fileprivate let notiViewStr:[Character] = ["e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let user_backgroundWithFormat:[Character] = ["s","e","r","v"]
fileprivate let appMakeName:String = "erUrfilter status var"

/*: "app. :*/
fileprivate let const_userName:String = "app.view to to"

/*: .com" :*/
fileprivate let data_matchFormat:String = ".comdata container"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let constToValue:String = "result to observe actualmf/chatR"
fileprivate let noti_clearMsg:[Character] = ["o","o","m","/","s","e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let show_viewStr:[Character] = ["r","e","c","h","a"]
fileprivate let notiEqualValue:[Character] = ["r","g","e","/","a"]
fileprivate let userDataFormat:String = "pplePamake share model selected"

/*: "user/appleLogin" :*/
fileprivate let appCenterTitle:String = "USER"
fileprivate let showLabelValue:String = "else trigger by caseleLogin"

/*: "IM/getMyBlackList" :*/
fileprivate let dataColorName:String = "IM/getrue line action"
fileprivate let appSizeName:String = "path m keytMyBla"
fileprivate let constRawUrl:[Character] = ["c","k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let show_valuePath:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e"]
fileprivate let appRowValue:[Character] = ["n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let constMaxTipSubMessage:String = "self varmf/busi"
fileprivate let const_pushStr:String = "info tip let sex cookieness/s"
fileprivate let mainAllowId:String = "awake var colorendGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let dataToText:String = "array path circle user typemf/c"
fileprivate let mainTitleUrl:String = "height time to letom/s"

/*: "en" :*/
fileprivate let app_wrapErrorData:[Character] = ["e","n"]

/*: "ar" :*/
fileprivate let app_attentionViewMsg:String = "alimit"

/*: "es" :*/
fileprivate let mainModelFormat:String = "emake"

/*: "pt" :*/
fileprivate let dataSharedPath:String = "pshould"

/*: "GJ21FB" :*/
fileprivate let userPathId:String = "GJ21noB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let app_appearId:String = "完成了检"
fileprivate let kItemMsg:String = "测,\u{8bf7}求状态正\u{5e38}"

/*: "接口请求失败" :*/
fileprivate let user_startName:String = "接\u{53e3}\u{8bf7}\u{6c42}失"
fileprivate let k_valueUrl:String = "\u{8d25}"

/*: "完成了检测,需要更换server url" :*/
fileprivate let notiNameKey:[Character] = ["完","\u{6210}","\u{4e86}","检","测",",","需","要","\u{66f4}","换","s","e","r"]
fileprivate let noti_modelTitle:String = "selectede"
fileprivate let k_rawLeadingTitle:String = "text count busy viewr url"

/*: "HTTPProxy" :*/
fileprivate let show_statusPath:String = "HTTyearyear"
fileprivate let appIndexMsg:String = "rtablexy"

/*: "__SCOPED__" :*/
fileprivate let userCenterName:[Character] = ["_","_","S","C","O","P"]
fileprivate let appActualStr:String = "message array infoED__"

/*: "tap" :*/
fileprivate let appWhiteFormat:String = "topap"

/*: "tun" :*/
fileprivate let userTitleId:String = "viewun"

/*: "ipsec" :*/
fileprivate let notiLabTaskUrl:[Character] = ["i","p","s","e","c"]

/*: "ppp" :*/
fileprivate let showLoadPath:String = "PPP"

/*: "func__updateTalkingCookies =  :*/
fileprivate let data_barMakeValue:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n"]
fileprivate let appTapCurveUrl:String = "tab if modelgCook"

/*: "https:// :*/
fileprivate let mainAreaTitle:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let showCustomMsg:[UInt8] = [0x85,0x96,0x8d,0x80,0xbc,0xbc,0x87,0x86,0x8f,0x86,0x97,0x86,0xb0,0x82,0x8e,0x86,0xa0,0x8c,0x8c,0x88,0x8a,0x86,0x90,0xb4,0x8a,0x97,0x8b,0xb6,0x91,0x8f,0xc3,0xde,0xc3]

private func endUser(model num: UInt8) -> UInt8 {
    return num ^ 227
}

/*: "UID" :*/
fileprivate let notiMakeData:String = "itemID"

/*: "PHPSESSID" :*/
fileprivate let show_contentData:String = "indexPSE"

/*: "func__saveAllCookies =  :*/
fileprivate let notiValueSharedFormat:[Character] = ["f","u","n","c","_","_","s"]
fileprivate let notiCardFillTitle:String = "aveAtrue fee true make share"
fileprivate let user_nameContent:String = "okies = request sense stroke"

/*: "func__saveAllCookies  error" :*/
fileprivate let kCellHiddenTitle:String = "layer transform bagfunc_"
fileprivate let mainMakeMsg:String = "llCofrom view var equal date"
fileprivate let noti_roundPath:[Character] = [" ","e","r","r","o","r"]

/*: "🍪 删除 :*/
fileprivate let k_modelData:String = "🍪 删manager"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  OffPromiseAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum ToScanBidirectionalCollection: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class OffPromiseAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = OffPromiseAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        sendPoint()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(const_showName))) || newValue.hasPrefix((notiDetailKey.replacingOccurrences(of: "view", with: "t") + appColorName.capitalized)) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if noti_halfId && !app_buttonMessage {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(main_hiddenId)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(main_hiddenId)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    listCookies(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func sendPoint() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = data_onEndFormat.integer(forKey: userControlMessage)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (constInnerKey.replacingOccurrences(of: "array", with: "er") + "rSel" + String(kTextUrl.prefix(5)) + String(notiViewStr)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = appLoad()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_showName))
        //: if Environment_Formal && !Encryption_Request {
        if noti_halfId && !app_buttonMessage {
            //: serverPrefix = "http://"
            serverPrefix = (String(const_showName))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(user_backgroundWithFormat) + String(appMakeName.prefix(4)) + "lStr = ") + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func appLoad() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func sessionImage(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(const_userName.prefix(4))) + "\(notiKeyStr)" + (String(data_matchFormat.prefix(4))), (String(const_userName.prefix(4))) + "\(notiKeyStr)" + (String(data_matchFormat.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(constToValue.suffix(8)) + String(noti_clearMsg)),
         //: "recharge/applePayNotify",
         (String(show_viewStr) + String(notiEqualValue) + String(userDataFormat.prefix(6)) + "yNotify"),
         //: "user/appleLogin"] + slowPathArr1
         (appCenterTitle.lowercased() + "/app" + String(showLabelValue.suffix(7)))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(dataColorName.prefix(5)) + String(appSizeName.suffix(6)) + String(constRawUrl)),
         //: "mf/business/sendMsg",
         (String(show_valuePath) + String(appRowValue)),
         //: "mf/business/sendGift",
         (String(constMaxTipSubMessage.suffix(7)) + String(const_pushStr.suffix(6)) + String(mainAllowId.suffix(7))),
         //: "mf/chatRoom/sendMsg"]
         (String(dataToText.suffix(4)) + "hatRo" + String(mainTitleUrl.suffix(4)) + "endMsg")]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = MixInReactiveCompatible.getSystemLangCode()
        let lang = MixInReactiveCompatible.withEvent()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .clickLength(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (userPathId.replacingOccurrences(of: "no", with: "F")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension OffPromiseAddrTool {
    //: func checkServerUrl() {
    func commentTap() {
        //: if StatisticalTableReactiveCompatible.share.networkStatus != .Unavailable {
        if StatisticalTableReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            ignore()
            //: } else {
        } else {
            //: func_starCodeTime()
            constantUp()
        }
    }

    //: func func_starCodeTime() {
    func constantUp() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.ignore()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func betweenUser() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func ignore() {
        //: if StatisticalTableReactiveCompatible.share.networkStatus == .Unavailable {
        if StatisticalTableReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        betweenUser()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        WithReactiveCompatible.adult { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (app_appearId.capitalized + kItemMsg))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (user_startName + k_valueUrl))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.constantUp()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(notiNameKey) + noti_modelTitle.replacingOccurrences(of: "selected", with: "v") + String(k_rawLeadingTitle.suffix(5))))
                    //: self.func__changeHostUrl()
                    self.appOn()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func appOn() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        betweenUser()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        data_onEndFormat.set(serverSelctIndex, forKey: userControlMessage)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_showName))
        //: if Environment_Formal && !Encryption_Request {
        if noti_halfId, !app_buttonMessage {
            //: serverPrefix = "http://"
            serverPrefix = (String(const_showName))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension OffPromiseAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func used() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(show_statusPath.replacingOccurrences(of: "year", with: "P") + appIndexMsg.replacingOccurrences(of: "table", with: "o"))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func master() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(userCenterName) + String(appActualStr.suffix(4)))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((appWhiteFormat.replacingOccurrences(of: "top", with: "t"))) || keyStr.contains((userTitleId.replacingOccurrences(of: "view", with: "t"))) || keyStr.contains((String(notiLabTaskUrl))) || keyStr.contains((showLoadPath.lowercased())) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension OffPromiseAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func listCookies(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(data_barMakeValue) + String(appTapCurveUrl.suffix(5)) + "ies = ") + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = old()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(const_showName)), with: (notiDetailKey.replacingOccurrences(of: "view", with: "t") + appColorName.capitalized))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((notiDetailKey.replacingOccurrences(of: "view", with: "t") + appColorName.capitalized)) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(mainAreaTitle)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            colorTap(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        funcDataUpwards()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func colorTap(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: showCustomMsg.map{endUser(model: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func old() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(const_showName))
        //: if Environment_Formal && !Encryption_Request {
        if noti_halfId, !app_buttonMessage {
            //: serverPrefix = "https://"
            serverPrefix = (notiDetailKey.replacingOccurrences(of: "view", with: "t") + appColorName.capitalized)
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(const_userName.prefix(4))) + "\(notiKeyStr)" + (String(data_matchFormat.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func modelNote(note _: Notification) {
        //: if StatisticalTableReactiveCompatible.share.loginUid.isEmptyString {
        if StatisticalTableReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            aboveHtmlVersion()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            searchCustomer()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func aboveHtmlVersion() {
        //: var htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (notiMakeData.replacingOccurrences(of: "item", with: "U")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(notiMakeData.replacingOccurrences(of: "item", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (notiMakeData.replacingOccurrences(of: "item", with: "U")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(notiMakeData.replacingOccurrences(of: "item", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID") {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((notiMakeData.replacingOccurrences(of: "item", with: "U"))), h5Cookie.keys.contains((notiMakeData.replacingOccurrences(of: "item", with: "U"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(notiMakeData.replacingOccurrences(of: "item", with: "U"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(notiMakeData.replacingOccurrences(of: "item", with: "U"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")), h5Cookie.keys.contains((show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func searchCustomer() {
        //: let htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (notiMakeData.replacingOccurrences(of: "item", with: "U")), cookie.value == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID") {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (notiMakeData.replacingOccurrences(of: "item", with: "U")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(notiMakeData.replacingOccurrences(of: "item", with: "U"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID") {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((notiMakeData.replacingOccurrences(of: "item", with: "U"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(notiMakeData.replacingOccurrences(of: "item", with: "U"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(show_contentData.replacingOccurrences(of: "index", with: "PH") + "SSID")] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func funcDataUpwards() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(notiValueSharedFormat) + String(notiCardFillTitle.prefix(4)) + "llCo" + String(user_nameContent.prefix(8))) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            data_onEndFormat.set(cookieData, forKey: mainLiveData)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(kCellHiddenTitle.suffix(5)) + "_saveA" + String(mainMakeMsg.prefix(4)) + "okies " + String(noti_roundPath)))
        }
    }

    //: func func__setAppCookies() {
    func transplant() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = data_onEndFormat.object(forKey: mainLiveData)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func sectionDismissCookies() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        data_onEndFormat.removeObject(forKey: mainLiveData)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
