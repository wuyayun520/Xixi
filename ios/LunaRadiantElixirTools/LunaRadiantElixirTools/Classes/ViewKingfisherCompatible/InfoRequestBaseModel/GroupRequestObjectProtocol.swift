
//: Declare String Begin

/*: / :*/
fileprivate let notiMakeMessage:String = "share"

/*:  ( :*/
fileprivate let user_contentFormat:[Character] = [" ","("]

/*: ; build: :*/
fileprivate let appEqualEnableQuoteValue:String = "; build:title view self direction"

/*: ; iOS  :*/
fileprivate let kSelectLikeMsg:String = "add"
fileprivate let appTimePathMsg:[Character] = [" ","i","O","S"," "]

/*: ;  :*/
fileprivate let dataRawImageStr:String = ";color"

/*: )" :*/
fileprivate let notiTitleName:String = ")"

/*: "audio" :*/
fileprivate let mainMakeTitle:String = "audofo"

/*: + :*/
fileprivate let data_managerModelName:String = "+"

/*: .wav" :*/
fileprivate let appTableHalfStr:String = ".wavlet lab extension"

/*: "audio/wav" :*/
fileprivate let noti_atId:String = "viewd"
fileprivate let const_colorData:String = "interval show end label rawio/wav"

/*: .jpg" :*/
fileprivate let userEnableKey:String = ".jpgto send bottom let"

/*: "image/jpeg" :*/
fileprivate let user_selectName:[Character] = ["i","m","a","g","e"]
fileprivate let main_toolListPath:[Character] = ["/","j","p","e","g"]

/*: "://app." :*/
fileprivate let dataWithKey:String = "://aptip value item"
fileprivate let notiImageId:String = "make casep."

/*: "data" :*/
fileprivate let main_equalAddPath:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let mainScanStr:[Character] = [":","n","u","l","l"]

/*: ":{}" :*/
fileprivate let noti_mediumFileUrl:String = ":{content"

/*: "Request failed, Try again later" :*/
fileprivate let mainColorContent:[UInt8] = [0xcb,0xfc,0xe8,0xec,0xfc,0xea,0xed,0xb9,0xff,0xf8,0xf0,0xf5,0xfc,0xfd,0xb5,0xb9,0xcd,0xeb,0xe0,0xb9,0xf8,0xfe,0xf8,0xf0,0xf7,0xb9,0xf5,0xf8,0xed,0xfc,0xeb]

private func ownerFinish(center num: UInt8) -> UInt8 {
    return num ^ 153
}

/*: "json error" :*/
fileprivate let show_partId:String = "let gesture modejson "
fileprivate let noti_infoPath:String = "ebuttonbuttonobutton"

/*: "platform=iphone&version= :*/
fileprivate let dataMainBackgroundStr:String = "plmodelfor"
fileprivate let show_toPlayerKey:[Character] = ["m","=","i","p","h","o","n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let userAddText:String = "color model language&packa"
fileprivate let app_toMsg:[Character] = ["="]

/*: &bundleId= :*/
fileprivate let appQuoteColorKey:String = "&bundleItrack as title"
fileprivate let const_layerMessage:[Character] = ["d","="]

/*: &lang= :*/
fileprivate let userCollectionKey:String = "&lang=return control fill manager"

/*: "?!@#$^&%*+,:;='" :*/
fileprivate let notiMainPath:[Character] = ["?","!","@","#","$","^","&","%","*"]
fileprivate let constMessageKey:[Character] = ["+",",",":",";","=","\'"]

/*: "`<>()[]{}/\\| " :*/
fileprivate let constAttributeUrl:[Character] = ["`","<",">","(",")","[","]","{"]
fileprivate let kBeautyMessage:String = "}/\\| "

/*: "/route/slowindex" :*/
fileprivate let const_useValue:String = "collection shared/route/s"
fileprivate let k_atNameFormat:String = "LOWINDEX"

/*: "/route/index" :*/
fileprivate let constViewStr:String = "reading search/rout"
fileprivate let constTimeId:[Character] = ["x"]

/*: "?path= :*/
fileprivate let show_colorValue:[Character] = ["?","p","a","t","h"]
fileprivate let showArraySharedPath:[Character] = ["="]

/*: "&GJNonceStr= :*/
fileprivate let k_nowPath:[Character] = ["&"]
fileprivate let show_textViewMessage:String = "model text phone viewGJNonc"

/*: "& :*/
fileprivate let k_numberId:[Character] = ["&"]

/*: = :*/
fileprivate let const_leadingPath:String = "="

/*: "&params= :*/
fileprivate let constCurrentFormat:[Character] = ["&","p","a","r","a","m","s","="]

/*: "/ :*/
fileprivate let appPostText:String = "/"

/*: "? :*/
fileprivate let noti_textStr:String = "?"

/*: "token" :*/
fileprivate let dataPathName:String = "tocoloren"

/*: "请求成功 :*/
fileprivate let data_labId:String = "请求成功"

/*: "AAAAAAAA" :*/
fileprivate let notiViewStr:String = "arrayarrayarrayA"
fileprivate let noti_modelMsg:String = "log"

/*: "AA" :*/
fileprivate let userTableData:String = "clickclick"

/*: "BB" :*/
fileprivate let k_viewFormat:[Character] = ["B","B"]

/*: "下载完成 -  :*/
fileprivate let mainToPartyKey:[Character] = ["下","载","\u{5b8c}","成"," ","-"," "]

/*: "/Documents/" :*/
fileprivate let noti_firstPath:String = "/"
fileprivate let mainBeanValue:String = "attach add topDocum"

/*: ".mp4" :*/
fileprivate let showBlackFormat:String = ".mp4beauty event height show action"

/*: "移动地址: :*/
fileprivate let showDataAfterMsg:[Character] = ["移","动","地","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let notiViewId:String = "后台任务下载\u{56de}"
fileprivate let user_alongKey:[Character] = ["来"]

/*: "下载成功 : :*/
fileprivate let kMakeDestroyPath:[Character] = ["下"]
fileprivate let data_modeValue:[Character] = ["载","成","功"," ",":"]

/*: "Net Error" :*/
fileprivate let constIndexText:[Character] = ["N"]
fileprivate let user_titleMoreImageUrl:[Character] = ["e","t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let appViewStr:String = "下frame败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GroupRequestObjectProtocol.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let dataAgeKey = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let k_clickViewNetStr = "\(dataLimitClickMsg)/\(show_buttonUseMessage) (\(showConnectName)" + (String(appEqualEnableQuoteValue.prefix(8))) + "\(noti_marginErrorLiveMessage)" + (kSelectLikeMsg.replacingOccurrences(of: "add", with: ";") + String(appTimePathMsg)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: ToErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let show_pageTitle = GroupRequestObjectProtocol()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class GroupRequestObjectProtocol: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func withCompletion(model: ContentBlockReactiveCompatible, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.arrayInput() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.gold(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.giftTitle(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func gold(model: ContentBlockReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.targetStatus(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.supposititiousAnnals(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    mediumResponse(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ToErrorResponse(errorCode: ResultVarArg.NetError.rawValue, errorMsg: app_netValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        alongShow(showMsg: app_netValue)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (mainMakeTitle.replacingOccurrences(of: "of", with: "i")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.betweenEnd())+\(showNoName)" + (String(appTableHalfStr.prefix(4))), mimeType: (noti_atId.replacingOccurrences(of: "view", with: "au") + String(const_colorData.suffix(6))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(userEnableKey.prefix(4))), mimeType: (String(user_selectName) + String(main_toolListPath)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    mediumResponse(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ToErrorResponse(errorCode: ResultVarArg.NetError.rawValue, errorMsg: app_netValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        alongShow(showMsg: app_netValue)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func giftTitle(model: ContentBlockReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.targetStatus(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.supposititiousAnnals(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: app_buttonMessage ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = dataAgeKey }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    mediumResponse(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ToErrorResponse(errorCode: ResultVarArg.NetError.rawValue, errorMsg: app_netValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        alongShow(showMsg: app_netValue)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = dataAgeKey }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    mediumResponse(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ToErrorResponse(errorCode: ResultVarArg.NetError.rawValue, errorMsg: app_netValue))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        alongShow(showMsg: app_netValue)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func mediumResponse(model: ContentBlockReactiveCompatible, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            OffPromiseAddrTool.share.listCookies(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if app_buttonMessage, model.requestServer.contains((String(dataWithKey.prefix(5)) + String(notiImageId.suffix(2)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.showWindowKey(key: OffPromiseAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(main_equalAddPath)) + "\"" + (String(mainScanStr)), with: "" + "\"" + (String(main_equalAddPath)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !app_buttonMessage {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(mainScanStr)), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<FirstHandyJSON>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == ResultVarArg.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, ToErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    alongShow(showMsg: responseModel.msg ?? String(bytes: mainColorContent.map{ownerFinish(center: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case ResultVarArg.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: notiCornerRecordUrl, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, ToErrorResponse(errorCode: ResultVarArg.NetError.rawValue, errorMsg: (String(show_partId.suffix(5)) + noti_infoPath.replacingOccurrences(of: "button", with: "r")).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func targetStatus(model: ContentBlockReactiveCompatible) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (dataMainBackgroundStr.replacingOccurrences(of: "model", with: "at") + String(show_toPlayerKey)) + "\(constClickUrl)" + (String(userAddText.suffix(6)) + "geId" + String(app_toMsg)) + "\(showNoName)" + (String(appQuoteColorKey.prefix(8)) + String(const_layerMessage)) + "\(showConnectName)" + (String(userCollectionKey.prefix(6))) + "\(OffPromiseAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if app_buttonMessage, serverUrl.contains((String(dataWithKey.prefix(5)) + String(notiImageId.suffix(2)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if OffPromiseAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(const_useValue.suffix(8)) + k_atNameFormat.lowercased()))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(constViewStr.suffix(5)) + "e/inde" + String(constTimeId))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(show_colorValue) + String(showArraySharedPath)) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(k_nowPath) + String(show_textViewMessage.suffix(6)) + "eStr=") + "\(OffPromiseAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.textVersion(key: OffPromiseAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(constCurrentFormat)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func supposititiousAnnals(model: ContentBlockReactiveCompatible) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(k_clickViewNetStr)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (dataPathName.replacingOccurrences(of: "color", with: "k")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func requestObserver(model: ContentBlockReactiveCompatible) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.targetStatus(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (data_labId.capitalized) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (notiViewStr.replacingOccurrences(of: "array", with: "AA") + noti_modelMsg.replacingOccurrences(of: "log", with: "A"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (String(mainToPartyKey)) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (noti_firstPath.capitalized + String(mainBeanValue.suffix(5)) + "ents/") + NSDate.betweenEnd() + (String(showBlackFormat.prefix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(showDataAfterMsg)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (notiViewId + String(user_alongKey)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = DataReactiveCompatible.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func sunnahOn(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: main_informationData.paring6decrypt(), with: notiKeyStr)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(kMakeDestroyPath) + String(data_modeValue)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ToErrorResponse(errorCode: ResultVarArg.Normal.rawValue, errorMsg: (String(constIndexText) + String(user_titleMoreImageUrl)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ToErrorResponse(errorCode: ResultVarArg.NetError.rawValue, errorMsg: (String(constIndexText) + String(user_titleMoreImageUrl)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (appViewStr.replacingOccurrences(of: "frame", with: "载失")) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func buttonDelete(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: main_informationData.paring6decrypt(), with: notiKeyStr)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(kMakeDestroyPath) + String(data_modeValue)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ToErrorResponse(errorCode: ResultVarArg.Normal.rawValue, errorMsg: (String(constIndexText) + String(user_titleMoreImageUrl)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ToErrorResponse(errorCode: ResultVarArg.NetError.rawValue, errorMsg: (String(constIndexText) + String(user_titleMoreImageUrl)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (appViewStr.replacingOccurrences(of: "frame", with: "载失")) + "\(errMsg)")
            }
        }
    }
}
