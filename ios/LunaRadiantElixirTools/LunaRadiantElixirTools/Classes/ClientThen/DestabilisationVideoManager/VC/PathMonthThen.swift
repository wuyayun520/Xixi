
//: Declare String Begin

/*: "cmd" :*/
fileprivate let user_shareValue:[UInt8] = [0x76,0x78,0x71]

/*: "cancelCall" :*/
fileprivate let mainTableFormat:String = "C"
fileprivate let kFileValue:String = "anviewe"

/*: "data" :*/
fileprivate let kMessageData:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "logId" :*/
fileprivate let userCellValue:String = "app model to application lengthlogId"

/*: "#322870" :*/
fileprivate let appEraseText:String = "#322870"

/*: "Tap to start video chatting" :*/
fileprivate let appPathTitle:String = "error frame modeTap to"
fileprivate let user_sizeValue:String = "t vidplayer show voice stroke"
fileprivate let k_finishKey:[Character] = ["t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let user_titleStatusMsg:String = "icon_self should"
fileprivate let mainDoingUrl:[Character] = ["_","n","o","r"]

/*: "icon_views_number" :*/
fileprivate let app_suiteData:String = "icon_vivoice nor self self"
fileprivate let data_showBroadName:String = "ews_ntemp var name"

/*: "%@ chatting" :*/
fileprivate let kVoicePath:[Character] = ["%","@"," ","c","h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let appFitNameStr:String = "iagen"
fileprivate let user_showKey:String = "ngrender"

/*: "icon_rank_coin" :*/
fileprivate let mainVideoArrayViewData:String = "icon_rsocial some model with"
fileprivate let const_errorStr:String = "backn"

/*: "vip_btn_top" :*/
fileprivate let kAllowData:String = "vip_self model mode if true"
fileprivate let show_sensePath:[Character] = ["p"]

/*: "btn_nearby_settings_nor" :*/
fileprivate let show_containerUrl:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t","i","n","g","s","_","n"]
fileprivate let app_iconId:[Character] = ["o","r"]

/*: "%@ Coins/min" :*/
fileprivate let constEqualValue:[Character] = ["%","@"," ","C","o","i","n","s","/"]
fileprivate let data_networkText:String = "insiden"

/*: "get json error" :*/
fileprivate let dataAppPath:String = "like result count price nameget j"
fileprivate let noti_cellSizeMsg:String = "your"

/*: "matchId" :*/
fileprivate let constPerspectiveManagerRequestName:String = "var text title self equalmatchId"

/*: "errno" :*/
fileprivate let show_keyStr:[UInt8] = [0x2b,0x3c,0x3c,0x20,0x21]

private func targetSucceed(app num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "VIPFee" :*/
fileprivate let main_userFormat:[Character] = ["V"]
fileprivate let k_colorViewData:[Character] = ["I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let user_ofValue:[Character] = ["M"]
fileprivate let notiTargetFormat:[Character] = ["a","t","c","h","i","n","g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let showLengthPath:[Character] = ["u","s","e","r","T","o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let const_toMsg:[Character] = ["m","f","/","f","r","e","e"]
fileprivate let user_fileTitle:[Character] = ["C","a","l","l","/","g","e","t","V","i","d","e","o","I","n","f","o"]

/*: "fromType" :*/
fileprivate let kReplyName:[UInt8] = [0x37,0x43,0x40,0x3e,0x25,0x4a,0x41,0x36]

fileprivate func modelValue(make num: UInt8) -> UInt8 {
    let value = Int(num) + 47
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "You've been barred from receiving calls" :*/
fileprivate let show_tempName:[UInt8] = [0xeb,0x1,0x7,0xb9,0x8,0xf7,0xb2,0xf4,0xf7,0xf7,0x0,0xb2,0xf4,0xf3,0x4,0x4,0xf7,0xf6,0xb2,0xf8,0x4,0x1,0xff,0xb2,0x4,0xf7,0xf5,0xf7,0xfb,0x8,0xfb,0x0,0xf9,0xb2,0xf5,0xf3,0xfe,0xfe,0x5]

fileprivate func modelShareLiveReason(main num: UInt8) -> UInt8 {
    let value = Int(num) + 110
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "source" :*/
fileprivate let mainBottomMessageTitle:[UInt8] = [0x98,0x94,0x9a,0x97,0x88,0x8a]

fileprivate func reportBlock(to num: UInt8) -> UInt8 {
    let value = Int(num) - 37
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let noti_videoData:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "uid" :*/
fileprivate let const_ofMessage:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let show_sharedSizeTitle:String = "fromFrsearch button make"
fileprivate let const_minLabName:String = "for texteeCall"

/*: "requestCall" :*/
fileprivate let userDecreaseText:String = "bottomque"
fileprivate let constNumberoValue:String = "class half sizestCall"

/*: "onVideoMatch" :*/
fileprivate let showModelFormat:String = "onVidtop if user stage"
fileprivate let appComponentPath:String = "cell direction hidden aseoMatch"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let k_beginQuoteContent:[UInt8] = [0x92,0x88,0xbc,0xbd,0xa6,0xa4,0xa8,0xbd,0xa0,0xaa,0xe9,0xbf,0xa0,0xad,0xac,0xa6,0x94,0xe9,0x9b,0xac,0xaa,0xac,0xa0,0xbf,0xac,0xe9,0xbf,0xa0,0xad,0xac,0xa6,0xe9,0xaa,0xa8,0xa5,0xa5,0xe9,0xa0,0xa7,0xaa,0xa6,0xa4,0xa0,0xa7,0xae]

private func toEvent(frame num: UInt8) -> UInt8 {
    return num ^ 201
}

/*: "refuseCall" :*/
fileprivate let main_tipKey:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "onCancelCall" :*/
fileprivate let const_toTitle:String = "user superonCa"
fileprivate let const_keyName:String = "Callname normal var array"

/*: "retryAfter" :*/
fileprivate let notiViewFormat:[Character] = ["r","e","t","r","y","A"]
fileprivate let appBottomUrl:[Character] = ["f","t","e","r"]

/*: "onRequestCall" :*/
fileprivate let kModelId:String = "to manageronReq"

/*: "failType" :*/
fileprivate let data_methodMessage:String = "background raw remove model hiddenfailTyp"
fileprivate let const_fieldStr:String = "show"

/*: "callWaitingTime" :*/
fileprivate let showStatusPath:String = "system ascallW"
fileprivate let showEqualContent:String = "labele"

/*: "toUid" :*/
fileprivate let userBottomFormat:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let noti_viewKeyMsg:[Character] = ["F","r","e","e"," ","1"," ","m","i","n"]

/*: "%@/min" :*/
fileprivate let app_talkNameMessage:String = "invite type input%@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PathMonthThen.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class PathMonthThen: DropBaseViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.costPrepare()
            }
        }
        //: self.resetCamera()
        self.reset()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        betweenSetup()
        //: setupSubViewsConstraint()
        responseUp()
        //: bindInteraction()
        largeTitle()
        //: videoManager.startPreview(isMatch: true)
        videoManager.fingertipBack(isMatch: true)
        //: setPlayer(isRandon: false)
        afterTop(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            cast()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            MakeSocketDelegate.shared.buildExecute(info: [String(bytes: user_shareValue.map{$0^21}, encoding: .utf8)!: (mainTableFormat.lowercased() + kFileValue.replacingOccurrences(of: "view", with: "c") + "lCall"), String(bytes: kMessageData.reversed(), encoding: .utf8)!: [(String(userCellValue.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        everyPlayer()
        //: if TalkingSocketManager.shared.isInfo == false {
        if MakeSocketDelegate.shared.isInfo == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            MakeSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            MakeSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            MakeSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            MakeSocketDelegate.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ToCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = ToCloudDelegate()
        //: let info = TalkingVideoChatModel()
        let info = EqualChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .blindConstraint()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(cast), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: appUseMessage + 1, height: kStatusName + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (appEraseText.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(appPathTitle.suffix(6)) + " star" + String(user_sizeValue.prefix(5)) + "eo cha" + String(k_finishKey)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(user_titleStatusMsg.prefix(5)) + "close" + String(mainDoingUrl))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(walkingInsert), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: TalkingItemButton = {
        //: let btn = TalkingButton()
        let btn = TalkingItemButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(app_suiteData.prefix(7)) + String(data_showBroadName.prefix(5)) + "umber")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.barName(name: (String(app_suiteData.prefix(7)) + String(data_showBroadName.prefix(5)) + "umber")), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.colorTitle(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(kVoicePath)).arguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.barName(name: (appFitNameStr.replacingOccurrences(of: "age", with: "co") + "_lou" + user_showKey.replacingOccurrences(of: "render", with: "e")))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.barName(name: (String(mainVideoArrayViewData.prefix(6)) + "ank_co" + const_errorStr.replacingOccurrences(of: "back", with: "i")))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.colorTitle(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.barName(name: (String(kAllowData.prefix(4)) + "btn_to" + String(show_sensePath)))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: TalkingItemButton = {
        //: let btn = TalkingButton()
        let btn = TalkingItemButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(appResume), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.barName(name: (String(show_containerUrl) + String(app_iconId))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.barName(name: (String(mainVideoArrayViewData.prefix(6)) + "ank_co" + const_errorStr.replacingOccurrences(of: "back", with: "i")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .dataConverterColor()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(constEqualValue) + data_networkText.replacingOccurrences(of: "inside", with: "mi")).arguments(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.colorTitle(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: TableCapacityReactiveCompatible = //: return TableCapacityReactiveCompatible(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension PathMonthThen {
    //: private func setPlayer(isRandon: Bool) {
    private func afterTop(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = RequestThen.default.littleness(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(dataAppPath.suffix(5)) + "son er" + noti_cellSizeMsg.replacingOccurrences(of: "you", with: "ro")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func apiece(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(constPerspectiveManagerRequestName.suffix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: user_shareValue.map{$0^21}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: show_keyStr.map{targetSucceed(app: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        DestabilisationVideoManager.sumimate(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.managerPicture()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(constPerspectiveManagerRequestName.suffix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func usGovernment() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        DestabilisationVideoManager.breed { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(main_userFormat) + String(k_colorViewData))].intValue
            //: self.refreshVideoCallPrice()
            self.costPrepare()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension PathMonthThen {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func appResume() {
        //: EditPushManager.share.func__pushToSubscribeAlert()
        EditPushManager.share.increaseAction()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func walkingInsert() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        showBtn()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        showSenseFormat.modelNeed(eventID: notiClickValue)

        //: if StatisticalTableReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if StatisticalTableReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.overModel()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        currentViewController()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func cast() {
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard MakeSocketDelegate.shared.isInfo == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.alongShow(showMsg: userClickMessage)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        beforeDismiss()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(user_ofValue) + String(notiTargetFormat)).localized
        //: setPlayer(isRandon: true)
        afterTop(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if StatisticalTableReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
        if StatisticalTableReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            showSenseFormat.modelNeed(eventID: kBusyId)
            //: req_getVideoInfo()
            changeInfo()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            showSenseFormat.modelNeed(eventID: mainCountimateUrl)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            MakeSocketDelegate.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            MakeSocketDelegate.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            MakeSocketDelegate.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            MakeSocketDelegate.shared.errorDelegate = self
            //: restart_randomVideo()
            transform()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func transform(cmd: String = (String(showLengthPath)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        everyPlayer()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        apiece(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func managerPicture() {
        // 停止定时器
        //: destroyWaitingTimer()
        everyPlayer()
        //: showCloseBtn()
        showBtn()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(appPathTitle.suffix(6)) + " star" + String(user_sizeValue.prefix(5)) + "eo cha" + String(k_finishKey)).localized
        //: setPlayer(isRandon: false)
        afterTop(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isInfo == false {
        if MakeSocketDelegate.shared.isInfo == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            MakeSocketDelegate.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            MakeSocketDelegate.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            MakeSocketDelegate.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            MakeSocketDelegate.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        costPrepare()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func everyPlayer() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func changeInfo() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(const_toMsg) + String(user_fileTitle))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: kReplyName.map{modelValue(make: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.managerPicture()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: StatisticalTableReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    StatisticalTableReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.cast()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.alongShow(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = ToHandyJSON.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = ScreenPlayListener()
                //: player.setMute(bEnable: true)
                player.modelObserver(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.primrosePath(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                RequestReactiveCompatible.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                UpPermissionTool.pathActionEqual { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.tableDHote()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func tableDHote() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        showSenseFormat.modelNeed(eventID: dataQuantityName)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard RequestReactiveCompatible.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.alongShow(showMsg: String(bytes: show_tempName.map{modelShareLiveReason(main: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            RequestReactiveCompatible.shared.userSecondCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.managerPicture()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = AttributeRecognizerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.managerPicture()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func beforeDismiss() {
        //: if StatisticalTableReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if StatisticalTableReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(StatisticalTableReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(StatisticalTableReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func showBtn() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func reset() {
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard MakeSocketDelegate.shared.isInfo == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.fingertipBack()
        }
    }
}

// MARK: - TableObjectProtocol

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension PathMonthThen: TableObjectProtocol {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func isVideo(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = MakeMixModelType.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(constPerspectiveManagerRequestName.suffix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: mainBottomMessageTitle.map{reportBlock(to: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: noti_videoData.reversed(), encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: const_ofMessage.reversed(), encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = DigitiserSendable.randow
        //: if StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && StatisticalTableReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && StatisticalTableReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(show_sharedSizeTitle.prefix(6)) + String(const_minLabName.suffix(6))))
            //: TalkingSocketManager.shared.isFreeCall = true
            MakeSocketDelegate.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: user_shareValue.map{$0^21}, encoding: .utf8)!: (userDecreaseText.replacingOccurrences(of: "bottom", with: "re") + String(constNumberoValue.suffix(6))), String(bytes: kMessageData.reversed(), encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        MakeSocketDelegate.shared.buildExecute(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            MakeSocketDelegate.shared.buildExecute(info: [String(bytes: user_shareValue.map{$0^21}, encoding: .utf8)!: (mainTableFormat.lowercased() + kFileValue.replacingOccurrences(of: "view", with: "c") + "lCall"), String(bytes: kMessageData.reversed(), encoding: .utf8)!: [(String(userCellValue.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.transform(cmd: (String(showModelFormat.prefix(5)) + String(appComponentPath.suffix(7))))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func christenMakeSituation(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(userCellValue.suffix(5)))].intValue
    }
}

// MARK: - TitleObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension PathMonthThen: TitleObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func addDown(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func socketOrCollection(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func commit(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func lineUpRemove(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            DataReactiveCompatible.header(body: String(bytes: k_beginQuoteContent.map{toEvent(frame: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = MakeMixModelType.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            openEffect(toUid: String(uid))
        }
        //: stop_randomVideo()
        managerPicture()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.livelihoodShouldLooseAll()
        //: StatisticalTableReactiveCompatible.share.start1v1TalkCall(info: data, situation: .Calling)
        StatisticalTableReactiveCompatible.share.mottle(info: data, situation: .Calling)
    }
}

// MARK: - BeanObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension PathMonthThen: BeanObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func reject(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        transform(cmd: (String(main_tipKey)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func viewObject(data _: [String: Any]) {}
}

// MARK: - PopObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension PathMonthThen: PopObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func toCurrent(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(const_toTitle.suffix(4)) + "ncel" + String(const_keyName.prefix(4))) { // 取消通话异常
            //: stop_randomVideo()
            managerPicture()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(showModelFormat.prefix(5)) + String(appComponentPath.suffix(7))), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            managerPicture()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(constPerspectiveManagerRequestName.suffix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.alongShow(showMsg: msg)
            //: uploadFailTime(type: 2)
            transomWindow(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(showModelFormat.prefix(5)) + String(appComponentPath.suffix(7))), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(notiViewFormat) + String(appBottomUrl))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(constPerspectiveManagerRequestName.suffix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.transform(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(kModelId.suffix(5)) + "uestCall"), errorNo == DrawKeyRepresentable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            managerPicture()
            //: guard StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue else { return }
            //: EditPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            EditPushManager.share.statusOf(clickEvent: userErrorStatusBarId, sufficient: false)
            //: uploadFailTime(type: 1)
            transomWindow(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        transform(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension PathMonthThen {
    //: func uploadFailTime(type: Int) {
    func transomWindow(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(data_methodMessage.suffix(7)) + const_fieldStr.replacingOccurrences(of: "show", with: "e"))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(showStatusPath.suffix(5)) + "aitingT" + showEqualContent.replacingOccurrences(of: "label", with: "im"))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        showSenseFormat.popStr(eventID: appImageText, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func openEffect(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(showStatusPath.suffix(5)) + "aitingT" + showEqualContent.replacingOccurrences(of: "label", with: "im"))] = time
        //: parm["toUid"] = toUid
        parm[(String(userBottomFormat))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        showSenseFormat.popStr(eventID: kNoId, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension PathMonthThen {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func costPrepare() {
        //: guard StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = DigitiserSendable.randow
        //: if StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && StatisticalTableReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes > 0, StatisticalTableReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if StatisticalTableReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if StatisticalTableReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(noti_viewKeyMsg)).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(app_talkNameMessage.suffix(6))).arguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(app_talkNameMessage.suffix(6))).arguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.finishFont(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func betweenSetup() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func responseUp() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(user_keyId + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func largeTitle() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(costPrepare),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: show_bottomFormatStr,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(usGovernment),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: kScaleTitle,
                                               //: object: nil)
                                               object: nil)
    }
}
