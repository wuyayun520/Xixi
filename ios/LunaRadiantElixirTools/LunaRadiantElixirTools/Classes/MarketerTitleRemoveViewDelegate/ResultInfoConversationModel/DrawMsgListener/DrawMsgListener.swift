
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let mainToResultName:[Character] = ["I","M","退","\u{51fa}","登","录","\u{5931}"]
fileprivate let data_femaleText:String = "败，code:let else block no view"

/*: , desc: :*/
fileprivate let constAddUrl:String = "gender bar view, desc"
fileprivate let constMaleRequestUrl:[Character] = [":"]

/*: ." :*/
fileprivate let appValueKey:String = "."

/*: "IM登录失败，code: :*/
fileprivate let const_timePath:String = "icon mode selfIM登录失"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let const_showTitle:String = "IM获\u{53d6}\u{672a}读消"
fileprivate let userSourceStr:[Character] = ["息","总","数","失","败","，","c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let const_phaseName:String = "frameus"

/*: "badNumber" :*/
fileprivate let kScaleMessage:[UInt8] = [0x72,0x65,0x62,0x6d,0x75,0x4e,0x64,0x61,0x62]

/*: "plaza" :*/
fileprivate let dataUpMsg:String = "pmodelza"

/*: "roomNum" :*/
fileprivate let mainStackId:String = "share nor at self nameroomNum"

/*: "num" :*/
fileprivate let noti_playerData:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let constBarHeadacheFormat:[Character] = ["o","n","l","y","O","n","e"]
fileprivate let k_countUrl:String = "RoomIdtime let model player return"

/*: "LIVE-CHAT" :*/
fileprivate let appKeyFormat:String = "LIVE-CHATin item collection"

/*: "PARTY-CHAT" :*/
fileprivate let dataAddGiftKey:String = "PARTY-Cmake program"
fileprivate let kToCropMessage:String = "touchT"

/*: "extra" :*/
fileprivate let show_arrayMsg:[Character] = ["e","x","t","r","a"]

/*: "msgType" :*/
fileprivate let show_makeName:[Character] = ["m","s","g","T","y","p"]
fileprivate let user_playerKey:String = "along"

/*: "newPkgGift" :*/
fileprivate let user_toMsg:String = "newPself cell"

/*: "MF:ChatMentionMsg" :*/
fileprivate let data_equalId:[Character] = ["M","F",":","C","h","a","t","M","e"]
fileprivate let const_whiteName:String = "equal content lab leftntionMsg"

/*: "uid" :*/
fileprivate let main_sizeValue:[UInt8] = [0x3c,0x20,0x2d]

/*: "onlineStatus" :*/
fileprivate let noti_modelViewPath:[UInt8] = [0x51,0x50,0x4e,0x4b,0x50,0x47,0x35,0x56,0x43,0x56,0x57,0x55]

fileprivate func collectionTarget(icon num: UInt8) -> UInt8 {
    let value = Int(num) - 226
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "isNewUser" :*/
fileprivate let mainNameId:[UInt8] = [0xb9,0xa3,0x9e,0xb5,0xa7,0x85,0xa3,0xb5,0xa2]

private func colorCell(submit num: UInt8) -> UInt8 {
    return num ^ 208
}

/*: "c2c_ :*/
fileprivate let constQuantityimateNowLeadingStr:String = "source namec2c_"

/*: "IM获取会话失败，convId: :*/
fileprivate let main_videoUrl:String = "IM\u{83b7}取"
fileprivate let notiShowGenderData:String = "name add，convId"
fileprivate let kImageData:[Character] = [":"]

/*: , code: :*/
fileprivate let showArrayValue:String = ", codpath model"
fileprivate let constPostViewMsg:String = "as size rawe:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let const_likeTitle:String = "IM\u{63d2}入消"
fileprivate let noti_toTextStr:String = "adelayge"
fileprivate let user_colorUrl:String = "tId:let view if var"

/*: "changeDirection" :*/
fileprivate let constReadText:String = "CHANG"
fileprivate let const_viewItemKeyData:[Character] = ["e","D","i","r","e","c","t","i","o","n"]

/*: "rYMsgType" :*/
fileprivate let const_toId:String = "rYMsgtitle actual model language"

/*: "GJ:CallCustom" :*/
fileprivate let dataActualKey:String = "to list content equalGJ:Ca"
fileprivate let mainModelMessage:[Character] = ["l","l","C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let notiMakeToTitle:[Character] = ["I","M","获","\u{53d6}","会","话"]
fileprivate let userManagerValue:[Character] = ["列","表","失","\u{8d25}","，","c","o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let constJumpFormat:String = "msgInfodown add data"

/*: "needFold" :*/
fileprivate let appBlockFormat:String = "normal"
fileprivate let show_timeName:String = "eedFoldmini center selected"

/*: "group_ :*/
fileprivate let show_resultCenterColorName:[Character] = ["g","r","o","u","p","_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let data_closeContent:String = "as background cos stringIM删除"
fileprivate let userAllMenuValue:[Character] = ["c","o","n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let show_dataWithMakeKey:String = "type leading elseIM置顶会话"
fileprivate let user_sendTitle:String = "onvId:view self info"

/*: "SDK 正在连接到服务器" :*/
fileprivate let kKeyMessage:String = "quick background randomSDK 正"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let app_succeedSoundStr:String = "SDK \u{5df2}"
fileprivate let notiEqualMsg:String = "接到服\u{52a1}器"

/*: "IM连接服务器失败，code: :*/
fileprivate let showMeEnterValue:String = "IM连接with app background make"
fileprivate let appErrorStr:String = "invite userode:"

/*: , error: :*/
fileprivate let dataCellPriceMessage:String = ", errerror medium self"
fileprivate let showDoingMessage:String = "page if if object downor:"

/*: "IM账号被挤下线。" :*/
fileprivate let app_colorTitle:[Character] = ["I","M","账","号","被","挤"]
fileprivate let showKindStr:[Character] = ["下","线","。"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let appListName:[UInt8] = [0x2f,0x19,0x3,0x4,0x56,0x17,0x15,0x15,0x19,0x3,0x18,0x2,0x56,0x1f,0x5,0x56,0x1a,0x19,0x11,0x11,0x13,0x12,0x56,0x1f,0x18,0x56,0x19,0x18,0x56,0x17,0x18,0x19,0x2,0x1e,0x13,0x4,0x56,0x12,0x13,0x0,0x1f,0x15,0x13,0x56,0x17,0x18,0x12,0x56,0xf,0x19,0x3,0x56,0x17,0x4,0x13,0x56,0x10,0x19,0x4,0x15,0x13,0x12,0x56,0x2,0x19,0x56,0x11,0x19,0x56,0x19,0x10,0x10,0x1a,0x1f,0x18,0x13,0x57,0x56,0x3f,0x10,0x56,0x1f,0x2,0x56,0x1f,0x5,0x56,0x18,0x19,0x2,0x56,0x12,0x19,0x18,0x13,0x56,0x14,0xf,0x56,0x1b,0x13,0x5a,0x56,0x2,0x1e,0x13,0x56,0x6,0x17,0x5,0x5,0x1,0x19,0x4,0x12,0x56,0x1b,0x17,0xf,0x56,0x1e,0x17,0x0,0x13,0x56,0x14,0x13,0x13,0x18,0x56,0x1a,0x13,0x17,0x1d,0x13,0x12,0x5a,0x56,0x17,0x18,0x12,0x56,0x1f,0x2,0x56,0x1f,0x5,0x56,0x4,0x13,0x15,0x19,0x1b,0x1b,0x13,0x18,0x12,0x13,0x12,0x56,0x2,0x19,0x56,0x15,0x1e,0x17,0x18,0x11,0x13,0x56,0x2,0x1e,0x13,0x56,0x6,0x17,0x5,0x5,0x1,0x19,0x4,0x12,0x58]

private func pathCellConfirm(view num: UInt8) -> UInt8 {
    return num ^ 118
}

/*: "Offline Notification" :*/
fileprivate let notiMomentStr:String = "Offlin interval"
fileprivate let mainViewName:String = "TIFICAT"
fileprivate let data_modelTitle:[Character] = ["i","o","n"]

/*: "Cancel" :*/
fileprivate let show_locationMessage:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let appNumberData:[Character] = ["R","e"," ","L","o"]
fileprivate let noti_afterActionId:String = "makein"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DrawMsgListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol DelegateObjectProtocol: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func towardPosition(data: [ResultInfoConversationModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func withCount(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func userDetailMsg(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum SaveDataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class DrawMsgListener: NSObject {
@objcMembers public class DrawMsgListener: NSObject {
    //: public static let shared = DrawMsgListener()
    public static let shared = DrawMsgListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [ResultInfoConversationModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [ResultInfoConversationModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [ResultInfoConversationModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [ResultInfoConversationModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [ResultInfoConversationModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: ResultInfoConversationModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.toRemove()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func showBar() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(data_screenId, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func toRemove() {
        //: func__removeAllDelegate()
        withDelegate()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.itemSegment()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func itemSegment() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = ResultInfoConversationModel.observerType(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = ResultInfoConversationModel.observerType(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func stateLive() -> [ResultInfoConversationModel] {
        // 审核模式，在消息列表移除官方客服
        //: if StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue {
        if StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == MixInReactiveCompatible.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == MixInReactiveCompatible.turnOver() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension DrawMsgListener {
extension DrawMsgListener {
    //: func func__removeAllDelegate() {
    func withDelegate() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func tip(_ delegate: DelegateObjectProtocol) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func popSend(_ delegate: DelegateObjectProtocol) {
        //: self.func__invokeDelegate {
        self.customer {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func customer(_ invocation: (DelegateObjectProtocol) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! DelegateObjectProtocol)
        }
    }
}

// MARK: - IM登录态管理

//: extension DrawMsgListener {
extension DrawMsgListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func equalGift() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            FeedLogTool.matchVoice(msg: (String(mainToResultName) + String(data_femaleText.prefix(7))) + "\(code)" + (String(constAddUrl.suffix(6)) + String(constMaleRequestUrl)) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.toRemove()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func theIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = StatisticalTableReactiveCompatible.share.appUserConfigMode.uSig
        let uSigStr = StatisticalTableReactiveCompatible.share.appUserConfigMode.uSig
        //: let uidStr = StatisticalTableReactiveCompatible.share.loginUserMode.userID
        let uidStr = StatisticalTableReactiveCompatible.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.hire()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            FeedLogTool.matchVoice(msg: (String(const_timePath.suffix(5)) + "败，code:") + "\(code)" + (String(constAddUrl.suffix(6)) + String(constMaleRequestUrl)) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func searchUpCanOperateList() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || StatisticalTableReactiveCompatible.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || StatisticalTableReactiveCompatible.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.alongShow(showMsg: app_netValue)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension DrawMsgListener {
extension DrawMsgListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func hire() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            FeedLogTool.matchVoice(msg: (const_showTitle + String(userSourceStr)) + "\(code)" + (String(constAddUrl.suffix(6)) + String(constMaleRequestUrl)) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func slab() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: ChatContentReactiveCompatible.req_sysMessageList { _, result in
        ChatContentReactiveCompatible.agendaRadiogramInfoCompletion { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(const_phaseName.replacingOccurrences(of: "frame", with: "cr") + "hBadge")] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: kPathTitle, object: self, userInfo: [String(bytes: kScaleMessage.reversed(), encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func sourceSumeraction(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: ChatContentReactiveCompatible.req_getChatRoomInfo { succeed, result, _ in
        ChatContentReactiveCompatible.actionSession { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(dataUpMsg.replacingOccurrences(of: "model", with: "la"))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(mainStackId.suffix(7)))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = ResultInfoConversationModel.observerType(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(noti_playerData))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(constBarHeadacheFormat) + String(k_countUrl.prefix(6)))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension DrawMsgListener: V2TIMAdvancedMsgListener {
extension DrawMsgListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(appKeyFormat.prefix(9)))) || msg.groupID.contains((String(dataAddGiftKey.prefix(7)) + kToCropMessage.replacingOccurrences(of: "touch", with: "HA")))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            LeadingThen.sendDoing().anima(msg: msg)
            //: return
            return
        }
        //: if msg.sender == StatisticalTableReactiveCompatible.share.loginUserMode.userID { return }
        if msg.sender == StatisticalTableReactiveCompatible.share.loginUserMode.userID { return }

        //: let dic = WearerReactiveCompatible.parseTXMessageData(data: msg.customElem.data)
        let dic = WearerReactiveCompatible.overPushData(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(String(show_arrayMsg))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? OverModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: noti_appName, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(show_makeName) + user_playerKey.replacingOccurrences(of: "along", with: "e"))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(user_toMsg.prefix(4)) + "kgGift") { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: const_buttonMsg, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if WearerReactiveCompatible.checkVoiceMessage(message: msg) {
            if WearerReactiveCompatible.moreAt(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = KitThen.botchAd(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                VoiceReactiveCompatible.keyDb(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.isometric(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SellerAnimatTool.shared.deviceDic(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            SellerAnimatTool.shared.filenameRemove(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(data_equalId) + String(const_whiteName.suffix(8))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            DeleteThen.share.rowComponent(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: BackstageChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: BackstageChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != MixInReactiveCompatible.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != MixInReactiveCompatible.messageTo() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.customer { $0.userDetailMsg?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            ManagingDirectorReactiveCompatible.shared.afterNextMedium(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: main_sizeValue.map{$0^73}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: noti_modelViewPath.map{collectionTarget(icon: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: mainNameId.map{colorCell(submit: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: data_statusScreenStr,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            UserTitleInfoManager.pull(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            BackstageChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: show_nameMsg, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: ReloadViewCellData, isPrivete: Bool) {
    @objc public func atomicSum85(targetId: String, msgCellData: ReloadViewCellData, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.formatIndex(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(constQuantityimateNowLeadingStr.suffix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.capacityList(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.funcInsideRollObserver(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func formatIndex(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = StatisticalTableReactiveCompatible.share.loginUserMode.userID
        let senderStr = StatisticalTableReactiveCompatible.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func isometric(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(constReadText.lowercased() + String(const_viewItemKeyData))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && EditPushManager.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(const_toId.prefix(5)) + "Type")].stringValue == (String(dataActualKey.suffix(5)) + String(mainModelMessage)) && isSelf && EditPushManager.share.colorVc()!.isKind(of: ImageBannerDelegate.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: showScreenTitle) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: showScreenTitle)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension DrawMsgListener: V2TIMConversationListener {
extension DrawMsgListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.capacityList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.funcInsideRollObserver(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.capacityList(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.funcInsideRollObserver(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: StatisticalTableReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        StatisticalTableReactiveCompatible.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.customer {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.withCount?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func funcInsideRollObserver(conVersationList: [ResultInfoConversationModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if UserTitleInfoManager.nearPost().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                UserTitleInfoManager.nearPost().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.customer {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.towardPosition?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func shoppingList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [ResultInfoConversationModel] = []

        // V2TIMConversation->ResultInfoConversationModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = ResultInfoConversationModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.commonConv(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [ResultInfoConversationModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = FeedInfoTable.constraintFill(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.detailAdd(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension DrawMsgListener {
extension DrawMsgListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func dataCompletion(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.shoppingList(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.funcInsideRollObserver(conVersationList: arr as? [ResultInfoConversationModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            FeedLogTool.matchVoice(msg: (String(notiMakeToTitle) + String(userManagerValue)) + "\(code)" + (String(constAddUrl.suffix(6)) + String(constMaleRequestUrl)) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func rubricId(targetId: String) -> ResultInfoConversationModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func capacityList(convList: [V2TIMConversation]) -> [ResultInfoConversationModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [ResultInfoConversationModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = ResultInfoConversationModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.commonConv(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.detailAdd(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func detailAdd(convModel: ResultInfoConversationModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.itemSegment()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: SaveDataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - StatisticalTableReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - StatisticalTableReactiveCompatible.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = UserTitleInfoManager.managerView(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = WearerReactiveCompatible.parseTXMessageData(data: elemData)
            let dict = WearerReactiveCompatible.overPushData(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(String(show_arrayMsg))][(String(constJumpFormat.prefix(7)))][(appBlockFormat.replacingOccurrences(of: "normal", with: "n") + String(show_timeName.prefix(7)))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.isUpType(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.isUpType(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func isUpType(convList: [ResultInfoConversationModel], dataType: SaveDataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func targetType(targetId: String, dataType: SaveDataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(constQuantityimateNowLeadingStr.suffix(4))) + "\(targetId)" : (String(show_resultCenterColorName)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            UserTitleInfoManager.objectEnter(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(StatisticalTableReactiveCompatible.share.loginUserMode.userID, toUid: targetId)
            VoiceReactiveCompatible.press(StatisticalTableReactiveCompatible.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.hire()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func supplement(convModel: ResultInfoConversationModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.isUpType(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.isUpType(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension DrawMsgListener: V2TIMSDKListener {
extension DrawMsgListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(kKeyMessage.suffix(5)) + "\u{5728}连接到服务器"))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (app_succeedSoundStr + "经成功\u{8fde}" + notiEqualMsg))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        FeedLogTool.matchVoice(msg: (String(showMeEnterValue.prefix(4)) + "\u{670d}务\u{5668}失败，c" + String(appErrorStr.suffix(4))) + "\(code)" + (String(dataCellPriceMessage.prefix(5)) + String(showDoingMessage.suffix(3))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        FeedLogTool.matchVoice(msg: (String(app_colorTitle) + String(showKindStr)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        MakeSocketDelegate.shared.option(userLoginOut: true)
        //: DrawMsgListener.shared.isConnection = false
        DrawMsgListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (ViewKingfisherCompatible.shared as! ViewKingfisherCompatible).lastDestroy()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: appListName.map{pathCellConfirm(view: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = DigitizerAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if OffPromiseAddrTool.share.interfaceLang == ToScanBidirectionalCollection.ar.rawValue || OffPromiseAddrTool.share.interfaceLang == ToScanBidirectionalCollection.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            OffPromiseAddrTool.share.interfaceLang == ToScanBidirectionalCollection.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.finishFont(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        BlockThen.rangeConfig()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        BlockThen.rangeConfig(title: (String(notiMomentStr.prefix(4)) + "ine No" + mainViewName.lowercased() + String(data_modelTitle)).localized, message: msg, leftBtnTitle: (String(show_locationMessage)).localized, rightBtnTitle: (String(appNumberData) + noti_afterActionId.replacingOccurrences(of: "make", with: "g")).localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ActionReactiveCompatible.editShared().justTime()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            DrawSaloonReactiveCompatible.signShared().nowClickItem()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            MixItemReactiveCompatible.ingot { t in
                //: ProgressHUD.dismiss()
                BeanNameProgressHUD.doingBlock()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: notiCornerRecordUrl, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            MixItemReactiveCompatible.ammunition { t in
                //: if t {
                if t {
                    //: DrawMsgListener.shared.func__LogingIn { _ in
                    DrawMsgListener.shared.theIn { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if ActionReactiveCompatible.editShared().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(ActionReactiveCompatible.editShared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    MakeSocketDelegate.shared.liveSocket()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
