
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showTagMsg:[UInt8] = [0xeb,0xec,0xeb,0xf6,0xaa,0xe1,0xed,0xe6,0xe7,0xf0,0xb8,0xab,0xa2,0xea,0xe3,0xf1,0xa2,0xec,0xed,0xf6,0xa2,0xe0,0xe7,0xe7,0xec,0xa2,0xeb,0xef,0xf2,0xee,0xe7,0xef,0xe7,0xec,0xf6,0xe7,0xe6]

private func sourceBlock(white num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "roomId" :*/
fileprivate let kCellStr:[Character] = ["r","o","o","m","I","d"]

/*: "type" :*/
fileprivate let mainSizeStr:[UInt8] = [0x32,0x3f,0x36,0x23]

private func advertisingView(equal num: UInt8) -> UInt8 {
    return num ^ 70
}

/*: "extra" :*/
fileprivate let showArrayUrl:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let data_modelMessage:[UInt8] = [0x37,0x28,0x30,0x2d,0x16,0x38,0x24]

/*: "Send Gift" :*/
fileprivate let app_makeFormat:[Character] = ["S","e","n"]
fileprivate let k_sexPath:String = "d Giftcontent size image"

/*: "View info" :*/
fileprivate let constEnterMsg:String = "Visession view for"
fileprivate let appViewMenuId:String = "view icon var type showew info"

/*: "Report" :*/
fileprivate let mainEqualData:String = "view else mReport"

/*: "uid" :*/
fileprivate let dataTopCustomFormat:[UInt8] = [0x63,0x7f,0x72]

private func linguisticContent(view num: UInt8) -> UInt8 {
    return num ^ 22
}

/*: "name" :*/
fileprivate let data_senseInputStr:[UInt8] = [0xed,0xe0,0xec,0xe4]

fileprivate func actionImage(to num: UInt8) -> UInt8 {
    let value = Int(num) + 129
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't send blank message" :*/
fileprivate let userVideoUrl:[Character] = ["C","a","n","\'","t"," ","s"]
fileprivate let data_iconTitle:[Character] = ["e","n","d"," ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let notiHeadShowName:String = "uslabr"

/*: "content" :*/
fileprivate let userFrameFormat:[UInt8] = [0xee,0xe2,0xe3,0xf9,0xe8,0xe3,0xf9]

private func addViewSize(content num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "msgType" :*/
fileprivate let kRequestText:[Character] = ["m","s","g","T","y","p","e"]

/*: "txt" :*/
fileprivate let const_titleId:[Character] = ["t","x","t"]

/*: "isGroup" :*/
fileprivate let constAttentionTitle:String = "slide returnisGro"
fileprivate let noti_viewStr:[Character] = ["u","p"]

/*: "jumps" :*/
fileprivate let mainButtonPath:String = "jumimages"

/*: "message" :*/
fileprivate let mainActionMakeBottomStr:[UInt8] = [0x23,0x2b,0x3d,0x3d,0x2f,0x29,0x2b]

private func classBound(equal num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "TxtMsg" :*/
fileprivate let const_sourceMsg:String = "with table make equalTxtMsg"

/*: "toUid" :*/
fileprivate let show_makeProgressId:String = "text to new returntoUid"

/*: "msgInfo" :*/
fileprivate let data_commonRequestTitle:String = "view to pastmsgInfo"

/*: "params" :*/
fileprivate let show_emptyNoMsg:[UInt8] = [0x73,0x6d,0x61,0x72,0x61,0x70]

/*: "contentType" :*/
fileprivate let kShareText:[UInt8] = [0x3f,0x33,0x32,0x28,0x39,0x32,0x28,0x8,0x25,0x2c,0x39]

private func artisticStyle(view num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "ImgMsg" :*/
fileprivate let main_inputPopName:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let showEditFormat:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let appFlagStr:[UInt8] = [0x40,0x44,0x48,0x4e,0x4c,0x7c,0x5b,0x40]

/*: "pic" :*/
fileprivate let show_hiddenKey:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let userThumbMessage:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let show_videoKey:[Character] = ["t","o"]
fileprivate let app_equalMsg:String = "record videotalMf"

/*: "giftId" :*/
fileprivate let mainFirstMessage:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let main_playerMessage:[Character] = ["g","i","f","t","N"]
fileprivate let appFilterName:String = "ulabel"

/*: "pkgItemsetId" :*/
fileprivate let app_normalKey:String = "pkgItview text when share"
fileprivate let user_resultUrl:String = "app image to height whiteemsetId"

/*: "msg" :*/
fileprivate let app_hiddenText:String = "msdata"

/*: "tips" :*/
fileprivate let kUserAtRightFormat:String = "tofps"

/*: "gift" :*/
fileprivate let main_appId:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let show_cookiePath:String = "after"
fileprivate let show_gameFormat:[Character] = ["i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let constPathUrl:[UInt8] = [0x32,0x3f,0x3b,0x3e,0xa,0x33,0x39]

/*: "All Numbers" :*/
fileprivate let k_labelMsg:[Character] = ["A","l","l"," ","N","u","m","b","e","r","s"]

/*: "sendNum" :*/
fileprivate let app_equalTitle:String = "for var selfsendNu"
fileprivate let mainSharedData:String = "number"

/*: "toUser" :*/
fileprivate let notiGiftUrl:String = "toUserform progress pan guard other"

/*: "msgIncome" :*/
fileprivate let notiArrayUrl:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "isConnection" :*/
fileprivate let main_feeTheFormat:String = "quality left let make finishisConn"
fileprivate let dataErrorMessage:String = "moment"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BignessViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class BignessViewController: DropBaseViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = ToPathInfoModel() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = ResultInfoConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = ToPathInfoModel.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showTagMsg.map{sourceBlock(white: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if StatisticalTableReactiveCompatible.share.appConfigMode.disableShootScreen {
            if StatisticalTableReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ClientShieldView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        nookAndCranny()
        //: createUIConstraints()
        sizeConstraints()
        //: bindInteraction()
        totalerchangeSumeraction()
        //: addTapGestureRecognizer()
        toEnableLength()
        //: func__chatRoom_insertTipMsg()
        videoList()
        //: chatSettings()
        showTitle()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            anyComponent()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.deviceJoin(join: false)
        //: self.destroyTipsTimer()
        self.fromPick()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: ExplainTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ExplainTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(ToViewCell.self, forCellReuseIdentifier: ToViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ToCreateThen.self, forCellReuseIdentifier: ToCreateThen.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(RequestMessageCell.self, forCellReuseIdentifier: RequestMessageCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(CapabilityReactiveCompatible.self, forCellReuseIdentifier: CapabilityReactiveCompatible.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: BroadcastThen.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(MixViewCell.self, forCellReuseIdentifier: BroadcastThen.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: LeadingInputView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = LeadingInputView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: RequestMakeView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = RequestMakeView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: MotionTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = MotionTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: TableKitView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = TableKitView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: FeedTitleThen = {
        //: let v = TalkingSomeoneAtMeView()
        let v = FeedTitleThen()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension BignessViewController {
    /// 群聊设置
    //: private func chatSettings() {
    private func showTitle() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        DeleteThen.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        deviceJoin(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func deviceJoin(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(kCellStr)): self.conversationData.groupID, String(bytes: mainSizeStr.map{advertisingView(equal: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: ChatContentReactiveCompatible.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        ChatContentReactiveCompatible.nameTag(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - ControllerFile

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension BignessViewController: ControllerFile {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ReloadViewCellData! {
    func accountingData(_: ExplainTableViewController!, shrug data: V2TIMMessage!) -> ReloadViewCellData! {
        //: let dic = WearerReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = WearerReactiveCompatible.overPushData(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = KitThen.flyByNightUnpaidWorker(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ReloadViewCellData!) -> BroadcastThen! {
    func along(for _: ExplainTableViewController!, pause data: ReloadViewCellData!) -> BroadcastThen! {
        //: if data.isKind(of: PerspectiveThen.self) {
        if data.isKind(of: PerspectiveThen.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = ToViewCell(style: .default, reuseIdentifier: ToViewCell.className())
            //: let cellData = data as! PerspectiveThen
            let cellData = data as! PerspectiveThen
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.meanSun(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.tv(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: ItemCellData.self) {
        if data.isKind(of: ItemCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ToCreateThen(style: .default, reuseIdentifier: ToCreateThen.className())
            //: let cellData = data as! ItemCellData
            let cellData = data as! ItemCellData
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: VideoCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = RequestMessageCell(style: .default, reuseIdentifier: RequestMessageCell.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! VideoCellData
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SourceCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = CapabilityReactiveCompatible(style: .default, reuseIdentifier: CapabilityReactiveCompatible.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SourceCellData
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: BroadcastThen!) {
    func message(_: ExplainTableViewController!, alongAcross cell: BroadcastThen!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(StatisticalTableReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(StatisticalTableReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                meanSun(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                addUser(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: BroadcastThen!) {
    func singleOut(_: ExplainTableViewController!, contrastInfo cell: BroadcastThen!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ToCreateThen.self) { // 图片
            //: let imageCellData = cell.messageData as! ItemCellData
            let imageCellData = cell.messageData as! ItemCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(notiClickText)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = TalkingHandyJSON()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = MakeHandyJSON()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [TalkingHandyJSON] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = InsertPhotosVc(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: ToViewCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! ToViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = WearerReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = WearerReactiveCompatible.overPushData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(showArrayUrl))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: data_modelMessage.map{$0^93}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<RateModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EditPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EditPushManager.share.child(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: CapabilityReactiveCompatible.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! CapabilityReactiveCompatible
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = WearerReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = WearerReactiveCompatible.overPushData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(showArrayUrl))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: data_modelMessage.map{$0^93}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<RateModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EditPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EditPushManager.share.child(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: BroadcastThen!) {
    func effect(_: ExplainTableViewController!, user cell: BroadcastThen!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        rawData(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension BignessViewController {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func fromPick() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func addUser(user: AccessModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = MotionPromiseViewDelegate(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.addList(cellTitleList: [(String(app_makeFormat) + String(k_sexPath.prefix(6))).localized, "@", (String(constEnterMsg.prefix(2)) + String(appViewMenuId.suffix(7))).localized, (String(mainEqualData.suffix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.notFirst(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.tantalum(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.meanSun(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.white(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func notFirst(user: AccessModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.circle()
        //: let model = TalkingChatRoomMemberModel()
        let model = SaveMeasurable()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.hypothesis(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func tantalum(user: AccessModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: dataTopCustomFormat.map{linguisticContent(view: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: data_senseInputStr.map{actionImage(to: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.selectDict(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func meanSun(uid: Int) {
        //: EditPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        EditPushManager.share.suddenVideo(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func white(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = SizeThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.pathView(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension BignessViewController {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func loadApp(msgStr: String, quoteModel _: ExamineedModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            alongShow(showMsg: (String(userVideoUrl) + String(data_iconTitle)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: data_modelMessage.map{$0^93}, encoding: .utf8)!: (notiHeadShowName.replacingOccurrences(of: "lab", with: "e")), String(bytes: dataTopCustomFormat.map{linguisticContent(view: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: userFrameFormat.map{addViewSize(content: $0)}, encoding: .utf8)!: temStr, (String(kRequestText)): (String(const_titleId)), (String(constAttentionTitle.suffix(5)) + String(noti_viewStr)): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(mainButtonPath.replacingOccurrences(of: "image", with: "p"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: mainActionMakeBottomStr.map{classBound(equal: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: mainSizeStr.map{advertisingView(equal: $0)}, encoding: .utf8)!: (String(const_sourceMsg.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(kCellStr)): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(show_makeProgressId.suffix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_commonRequestTitle.suffix(7))): msgInfo, String(bytes: show_emptyNoMsg.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            rawData(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func corner(img: UIImage) {
        //: let imagePath = ItemCellData.getChatImagePath(image: img)
        let imagePath = ItemCellData.atEnable(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: kShareText.map{artisticStyle(view: $0)}, encoding: .utf8)!: (String(main_inputPopName)), (String(kRequestText)): (String(showEditFormat)), String(bytes: appFlagStr.map{$0^41}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(show_hiddenKey)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: mainSizeStr.map{advertisingView(equal: $0)}, encoding: .utf8)!: (String(main_inputPopName)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(kCellStr)): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_commonRequestTitle.suffix(7))): msgInfo, String(bytes: show_emptyNoMsg.reversed(), encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            rawData(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ReloadViewCellData, reSend: Bool) {
    private func rawData(msgData: ReloadViewCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            alongShow(showMsg: app_netValue)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.index(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(show_hiddenKey))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(show_hiddenKey))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(show_hiddenKey))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: ChatContentReactiveCompatible.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        ChatContentReactiveCompatible.firstMiniUser(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.ofGermanism(msgData, application: nil, oeuvreStatus: NSNumber(value: 0), accelerationLeadershipIncomeAnglicism: .Msg_Status_Fail)
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.airt(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(data_commonRequestTitle.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.ofGermanism(msgData, application: msgInfo[(String(userThumbMessage))] as? String, oeuvreStatus: NSNumber(value: 0), accelerationLeadershipIncomeAnglicism: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(show_videoKey) + String(app_equalMsg.suffix(5)) + "Coin")) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(show_videoKey) + String(app_equalMsg.suffix(5)) + "Coin")] as! NSNumber
                //: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func airt(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard StatisticalTableReactiveCompatible.share.loginUserMode.status != 1 else {
            guard StatisticalTableReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    alongShow(showMsg: errorStr)
                }
                //: return
                return
            }

            //: EditPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            EditPushManager.share.statusOf(clickEvent: user_scaleMessage, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.trademark()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            viewIcon(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = DateView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName))
            //: view.show()
            view.reShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                alongShow(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension BignessViewController: CeremonyReactiveCompatible {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func consultingService(giftModel _: GroupModelType, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func accumulation() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func userEnableColor(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = PresentationRecognizerDelegate()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.selectDict(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func instil(actionType: FeedRelatableTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            barFuncManager()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func changeFor(giftModel _: GroupModelType, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func statusQuo(giftModel: GroupModelType, giftNum: String, model: SaveMeasurable) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            alongShow(showMsg: app_netValue)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(show_makeProgressId.suffix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(mainFirstMessage))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(main_playerMessage) + appFilterName.replacingOccurrences(of: "label", with: "m"))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(kCellStr))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(app_normalKey.prefix(5)) + String(user_resultUrl.suffix(7)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: ChatContentReactiveCompatible.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        ChatContentReactiveCompatible.roundOfCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.airt(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.stateFor(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.past(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func betise(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.value(true)
            self.msgView.value(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func contentBlock(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.value(true)
            self.msgView.value(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func section(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.section(tap)
    }

    //: func func__starRecordBtnDown() {}
    func towardTo() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func colorBy(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension BignessViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func barFuncManager() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        UpPermissionTool.showLocation(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = CreateVideoThen.notary(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: EditPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                EditPushManager.share.colorVc()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.corner(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func gestureValue() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        UpPermissionTool.utilityWith(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.corner(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension BignessViewController {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func underMention(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(kCellStr))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension BignessViewController {
    //: func func__TipsNotification() {
    func medium() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(lessImage(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: dataGameTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func lessImage(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SourceCellData = userinfo[(app_hiddenText.replacingOccurrences(of: "data", with: "g"))] as! SourceCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.value(true)
        self.msgView.value(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func viewIcon(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: userFrameFormat.map{addViewSize(content: $0)}, encoding: .utf8)!: tipStr, (String(kRequestText)): (kUserAtRightFormat.replacingOccurrences(of: "of", with: "i"))]
        //: var message: ReloadViewCellData?
        var message: ReloadViewCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = KitThen.streetSmart(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.value(true)
        self.msgView.value(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func videoList() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard StatisticalTableReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard StatisticalTableReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: StatisticalTableReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.viewIcon(tipStr: StatisticalTableReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension BignessViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func past(extral: [String: Any?], model: SaveMeasurable, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(data_commonRequestTitle.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(constAttentionTitle.suffix(5)) + String(noti_viewStr))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(kRequestText))] = (String(main_appId))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(data_commonRequestTitle.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: dataTopCustomFormat.map{linguisticContent(view: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (show_cookiePath.replacingOccurrences(of: "after", with: "n") + String(show_gameFormat)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: constPathUrl.map{$0^90}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(k_labelMsg)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(main_appId))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(main_appId))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(show_cookiePath.replacingOccurrences(of: "after", with: "n") + String(show_gameFormat))] = "\(giftdic[(String(app_equalTitle.suffix(6)) + mainSharedData.replacingOccurrences(of: "number", with: "m"))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(notiGiftUrl.prefix(6)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = KitThen.streetSmart(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.index(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(notiArrayUrl))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(notiArrayUrl))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.ofGermanism(cellData, application: msgInfoDic[(String(userThumbMessage))] as? String, oeuvreStatus: msgIncome, accelerationLeadershipIncomeAnglicism: .Msg_Status_Succ)
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        far(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(show_videoKey) + String(app_equalMsg.suffix(5)) + "Coin")) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(show_videoKey) + String(app_equalMsg.suffix(5)) + "Coin")] as! NSNumber
            //: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.obscure(needReload: false, mf_coin: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.tagButtonMembers()
    }

    //: func initCachEffectData() {
    func anyComponent() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.fileSend(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func far(giftDic: [String: Any], model: SaveMeasurable) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.labelIn(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in TableUpReactiveCompatible.share.underFinish() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [MakeTransformable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.allInput(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.everyErrorDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func bridePrice(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in TableUpReactiveCompatible.share.underFinish() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [MakeTransformable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.allInput(model: giftModel as! GiftHandyJSON)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.lagniappe(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension BignessViewController {
    /// 创建UI
    //: private func createUI() {
    private func nookAndCranny() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func sizeConstraints() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(user_viewSumervalKey)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(user_viewSumervalKey)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - constBarStr)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func totalerchangeSumeraction() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = PresentationRecognizerDelegate()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! ReloadViewCellData).isKind(of: PerspectiveThen.self) {
                    if (cellData as! ReloadViewCellData).isKind(of: PerspectiveThen.self) {
                        //: let groupCellData = cellData as! PerspectiveThen
                        let groupCellData = cellData as! PerspectiveThen
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(userThumbMessage))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: DrawMsgListener.shared.rx
        DrawMsgListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(main_feeTheFormat.suffix(6)) + "ectio" + dataErrorMessage.replacingOccurrences(of: "moment", with: "n")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
