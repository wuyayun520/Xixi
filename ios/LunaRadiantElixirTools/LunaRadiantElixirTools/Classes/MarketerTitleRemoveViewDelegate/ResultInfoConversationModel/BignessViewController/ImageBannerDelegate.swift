
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kErrorHiddenId:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "F5F5F5" :*/
fileprivate let app_theoryWithTitle:[Character] = ["F","5","F","5","F"]
fileprivate let noti_infoFormat:String = "5"

/*: "#128CFF" :*/
fileprivate let show_lastPlayerFalsePath:[Character] = ["#","1"]
fileprivate let k_originId:[Character] = ["2","8","C","F","F"]

/*: "Feedback" :*/
fileprivate let userViewFormat:String = "interrupt imageFee"
fileprivate let app_frameName:[Character] = ["d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let dataSubEndUrl:[Character] = ["#","8","3","7","0"]
fileprivate let showSharedUrl:String = "equal"

/*: "Quick Greeting" :*/
fileprivate let k_indicatorValue:String = "var make model height colorQuick "
fileprivate let main_frameMessage:String = "row addGreeting"

/*: "Beauty Photos" :*/
fileprivate let notiAddUrl:String = "Beautytext app show text time"
fileprivate let userPathWithoutMessage:String = "camera style let Photos"

/*: "url" :*/
fileprivate let showAppFrameOutTitle:String = "uuser"

/*: "unlockGift" :*/
fileprivate let userTotalStr:String = "UNLOCK"

/*: "%d" :*/
fileprivate let mainLabelValue:String = "height status%d"

/*: "isBurn" :*/
fileprivate let dataBuildValue:[UInt8] = [0x84,0x8e,0x5d,0x90,0x8d,0x89]

fileprivate func modelMessage(error num: UInt8) -> UInt8 {
    let value = Int(num) + 229
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let appIconFormat:String = "giftIshare player equal"
fileprivate let kAddFirstMessage:[Character] = ["d"]

/*: "rYMsgType" :*/
fileprivate let main_fromKey:String = "rYMrequest true target to"
fileprivate let user_listTitle:String = "content section event sign messagesgType"

/*: "GJ:CallCustom" :*/
fileprivate let k_mKey:String = "open make attach capacity lineGJ:"
fileprivate let k_heIndexPicContent:String = "request fill var in finishCall"

/*: "changeDirection" :*/
fileprivate let data_picId:String = "CHAN"
fileprivate let dataAddContent:String = "rectmessage"
fileprivate let app_modelId:String = "equal"

/*: "extra" :*/
fileprivate let show_arrayValue:String = "videotra"

/*: "msgInfo" :*/
fileprivate let const_fromPath:String = "msgInfocontent text status data"

/*: "toUid" :*/
fileprivate let mainLeadingData:[Character] = ["t","o","U","i","d"]

/*: "wantGift" :*/
fileprivate let kViewTextComponentFormat:[Character] = ["w","a","n","t","G","i","f","t"]

/*: "jumpKey" :*/
fileprivate let showDataUserUrl:[Character] = ["j","u","m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let mainSendTitle:[Character] = ["t","r","u","e","P","e","r","s","o"]
fileprivate let kCellValue:String = "nAuthedge tap view mode one"

/*: "logType" :*/
fileprivate let showDataValue:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let userEdgeData:[UInt8] = [0xf,0x11,0x5,0x36,0x1b,0x12,0x7]

private func textCenter(push num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "txt" :*/
fileprivate let userShowId:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let notiKeyRoundTitle:String = "tycolor"

/*: "QuickMsg" :*/
fileprivate let user_appearanceMsg:String = "Quicindex gender show let layer"

/*: "params" :*/
fileprivate let showRegularStr:[UInt8] = [0xc7,0xd6,0xc5,0xd6,0xda,0xc4]

/*: "content" :*/
fileprivate let user_equalMessage:[UInt8] = [0x8f,0x83,0x82,0x98,0x89,0x82,0x98]

private func headMove(time num: UInt8) -> UInt8 {
    return num ^ 236
}

/*: "id" :*/
fileprivate let notiLeadingFormat:[Character] = ["i","d"]

/*: "txtId" :*/
fileprivate let userLineData:String = "txtIdself empty"

/*: "message" :*/
fileprivate let kTimeMsg:[UInt8] = [0xb5,0xad,0xbb,0xbb,0xa9,0xaf,0xad]

fileprivate func imageStyle(interaction num: UInt8) -> UInt8 {
    let value = Int(num) + 184
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let userLoadStr:String = "error list view imageTxtMs"
fileprivate let notiEqualId:String = "G"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let app_showUrl:[UInt8] = [0x2f,0x48,0x42,0x3c,0x3e,0xf9,0x41,0x3e,0x45,0x45,0x48,0xf9,0x3d,0x48,0x50,0x47,0x45,0x48,0x3a,0x3d,0xf9,0x3f,0x3a,0x42,0x45,0x3e,0x3d,0x7,0xf9,0x29,0x4b,0x48,0x3c,0x3e,0x3d,0x4e,0x4b,0x3e]

fileprivate func openComment(video num: UInt8) -> UInt8 {
    let value = Int(num) - 217
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let kImageLabStr:[Character] = ["l","e","n","g","t"]
fileprivate let userFrameExplainPath:String = "player"

/*: "audio" :*/
fileprivate let const_toEqualFormat:String = "audnormal"

/*: "audioId" :*/
fileprivate let show_dismissHandleId:String = "audioIscale info manager skin"
fileprivate let k_locationMessage:String = "mode"

/*: "audioLength" :*/
fileprivate let show_stopValue:[UInt8] = [0xee,0xfa,0xeb,0xe6,0xe0,0xc3,0xea,0xe1,0xe8,0xfb,0xe7]

private func appVideo(share num: UInt8) -> UInt8 {
    return num ^ 143
}

/*: "audioSandbox" :*/
fileprivate let k_plusName:String = "aequaldio"
fileprivate let showManagerId:String = "boto"

/*: "senderId" :*/
fileprivate let main_sourceName:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x6e,0x65,0x73]

/*: "AudioMsg" :*/
fileprivate let show_beginFormat:String = "AudioMsgin for string"

/*: "Can't send blank message" :*/
fileprivate let showViewHiddenStr:String = "Can\'t "
fileprivate let data_matchName:[Character] = ["b","l","a"]
fileprivate let appGiftTitle:[Character] = ["n","k"," ","m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let app_totalFormat:String = "qubottome"

/*: "quoteMsgInfo" :*/
fileprivate let kTopMsg:String = "qsucceedote"
fileprivate let user_nowToFormat:[Character] = ["M","s","g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let app_cardStr:[UInt8] = [0xbc,0xb0,0xb1,0xab,0xba,0xb1,0xab,0x8b,0xa6,0xaf,0xba]

private func photoColor(zone num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "ImgMsg" :*/
fileprivate let data_labTitle:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let app_valueName:String = "imeconomic"

/*: "imageUri" :*/
fileprivate let data_betweenStr:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "customSend" :*/
fileprivate let noti_pathRegularStr:[UInt8] = [0xfa,0xc,0xa,0xb,0x6,0x4,0xea,0xfc,0x5,0xfb]

fileprivate func receiveLab(array num: UInt8) -> UInt8 {
    let value = Int(num) + 105
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let constEventDataStr:[Character] = ["p","i","c"]

/*: "VideoMsg" :*/
fileprivate let appLeadingData:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "video" :*/
fileprivate let show_fillValue:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let const_noseText:[UInt8] = [0xb7,0xbb,0xa2,0xb1,0xa6,0x9d,0xb9,0xb3]

/*: "videoUrl" :*/
fileprivate let const_sendData:[UInt8] = [0xd,0x12,0x1f,0x1e,0x14,0x2e,0x9,0x17]

private func terminalLayer(fatal num: UInt8) -> UInt8 {
    return num ^ 123
}

/*: "duration" :*/
fileprivate let showFlushTitle:[UInt8] = [0xd1,0xc0,0xc7,0xd4,0xc1,0xdc,0xda,0xdb]

private func topPopular(page num: UInt8) -> UInt8 {
    return num ^ 181
}

/*: "len" :*/
fileprivate let constRawStr:[UInt8] = [0x6,0xf,0x4]

/*: "Documents/User/voice/" :*/
fileprivate let data_lengthTitle:String = "info normal ifDocumen"
fileprivate let data_rangeId:[Character] = ["t","s","/","U","s","e","r","/","v","o","i","c","e","/"]

/*: "Documents/User/record/" :*/
fileprivate let kTimeTitle:String = "height elseDocum"
fileprivate let data_finishMsg:[Character] = ["/","U"]
fileprivate let appFemaleId:String = "of player gift selfser/re"

/*: "msgIncome" :*/
fileprivate let const_intimateId:String = "msgIncomein true view search"

/*: "mfBean" :*/
fileprivate let notiCaptureStr:[UInt8] = [0xaa,0xa1,0x85,0xa2,0xa6,0xa9]

private func listMake(network num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "messageType" :*/
fileprivate let notiModelText:String = "mcolorsag"
fileprivate let appInfoTitle:String = "as path nameeType"

/*: "WantGift" :*/
fileprivate let notiDataMsg:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let const_countMessage:String = "_Privafinish manager view content center"
fileprivate let app_backgroundValue:String = "value"
fileprivate let noti_actualPath:[Character] = ["T","i","p","s","T","i","t","l","e","_"]

/*: "msgId" :*/
fileprivate let userEffectText:String = "msgIdmodel style add extension"

/*: "totalMfCoin" :*/
fileprivate let user_wrapPath:String = "make"
fileprivate let data_blockKey:[Character] = ["o","t","a","l","M","f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let constBackUrl:[Character] = ["a","t"]
fileprivate let app_playerName:[Character] = ["t","a","c","h","S","y","s","M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let k_bagTitle:String = "videontvideo"
fileprivate let appReloadMsg:[Character] = ["h","o","t","o"]

/*: _ :*/
fileprivate let mainUserMsg:String = "name"

/*: "totalIntimate" :*/
fileprivate let notiBottomUrl:String = "totshould"
fileprivate let show_errorData:[Character] = ["I","n","t","i","m","a","t","e"]

/*: "uid" :*/
fileprivate let userAttachUrl:[UInt8] = [0x29,0x35,0x38]

private func bindPush(camera num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "intimate" :*/
fileprivate let userAfterData:[UInt8] = [0x2d,0x32,0x38,0x2d,0x31,0x25,0x38,0x29]

fileprivate func itemRefresh(equal num: UInt8) -> UInt8 {
    let value = Int(num) + 60
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let kActualTitle:[UInt8] = [0x7e,0x5c,0x53,0x1a,0x49,0x1d,0x48,0x4e,0x58,0x1d,0x49,0x55,0x58,0x1d,0x5b,0x48,0x53,0x5e,0x49,0x54,0x52,0x53,0x1d,0x59,0x48,0x58,0x1d,0x49,0x52,0x1d,0x44,0x52,0x48,0x4f,0x1d,0x51,0x5c,0x5e,0x56,0x1d,0x52,0x5b,0x1d,0x54,0x53,0x49,0x54,0x50,0x5c,0x5e,0x44,0x1d,0x51,0x58,0x4b,0x58,0x51,0x13]

private func withPath(position num: UInt8) -> UInt8 {
    return num ^ 61
}

/*: "giftNum" :*/
fileprivate let const_lengthData:String = "manageri"
fileprivate let dataMakeTitle:[Character] = ["f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let showZoneValue:[Character] = ["p","k","g","I","t","e","m","s","e","t","I"]
fileprivate let user_appKey:[Character] = ["d"]

/*: "from" :*/
fileprivate let user_cellId:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let main_leadingDataColorMsg:[Character] = ["w","a","n","t","G","i"]
fileprivate let kViewTextTitle:[Character] = ["f","t","I","d"]

/*: "WantGiftMsg" :*/
fileprivate let showBlockKey:String = "hidden name with text titleWantGi"
fileprivate let show_localShouldKey:String = "ftMsgshrink sub"

/*: "gift" :*/
fileprivate let noti_postTitle:String = "gilabelt"

/*: "View info" :*/
fileprivate let appToReceiveText:[Character] = ["V","i","e","w"," ","i","n","f"]
fileprivate let k_appFormat:String = "priority"

/*: "Notes" :*/
fileprivate let user_promptLabelMessage:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let mainItemValue:String = "as do image array toReport"

/*: "View read receipts?" :*/
fileprivate let k_editKey:[Character] = ["V","i","e","w"," ","r","e","a"]
fileprivate let noti_removeName:String = "voice"
fileprivate let appTextTitle:[Character] = [" ","r","e","c","e","i","p","t","s","?"]

/*: "He" :*/
fileprivate let notiConstraintName:String = "let brow modelHe"

/*: "She" :*/
fileprivate let show_minPath:String = "tip top normal strength enableShe"

/*: "%@ is using too low a version" :*/
fileprivate let const_menuModeName:[Character] = ["%","@"," ","i","s"," ","u","s","i","n"]
fileprivate let dataSectionQuantityInformationStr:[Character] = ["g"," ","t","o","o"," ","l","o","w"]
fileprivate let const_liveName:[Character] = [" ","a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let k_scaleMessage:[Character] = ["S","h","o","o","t"," ","v","i","d","e","o"," ","a","t"," ","l","e","a","s","t"," ","5"]
fileprivate let main_burnMessage:String = "add to title stroke back se"
fileprivate let app_titleName:[Character] = ["c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let mainMiniPath:String = "item top self self alongUploa"
fileprivate let show_basicName:String = "array"

/*: "播放状态: :*/
fileprivate let show_frameMsg:[Character] = ["播","放","状","态",":"]

/*: "c2c_" :*/
fileprivate let k_bubbleGoingPriceStr:String = "break for string truec2c_"

/*: "msg" :*/
fileprivate let showIndexData:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let main_domainFormat:String = "timain"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let notiTouchViewMsg:[UInt8] = [0x26,0x10,0xa,0x5f,0x1c,0x1e,0x11,0x5f,0xa,0xc,0x1a,0x5f,0xb,0x17,0x1a,0x5f,0x9,0x16,0x1b,0x1a,0x10,0x5f,0x1c,0x17,0x1e,0xb,0x5f,0x19,0x1a,0x1e,0xb,0xa,0xd,0x1a,0x5f,0xb,0x10,0x5f,0x18,0x16,0x9,0x1a,0x5f,0x5a,0x3f,0x5f,0x1e,0x5f,0x1c,0x1e,0x13,0x13]

private func levelKey(head num: UInt8) -> UInt8 {
    return num ^ 127
}

/*: "cellData" :*/
fileprivate let show_userGiftKey:String = "out in index sex layercellData"

/*: "System notification" :*/
fileprivate let user_pushMessageFormat:String = "as top color rangeSystem"
fileprivate let k_reasonFormat:String = "attribute model notifi"
fileprivate let main_sexData:String = "cattargetn"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ImageBannerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum TrailHeadFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ImageBannerDelegate: DropBaseViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var isHaveSession = false
    var isHaveSession = false
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = ViewThen()
    private var userWrap = ViewThen() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = TrailHeadFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: ViewThen = ViewThen()) {
    init(chatID: String, isFrom: TrailHeadFromEnum = .Normal, userWrap: ViewThen = ViewThen()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ImageBannerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        BackstageChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == MixInReactiveCompatible.getXiaoMiID() {
        if targetId == MixInReactiveCompatible.messageTo() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == MixInReactiveCompatible.getCustomerServiceID() {
        } else if targetId == MixInReactiveCompatible.turnOver() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kErrorHiddenId.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if StatisticalTableReactiveCompatible.share.appConfigMode.disableShootScreen {
            if StatisticalTableReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ClientShieldView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        viewData()
        //: createUI()
        showNumberUi()
        //: createUIConstraints()
        generateSingle()
        //: bindInteraction()
        equalSex()
        //: addTapGestureRecognizer()
        toEnableLength()
        //: func__requestChatInfo()
        actionBar()
        //: func__getFillDraftText()
        textList()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        UpPlayerDelegate.shared.screenOutDelegate()
        //: func__addVoiceNotification()
        indexMyModel()
        //: addReadPeceiptNotification()
        noneNext()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            lowerClassCount()
            //: func__TipsNotification()
            searched()
            //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
                //: func__setReplyTipsView()
                viewer()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        checkCorner(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        checkCorner(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            detailTask()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: ExplainTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ExplainTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (String(app_theoryWithTitle) + noti_infoFormat.capitalized))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(FileUpgradeMessageCell.self, forCellReuseIdentifier: FileUpgradeMessageCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(ToCreateThen.self, forCellReuseIdentifier: ToCreateThen.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(MerchantMsgCell.self, forCellReuseIdentifier: MerchantMsgCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(RateMonthMsgCell.self, forCellReuseIdentifier: RateMonthMsgCell.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(InfoMsgCell.self, forCellReuseIdentifier: InfoMsgCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(CapabilityReactiveCompatible.self, forCellReuseIdentifier: CapabilityReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(RawViewCell.self, forCellReuseIdentifier: RawViewCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(ChatMessageCell.self, forCellReuseIdentifier: ChatMessageCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(MonthNotifMessageCell.self, forCellReuseIdentifier: MonthNotifMessageCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: BroadcastThen.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(MixViewCell.self, forCellReuseIdentifier: BroadcastThen.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: GroupMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = GroupMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: LeadingInputView = {
        //: var type: InputViewType = .PrivateChat
        var type: DataViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = LeadingInputView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(show_lastPlayerFalsePath) + String(k_originId)))!, .font: UIFont.colorTitle(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(userViewFormat.suffix(3)) + String(app_frameName)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(blockText), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: RequestMakeView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = RequestMakeView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: TableKitView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = TableKitView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.colorTitle(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(dataSubEndUrl) + showSharedUrl.replacingOccurrences(of: "equal", with: "FF"))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(k_indicatorValue.suffix(6)) + String(main_frameMessage.suffix(8))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.readModeAchromaticColour().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(total), for: .touchUpInside)
        //: btn.isHidden = !StatisticalTableReactiveCompatible.share.appUserConfigMode.showDirectGreet || StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !StatisticalTableReactiveCompatible.share.appUserConfigMode.showDirectGreet || StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue || StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: EditView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = EditView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: ClientTranslationView = {
        //: let v = TalkingSelectTranslationView()
        let v = ClientTranslationView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.nonsolidColour(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: ClientResultView = {
        //: let v = TalkingTranslationResultView()
        let v = ClientResultView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.loadApp(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.colorTitle(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(dataSubEndUrl) + showSharedUrl.replacingOccurrences(of: "equal", with: "FF"))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(notiAddUrl.prefix(6)) + String(userPathWithoutMessage.suffix(7))).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.readModeAchromaticColour().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(photosClick), for: .touchUpInside)
        //: btn.isHidden = StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue || StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: FileDataSource = {
        //: let v = TalkingLiveRoomGamesView()
        let v = FileDataSource()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: NowBannerView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = NowBannerView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: ColoredRecognizerDelegate = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = ColoredRecognizerDelegate()
        //: v.isHidden = (StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: TopThen = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = TopThen()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.magnitudeeractionView()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ImageBannerDelegate: UserSourceBannerDelegate {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func page(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.scienceLab(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ImageBannerDelegate: ControllerFile {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func total() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        BackstageChatManager.handler(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.alongShow(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.placeDown(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func photosClick() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        BackstageChatManager.makeError(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = PhotoEditReactiveCompatible()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().mentalImageDownRelease(urlStr: dict?[(showAppFrameOutTitle.replacingOccurrences(of: "user", with: "rl"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(userTotalStr.lowercased() + "Gift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ToMainModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: dataBuildValue.map{modelMessage(error: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(appIconFormat.prefix(5)) + String(kAddFirstMessage))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.sendModel(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ReloadViewCellData! {
    func accountingData(_: ExplainTableViewController!, shrug data: V2TIMMessage!) -> ReloadViewCellData! {
        //: let dic = WearerReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = WearerReactiveCompatible.overPushData(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = KitThen.flyByNightUnpaidWorker(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: AbsorbCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(main_fromKey.prefix(3)) + String(user_listTitle.suffix(6)))].stringValue == (String(k_mKey.suffix(3)) + String(k_heIndexPicContent.suffix(4)) + "Custom") { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(data_picId.lowercased() + "geDi" + dataAddContent.replacingOccurrences(of: "message", with: "io") + app_modelId.replacingOccurrences(of: "equal", with: "n"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(show_arrayValue.replacingOccurrences(of: "video", with: "ex"))][(String(const_fromPath.prefix(7)))][(String(mainLeadingData))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = AttributeCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = AttributeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: PresentationReactiveCompatible.self) == true ||
        if cellData?.isKind(of: PresentationReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: ItemCellData.self) == true ||
            cellData?.isKind(of: ItemCellData.self) == true ||
            //: cellData?.isKind(of: AttributeCellData.self) == true ||
            cellData?.isKind(of: AttributeCellData.self) == true ||
            //: cellData?.isKind(of: SmartThen.self) == true {
            cellData?.isKind(of: SmartThen.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            BackstageChatManager.wellhead(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            BackstageChatManager.appAnd(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ReloadViewCellData!) -> BroadcastThen! {
    func along(for _: ExplainTableViewController!, pause data: ReloadViewCellData!) -> BroadcastThen! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = omitWith(data: data)

        //: if data.isKind(of: AttributeCellData.self) {
        if data.isKind(of: AttributeCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = FileUpgradeMessageCell(style: .default, reuseIdentifier: FileUpgradeMessageCell.className())
            //: let cellData = data as! AttributeCellData
            let cellData = data as! AttributeCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.add() {
                //: if cellData.msgModel.quoteMsgInfo.uid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = StatisticalTableReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = StatisticalTableReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: ItemCellData.self) {
        if data.isKind(of: ItemCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = ToCreateThen(style: .default, reuseIdentifier: ToCreateThen.className())
            //: let cellData = data as! ItemCellData
            let cellData = data as! ItemCellData
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: SmartThen.self) {
        if data.isKind(of: SmartThen.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = MerchantMsgCell(style: .default, reuseIdentifier: MerchantMsgCell.className())
            //: let cellData = data as! SmartThen
            let cellData = data as! SmartThen
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: ImageCellData.self) {
        if data.isKind(of: ImageCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = RateMonthMsgCell(style: .default, reuseIdentifier: RateMonthMsgCell.className())
            //: let cellData = data as! ImageCellData
            let cellData = data as! ImageCellData
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: ClientCellData.self) {
        if data.isKind(of: ClientCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = InfoMsgCell(style: .default, reuseIdentifier: InfoMsgCell.className())
            //: let cellData = data as! ClientCellData
            let cellData = data as! ClientCellData
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = GroupModelType()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.equalBySend(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(kViewTextComponentFormat)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: PresentationReactiveCompatible.self) {
        if data.isKind(of: PresentationReactiveCompatible.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = ChatMessageCell(style: .default, reuseIdentifier: ChatMessageCell.className())
            //: let cellData = data as! PresentationReactiveCompatible
            let cellData = data as! PresentationReactiveCompatible
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: AbsorbCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = RawViewCell(style: .default, reuseIdentifier: RawViewCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! AbsorbCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: SourceCellData.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = CapabilityReactiveCompatible(style: .default, reuseIdentifier: CapabilityReactiveCompatible.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! SourceCellData
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: MonthReactiveCompatible.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = MonthNotifMessageCell(style: .default, reuseIdentifier: MonthNotifMessageCell.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! MonthReactiveCompatible
            //: cell.fill(with: cellData)
            cell.changeDestroyWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: BroadcastThen!) {
    func message(_: ExplainTableViewController!, alongAcross cell: BroadcastThen!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != MixInReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != MixInReactiveCompatible.turnOver() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: EditPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            EditPushManager.share.suddenVideo(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: BroadcastThen!) {
    func singleOut(_: ExplainTableViewController!, contrastInfo cell: BroadcastThen!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ToCreateThen.self) { // 图片
            //: let imageCellData = cell.messageData as! ItemCellData
            let imageCellData = cell.messageData as! ItemCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = QuantityimateMonthThen(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.row(imageCellData, countimate: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.mistake(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showUserLimitMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.deadlineView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.doingObserver(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                totalry()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(notiClickText)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = TalkingHandyJSON()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = MakeHandyJSON()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [TalkingHandyJSON] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = InsertPhotosVc(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                totalry()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: MerchantMsgCell.self) { // 视频
            //: let videoCellData = cell.messageData as! SmartThen
            let videoCellData = cell.messageData as! SmartThen
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = PresentationVideoVc(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                totalry()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = PreviewPlayerDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.customTap(videoCellData, message: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.mistake(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: showUserLimitMessage)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.deadlineView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.doingObserver(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                totalry()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ChatMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ChatMessageCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! PresentationReactiveCompatible, cellView: audioCell)
            touchSearch(cellData: audioCell.cellData as! PresentationReactiveCompatible, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: FileUpgradeMessageCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! FileUpgradeMessageCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = WearerReactiveCompatible.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = WearerReactiveCompatible.overPushData(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(show_arrayValue.replacingOccurrences(of: "video", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(showDataUserUrl))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(showDataUserUrl))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(mainSendTitle) + String(kCellValue.prefix(5))) {
                    //: if StatisticalTableReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isSuccessed.rawValue {
                    if StatisticalTableReactiveCompatible.share.loginUserMode.isTPAuth == ReportSendable.isSuccessed.rawValue {
                        //: return
                        return
                            //: } else if StatisticalTableReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.unknown.rawValue {
                    } else if StatisticalTableReactiveCompatible.share.loginUserMode.isTPAuth == ReportSendable.unknown.rawValue {
                        //: EditPushManager.share.func__pushUserVerifyController(toast: nil)
                        EditPushManager.share.locationFor(toast: nil)
                        //: } else if StatisticalTableReactiveCompatible.share.loginUserMode.isTPAuth == TPUserAuth.isOnGoing.rawValue {
                    } else if StatisticalTableReactiveCompatible.share.loginUserMode.isTPAuth == ReportSendable.isOnGoing.rawValue {
                        //: let vc = TalkingFinalVerificationVC()
                        let vc = CapacityGroupVerificationVc()
                        //: EditPushManager.share.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        EditPushManager.share.colorVc()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: EditPushManager.share.func__pushUserVerifyController(toast: nil)
                        EditPushManager.share.locationFor(toast: nil)
                    }
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<RateModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EditPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EditPushManager.share.child(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(main_fromKey.prefix(3)) + String(user_listTitle.suffix(6)))] as? String == (String(k_mKey.suffix(3)) + String(k_heIndexPicContent.suffix(4)) + "Custom") { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(const_fromPath.prefix(7)))][(String(showDataValue))].intValue == 1 {
                    //: checkAndCallVoice()
                    lineVoice()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    timeVideo()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: CapabilityReactiveCompatible.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! CapabilityReactiveCompatible
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = WearerReactiveCompatible.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = WearerReactiveCompatible.overPushData(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(show_arrayValue.replacingOccurrences(of: "video", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(showDataUserUrl))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<RateModelType>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: EditPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    EditPushManager.share.child(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: RawViewCell.self) {
            //: EditPushManager.share.func__pushToUserDetailVC(uid: targetId)
            EditPushManager.share.suddenVideo(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: BroadcastThen!) {
    func effect(_: ExplainTableViewController!, user cell: BroadcastThen!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: AggregationThen?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: ToCreateThen.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! ToCreateThen
            //: cellData = imageCell.cellData as! ItemCellData
            cellData = imageCell.cellData as! ItemCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: MerchantMsgCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! MerchantMsgCell
            //: cellData = videoCell.cellData as! SmartThen
            cellData = videoCell.cellData as! SmartThen
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: ChatMessageCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! ChatMessageCell
            //: cellData = audioCell.cellData as! PresentationReactiveCompatible
            cellData = audioCell.cellData as! PresentationReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: FileUpgradeMessageCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! FileUpgradeMessageCell
            //: cellData = textCell.cellData as! AttributeCellData
            cellData = textCell.cellData as! AttributeCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: ReloadViewCellData? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: ReloadViewCellData? = KitThen.topData(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: SmartThen.self) {
                if reCellData!.isKind(of: SmartThen.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    ordinal(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    toShow(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: ReloadViewCellData!) {
    func shareOrientation(on _: ExplainTableViewController!, menu cellData: ReloadViewCellData!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = BackstageChatManager.compartmentText(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: ReloadViewCellData!) {
    func indicator(_: ExplainTableViewController!, name cellData: ReloadViewCellData!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        BackstageChatManager.remoteId(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: ReloadViewCellData) -> ReloadViewCellData {
    private func omitWith(data: ReloadViewCellData) -> ReloadViewCellData {
        //: let isSelf = data.msgModel.user.uid == Int(StatisticalTableReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(StatisticalTableReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if StatisticalTableReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if StatisticalTableReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = StatisticalTableReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = StatisticalTableReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = StatisticalTableReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = StatisticalTableReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func clickShould() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (String(userShowId))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe")): (String(user_appearanceMsg.prefix(4)) + "kMsg")]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(const_fromPath.prefix(7))): msgInfo, String(bytes: showRegularStr.map{$0^183}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.toShow(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func placeDown(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: user_equalMessage.map{headMove(time: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: user_equalMessage.map{headMove(time: $0)}, encoding: .utf8)!: content, (String(userLineData.prefix(5))): greetId, String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (String(userShowId))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: kTimeMsg.map{imageStyle(interaction: $0)}, encoding: .utf8)!: content, (String(userLineData.prefix(5))): greetId, (notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe")): (String(userLoadStr.suffix(5)) + notiEqualId.lowercased())]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(const_fromPath.prefix(7))): msgInfo, String(bytes: showRegularStr.map{$0^183}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.toShow(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            WithObjectProtocol.shared.groupBindHandle(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.alongShow(showMsg: String(bytes: app_showUrl.map{openComment(video: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        BackstageChatManager.endUser(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(kImageLabStr) + userFrameExplainPath.replacingOccurrences(of: "player", with: "h"))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (const_toEqualFormat.replacingOccurrences(of: "normal", with: "io")),
                               //: "audioId": greetId,
                               (String(show_dismissHandleId.prefix(6)) + k_locationMessage.replacingOccurrences(of: "mode", with: "d")): greetId,
                               //: "audioLength": length,
                               String(bytes: show_stopValue.map{appVideo(share: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (k_plusName.replacingOccurrences(of: "equal", with: "u") + "Sand" + showManagerId.replacingOccurrences(of: "to", with: "x")): model.unZipFilePath,
                               //: "senderId": StatisticalTableReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: main_sourceName.reversed(), encoding: .utf8)!: StatisticalTableReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(show_dismissHandleId.prefix(6)) + k_locationMessage.replacingOccurrences(of: "mode", with: "d")): greetId,
                                 //: "audioLength": length,
                                 String(bytes: show_stopValue.map{appVideo(share: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (k_plusName.replacingOccurrences(of: "equal", with: "u") + "Sand" + showManagerId.replacingOccurrences(of: "to", with: "x")): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe")): (String(show_beginFormat.prefix(8)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(const_fromPath.prefix(7))): msgInfo, String(bytes: showRegularStr.map{$0^183}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.toShow(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func loadApp(msgStr: String, quoteModel: ExamineedModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            alongShow(showMsg: (showViewHiddenStr + "send " + String(data_matchName) + String(appGiftTitle)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: user_equalMessage.map{headMove(time: $0)}, encoding: .utf8)!: temStr, String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (String(userShowId))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: kTimeMsg.map{imageStyle(interaction: $0)}, encoding: .utf8)!: temStr, (notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe")): (String(userLoadStr.suffix(5)) + notiEqualId.lowercased())]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(const_fromPath.prefix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(app_totalFormat.replacingOccurrences(of: "bottom", with: "ot") + "MsgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(kTopMsg.replacingOccurrences(of: "succeed", with: "u") + String(user_nowToFormat))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: showRegularStr.map{$0^183}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? ExamineedModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            toShow(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func sendModel(img: UIImage, sendModel: ToMainModel) {
        //: let imagePath = ItemCellData.getChatImagePath(image: img)
        let imagePath = ItemCellData.atEnable(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: app_cardStr.map{photoColor(zone: $0)}, encoding: .utf8)!: (String(data_labTitle)), String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (app_valueName.replacingOccurrences(of: "economic", with: "g")), String(bytes: data_betweenStr.reversed(), encoding: .utf8)!: imagePath, String(bytes: noti_pathRegularStr.map{receiveLab(array: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(constEventDataStr)): imagePath,
                                        //: "type": "ImgMsg",
                                        (notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe")): (String(data_labTitle)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataBuildValue.map{modelMessage(error: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(appIconFormat.prefix(5)) + String(kAddFirstMessage)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(const_fromPath.prefix(7))): msgInfo, String(bytes: showRegularStr.map{$0^183}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            toShow(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: ReloadViewCellData?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func ordinal(msgData: ReloadViewCellData?, sendModel: ToMainModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: app_cardStr.map{photoColor(zone: $0)}, encoding: .utf8)!: (String(appLeadingData)), String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (String(show_fillValue)), String(bytes: noti_pathRegularStr.map{receiveLab(array: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe")): (String(appLeadingData)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: const_noseText.map{$0^212}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: const_sendData.map{terminalLayer(fatal: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: showFlushTitle.map{topPopular(page: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataBuildValue.map{modelMessage(error: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(appIconFormat.prefix(5)) + String(kAddFirstMessage)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(const_fromPath.prefix(7))): msgInfo, String(bytes: showRegularStr.map{$0^183}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = KitThen.streetSmart(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.index(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.dataUp(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.status(from: cellData, cypherAge: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.dataUp(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.icon(dict, price: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        toShow(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func colorBy(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (const_toEqualFormat.replacingOccurrences(of: "normal", with: "io")),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: show_stopValue.map{appVideo(share: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (k_plusName.replacingOccurrences(of: "equal", with: "u") + "Sand" + showManagerId.replacingOccurrences(of: "to", with: "x")): voicePath,
                       //: "senderId": StatisticalTableReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: main_sourceName.reversed(), encoding: .utf8)!: StatisticalTableReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(k_plusName.replacingOccurrences(of: "equal", with: "u") + "Sand" + showManagerId.replacingOccurrences(of: "to", with: "x")): voicePath,
                         //: "type": "AudioMsg",
                         (notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe")): (String(show_beginFormat.prefix(8))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: constRawStr.map{$0^106}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(const_fromPath.prefix(7))): msgInfo, String(bytes: showRegularStr.map{$0^183}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            toShow(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ReloadViewCellData, reSend: Bool) {
    private func toShow(msgData: ReloadViewCellData, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            alongShow(showMsg: app_netValue)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(mainLeadingData))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: SmartThen.self) == false && msgData.isKind(of: ClientCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: SmartThen.self) == false, msgData.isKind(of: ClientCellData.self) == false, params[(notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe"))] as? String != (String(user_appearanceMsg.prefix(4)) + "kMsg") {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.index(msgData)
        }
        //: let recordTime = WearerReactiveCompatible.getMessageInsertTime()
        let recordTime = WearerReactiveCompatible.backgroundTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((String(constEventDataStr))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(constEventDataStr))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(constEventDataStr))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(show_beginFormat.prefix(8))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((k_plusName.replacingOccurrences(of: "equal", with: "u") + "Sand" + showManagerId.replacingOccurrences(of: "to", with: "x"))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(k_plusName.replacingOccurrences(of: "equal", with: "u") + "Sand" + showManagerId.replacingOccurrences(of: "to", with: "x"))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (k_plusName.replacingOccurrences(of: "equal", with: "u") + "Sand" + showManagerId.replacingOccurrences(of: "to", with: "x")))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(data_lengthTitle.suffix(7)) + String(data_rangeId))) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = user_useTitle + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(kTimeTitle.suffix(5)) + "ents" + String(data_finishMsg) + String(appFemaleId.suffix(6)) + "cord/")) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = noti_pointListId + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(show_dismissHandleId.prefix(6)) + k_locationMessage.replacingOccurrences(of: "mode", with: "d"))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(const_toEqualFormat.replacingOccurrences(of: "normal", with: "io"))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: ChatContentReactiveCompatible.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        ChatContentReactiveCompatible.barAwakeContainer(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(user_appearanceMsg.prefix(4)) + "kMsg") {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.ofGermanism(msgData, application: nil, oeuvreStatus: NSNumber(value: 0), accelerationLeadershipIncomeAnglicism: .Msg_Status_Fail)

                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.mistake(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: data_deviceContent)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(userLineData.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    BackstageChatManager.endUser(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.gift(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(const_fromPath.prefix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(const_intimateId.prefix(9)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(const_intimateId.prefix(9)))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if ActionReactiveCompatible.editShared().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: appTransformMessage, object: nil, userInfo: [String(bytes: notiCaptureStr.map{listMake(network: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.countimatePush(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(notiModelText.replacingOccurrences(of: "color", with: "es") + String(appInfoTitle.suffix(5)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(user_appearanceMsg.prefix(4)) + "kMsg") {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!] = (String(notiDataMsg))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    showSenseFormat.modelNeed(eventID: userViewValue)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(user_appearanceMsg.prefix(4)) + "kMsg") {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!] = (String(userShowId))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(const_fromPath.prefix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.index(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(StatisticalTableReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(StatisticalTableReactiveCompatible.share.loginUserMode.userID)" + (String(const_countMessage.prefix(6)) + "teCha" + app_backgroundValue.replacingOccurrences(of: "value", with: "t") + String(noti_actualPath)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue, type != (String(user_appearanceMsg.prefix(4)) + "kMsg") {
                //: self.removeReplyTipsView()
                self.hiddenSub()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.ofGermanism(updateMsgData, application: msgInfo[(String(userEffectText.prefix(5)))] as? String, oeuvreStatus: msgIncome, accelerationLeadershipIncomeAnglicism: .Msg_Status_Succ)

            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            BackstageChatManager.appAnd(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((user_wrapPath.replacingOccurrences(of: "make", with: "t") + String(data_blockKey))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(user_wrapPath.replacingOccurrences(of: "make", with: "t") + String(data_blockKey))] as! NSNumber
                //: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((String(constBackUrl) + String(app_playerName))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(String(constBackUrl) + String(app_playerName))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.deadlineStr(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((k_bagTitle.replacingOccurrences(of: "video", with: "i") + "mateP" + String(appReloadMsg))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(k_bagTitle.replacingOccurrences(of: "video", with: "i") + "mateP" + String(appReloadMsg))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? StorageResponseModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.row(updateMsgData, countimate: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(show_fillValue))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(show_fillValue))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? ByModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.customTap(updateMsgData, message: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        ditheredColourMsg()

        //: updateSession()
        advertising()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func advertising() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        UserTitleInfoManager.icon(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func mistake(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard StatisticalTableReactiveCompatible.share.loginUserMode.status != 1 else {
            guard StatisticalTableReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    alongShow(showMsg: errorStr)
                }
                //: return
                return
            }

            //: EditPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            EditPushManager.share.statusOf(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.trademark()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = DateView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName))
            //: view.show()
            view.reShow()
            //: } else if errorCode == 60003, !StatisticalTableReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !StatisticalTableReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            InputWindowReactiveCompatible.shared.relieve()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                alongShow(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func doingObserver(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(const_fromPath.prefix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.countimatePush(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func computerMenu() {
        //: if self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.objectApp(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(StatisticalTableReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(StatisticalTableReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (data_onEndFormat.array(forKey: const_deviceAcceptKey)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.seeking()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                data_onEndFormat.set(intimateCacheData, forKey: const_deviceAcceptKey)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func theEmpty(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = WearerReactiveCompatible.parseTXMessageData(data: data.customElem.data)
        let dic = WearerReactiveCompatible.overPushData(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(show_arrayValue.replacingOccurrences(of: "video", with: "ex"))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(const_fromPath.prefix(7)))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(notiBottomUrl.replacingOccurrences(of: "should", with: "al") + String(show_errorData))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.upTime(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func countimatePush(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((notiBottomUrl.replacingOccurrences(of: "should", with: "al") + String(show_errorData))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(notiBottomUrl.replacingOccurrences(of: "should", with: "al") + String(show_errorData))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.upTime(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func upTime(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = UserTitleInfoManager.managerView(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.objectApp(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.statusShared(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.nameButton(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.nameButton(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        UserTitleInfoManager.marginOfSafety(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: userAttachUrl.map{bindPush(camera: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: userAfterData.map{itemRefresh(equal: $0)}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: appFoundName,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ImageBannerDelegate: CeremonyReactiveCompatible {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func accumulation() {
        //: guard StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.messageLanguage(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func userEnableColor(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func instil(actionType: FeedRelatableTarget) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    rowApp()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    elect()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                rowApp()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus {
                if StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.baptiseTo(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                elect()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    actionVideo()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    elect()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                actionVideo()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.reflexionImage(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func elect() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.alongShow(showMsg: String(bytes: kActualTitle.map{withPath(position: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func statusQuo(giftModel _: GroupModelType, giftNum _: String, model _: SaveMeasurable) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func changeFor(giftModel: GroupModelType, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        equalBySend(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: ClientCellData?, fromStr: String?) {
    func equalBySend(giftModel: GroupModelType, giftNum: String, cellData: ClientCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            alongShow(showMsg: app_netValue)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(mainLeadingData))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(appIconFormat.prefix(5)) + String(kAddFirstMessage))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(const_lengthData.replacingOccurrences(of: "manager", with: "g") + String(dataMakeTitle))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(showZoneValue) + String(user_appKey))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(user_cellId))] = fromStr
        }
        // 送礼接口
        //: ChatContentReactiveCompatible.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ChatContentReactiveCompatible.receiveParam(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.mistake(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_scaleMessage)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.stateFor(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.deadlineView(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.advertising()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(const_fromPath.prefix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((notiBottomUrl.replacingOccurrences(of: "should", with: "al") + String(show_errorData))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(notiBottomUrl.replacingOccurrences(of: "should", with: "al") + String(show_errorData))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.upTime(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.change(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                showSenseFormat.modelNeed(eventID: main_showMessage)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func consultingService(giftModel: GroupModelType, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        showSenseFormat.modelNeed(eventID: data_clickName)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            alongShow(showMsg: app_netValue)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= StatisticalTableReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            elect()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(main_leadingDataColorMsg) + String(kViewTextTitle))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(notiKeyRoundTitle.replacingOccurrences(of: "color", with: "pe"))] = (String(showBlockKey.suffix(6)) + String(show_localShouldKey.prefix(5)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (String(notiDataMsg))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(const_fromPath.prefix(7))): msgInfo, String(bytes: showRegularStr.map{$0^183}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = KitThen.streetSmart(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            toShow(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func betise(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(notiKeyFormat - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.value(true)
            self.msgView.value(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func contentBlock(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.value(true)
            self.msgView.value(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func section(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.section(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                scaleValueAnimation(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func towardTo() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if UpPlayerDelegate.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            UpPlayerDelegate.shared.modelLast()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func deadlineView(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(const_fromPath.prefix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!] = (noti_postTitle.replacingOccurrences(of: "label", with: "f"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(const_fromPath.prefix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = KitThen.streetSmart(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.index(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(const_intimateId.prefix(9)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(const_intimateId.prefix(9)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.ofGermanism(cellData, application: msgInfoDic[(String(userEffectText.prefix(5)))] as? String, oeuvreStatus: msgIncome, accelerationLeadershipIncomeAnglicism: .Msg_Status_Succ)

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        appCreate(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((user_wrapPath.replacingOccurrences(of: "make", with: "t") + String(data_blockKey))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(user_wrapPath.replacingOccurrences(of: "make", with: "t") + String(data_blockKey))] as! NSNumber
            //: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.obscure(needReload: false, mf_coin: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func lowerClassCount() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.fileSend(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func appCreate(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.everyErrorDict(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func viewArr(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.lagniappe(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func meander() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func lineVoice() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.birth { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.advertising()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func timeVideo() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.male { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.advertising()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func blockText() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = RemoveFeedbackVc()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        totalry()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func dispose() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = MotionPromiseViewDelegate(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.addList(cellTitleList: [(String(appToReceiveText) + k_appFormat.replacingOccurrences(of: "priority", with: "o")).localized, (String(user_promptLabelMessage)).localized, (String(mainItemValue.suffix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .blindConstraint()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: EditPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                EditPushManager.share.suddenVideo(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                magnitudeeractionView()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = SizeThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.pathView(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func magnitudeeractionView() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = NotesToThen()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.listSize(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.pathPush(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func comment() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            scaleValueAnimation(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            totalry()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func cleanStatusBlock(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ColorModelType.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.popReceive(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func noneNext() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(backSuccess(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: show_nameMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(sharedList),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: appCancelMessage,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func backSuccess(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func sharedList() {
        //: StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus = true
        StatisticalTableReactiveCompatible.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func totaleractionOn() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? ReloadViewCellData else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? ReloadViewCellData else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if BackstageChatManager.mobileChange(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.userSize(tipStr: (String(k_editKey) + noti_removeName.replacingOccurrences(of: "voice", with: "d") + String(appTextTitle)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ImageBannerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func rowApp() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        UpPermissionTool.showLocation(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = CreateVideoThen.notary(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.currentName(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func photos() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        UpPermissionTool.utilityWith(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.currentName(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func currentName(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ToMainModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.sendModel(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = InsertPhotosVc(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ToMainModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.sendModel(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        totalry()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func actionVideo() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard BackstageChatManager.contentVersion(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == FalloutCompatibleValue.male.rawValue ? "He".localized : (String(show_minPath.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.alongShow(showMsg: (String(const_menuModeName) + String(dataSectionQuantityInformationStr) + String(const_liveName)).arguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.gestureNumber()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func gestureNumber() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = CreateVideoThen.notary(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            CreateVideoThen.stack(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.follow(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.alongShow(showMsg: (String(k_scaleMessage) + String(main_burnMessage.suffix(3)) + String(app_titleName)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func follow(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: ScreenEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = DrawViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ToMainModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.ordinal(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        BeanNameProgressHUD.doingBlock()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        totalry()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: ReloadViewCellData, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func dataUp(msgData: ReloadViewCellData, sendModel: ToMainModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        AttributeSignatureProvider.shared.strength(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.alongShow(showMsg: (String(mainMiniPath.suffix(5)) + "d fail" + show_basicName.replacingOccurrences(of: "array", with: "ed")).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.ofGermanism(msgData, application: nil, oeuvreStatus: NSNumber(value: 0), accelerationLeadershipIncomeAnglicism: .Msg_Status_Fail)

                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.ordinal(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    //: func func__addVoiceNotification() {
    func indexMyModel() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(active),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(voiceNowBack),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(angle(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(createTime(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: dataBarTitle,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(theEmpty(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: noti_appName,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func checkCorner(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            UpPlayerDelegate.shared.screenOutDelegate()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            UpPlayerDelegate.shared.promptView()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func active() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        checkCorner(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func voiceNowBack() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        checkCorner(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func angle(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            checkCorner(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            checkCorner(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: PresentationReactiveCompatible, cellView: TalkingChatAudioMsgCell) {
    func touchSearch(cellData: PresentationReactiveCompatible, cellView: ChatMessageCell) {
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard MakeSocketDelegate.shared.isInfo == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.alongShow(showMsg: userClickMessage)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? ReloadViewCellData)!.isKind(of: PresentationReactiveCompatible.self) == false {
            if (indexModel as? ReloadViewCellData)!.isKind(of: PresentationReactiveCompatible.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! PresentationReactiveCompatible
            let audioModel = indexModel as! PresentationReactiveCompatible
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = UserTitleInfoManager.analogDigitalConverter(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        UpPlayerDelegate.shared.modelLast()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if UpPlayerDelegate.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(show_frameMsg)) + "\(UpPlayerDelegate.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if UpPlayerDelegate.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                UpPlayerDelegate.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        UpPlayerDelegate.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        UpPlayerDelegate.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: ReloadViewCellData = self.msgView.uiMsgs.object(at: index) as! ReloadViewCellData
                let subModel: ReloadViewCellData = self.msgView.uiMsgs.object(at: index) as! ReloadViewCellData
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: PresentationReactiveCompatible.self) {
                if subModel.isKind(of: PresentationReactiveCompatible.self) {
                    //: let audioModel = subModel as! PresentationReactiveCompatible
                    let audioModel = subModel as! PresentationReactiveCompatible
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = UserTitleInfoManager.analogDigitalConverter(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    VoiceReactiveCompatible.numbererval(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        UpPlayerDelegate.shared.multipleStar(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func actionBar() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.fromMake()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.placeDown(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? ReloadViewCellData
                let indexModel = model as? ReloadViewCellData
                //: if indexModel?.isKind(of: PresentationReactiveCompatible.self) == true || indexModel?.isKind(of: ItemCellData.self) == true || indexModel?.isKind(of: AttributeCellData.self) == true || indexModel?.isKind(of: SmartThen.self) == true {
                if indexModel?.isKind(of: PresentationReactiveCompatible.self) == true || indexModel?.isKind(of: ItemCellData.self) == true || indexModel?.isKind(of: AttributeCellData.self) == true || indexModel?.isKind(of: SmartThen.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.ditheredColourMsg()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.sharedMsg()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.totaleractionOn()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func fromMake() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.sinceMoment(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.effectListBy(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.computerMenu()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == StatisticalTableReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == StatisticalTableReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == StatisticalTableReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == StatisticalTableReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.pathPush(userWrap)
    }

    //: func func__getFillDraftText() {
    func textList() {
        //: let userID = "c2c_"+targetId
        let userID = (String(k_bubbleGoingPriceStr.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.accumulation()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func detailTask() {
        //: let userID = "c2c_"+targetId
        let userID = (String(k_bubbleGoingPriceStr.suffix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    //: func func__TipsNotification() {
    func searched() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(aggregation(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: dataGameTitle,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func aggregation(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: SourceCellData = userinfo[(String(showIndexData))] as! SourceCellData

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.value(true)
        self.msgView.value(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func userSize(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: user_equalMessage.map{headMove(time: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: userEdgeData.map{textCenter(push: $0)}, encoding: .utf8)!: (main_domainFormat.replacingOccurrences(of: "main", with: "ps"))]
        //: var message: ReloadViewCellData?
        var message: ReloadViewCellData?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = KitThen.phoneMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = KitThen.streetSmart(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.value(true)
        self.msgView.value(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func sharedMsg() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        BackstageChatManager.chat { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.userSize(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func deadlineStr(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.userSize(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func ditheredColourMsg() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        BackstageChatManager.betweenVoiceHandler(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.userSize(tipStr: String(bytes: notiTouchViewMsg.map{levelKey(head: $0)}, encoding: .utf8)!.arguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func createTime(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: ReloadViewCellData = dict?["cellData"] as! ReloadViewCellData
        let cellData: ReloadViewCellData = dict?[(String(show_userGiftKey.suffix(8)))] as! ReloadViewCellData
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.value(true)
        self.msgView.value(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ImageBannerDelegate: ProtocolReactiveCompatible {
    //: func func__setReplyTipsView() {
    func viewer() {
        //: let key =  "\(StatisticalTableReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(StatisticalTableReactiveCompatible.share.loginUserMode.userID)" + (String(const_countMessage.prefix(6)) + "teCha" + app_backgroundValue.replacingOccurrences(of: "value", with: "t") + String(noti_actualPath)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.futurism()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            page(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            hiddenSub()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func checkStr(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        loadApp(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        hiddenSub()
    }

    //: func removeReplyTipsView() {
    func hiddenSub() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            page(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func viewData() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: show_noValue, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: kStatusName - notiKeyFormat, width: appUseMessage, height: kStatusName)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: appUseMessage,
                                     //: height: ScreenHeight)
                                     height: kStatusName)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func scaleValueAnimation(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.currentViewController() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if DrawSaloonReactiveCompatible.signShared().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: constPlayContent, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: NaturalRecognizerDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: kStatusName - notiKeyFormat,
                                     //: width: ScreenWidth,
                                     width: appUseMessage,
                                     //: height: ScreenHeight)
                                     height: kStatusName)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if DrawSaloonReactiveCompatible.signShared().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: constPlayContent, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ImageBannerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func showNumberUi() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(user_pushMessageFormat.suffix(6)) + String(k_reasonFormat.suffix(7)) + main_sexData.replacingOccurrences(of: "target", with: "io")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func generateSingle() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? notiKeyFormat : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.occurrenceOf(width: contentView.width,
                                     //: height: contentView.height,
                                     height: contentView.height,
                                     //: corners: [.topLeft, .topRight],
                                     corners: [.topLeft, .topRight],
                                     //: cornerRadii: .init(width: 8, height: 8))
                                     cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? noti_noId : user_viewSumervalKey)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + constBarStr)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? noti_noId : user_viewSumervalKey)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + constBarStr)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + constBarStr)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func equalSex() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.comment()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.dispose()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.instil(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                showSenseFormat.modelNeed(eventID: kInfoMessage, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
