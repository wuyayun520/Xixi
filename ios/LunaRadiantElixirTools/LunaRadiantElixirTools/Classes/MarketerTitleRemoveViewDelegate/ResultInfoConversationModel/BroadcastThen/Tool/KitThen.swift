
//: Declare String Begin

/*: "msgType" :*/
fileprivate let dataFirstObserverMsg:[Character] = ["m","s","g","T","y"]
fileprivate let const_topHiddenUrl:[Character] = ["p","e"]

/*: "tips" :*/
fileprivate let main_lightId:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let constAddMessage:String = "EXTRA"

/*: "msgInfo" :*/
fileprivate let dataIndexName:[Character] = ["m","s"]
fileprivate let constLayerPath:String = "self value stringgInfo"

/*: "user" :*/
fileprivate let user_strokeText:String = "usechannel"

/*: "uid" :*/
fileprivate let mainOriginLeadingMsg:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let constFeeMessage:[UInt8] = [0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e]

/*: "headPic" :*/
fileprivate let user_errorStr:[UInt8] = [0xbd,0xb0,0xb4,0xb1,0x85,0xbc,0xb6]

/*: "age" :*/
fileprivate let show_reportLayerMsg:[UInt8] = [0xe8,0xee,0xec]

private func viewAlong(cell num: UInt8) -> UInt8 {
    return num ^ 137
}

/*: "gift" :*/
fileprivate let appManagerData:String = "gicountt"

/*: "isGroup" :*/
fileprivate let notiAcceptTitle:[Character] = ["i","s","G","r","o","u","p"]

/*: "WantGift" :*/
fileprivate let main_clickHostIfTitle:String = "any pathWantGift"

/*: "txt" :*/
fileprivate let appSpaceData:String = "pathxpath"

/*: "img" :*/
fileprivate let const_senseTitleStr:String = "appg"

/*: "video" :*/
fileprivate let constDirectionValue:String = "labeldeo"

/*: "audio" :*/
fileprivate let dataSearchFormat:[Character] = ["a","u","d","i","o"]

/*: "messageType" :*/
fileprivate let kViewFormat:String = "messlist"

/*: "contentType" :*/
fileprivate let mainInfoTitleKey:[Character] = ["c","o","n","t","e","n","t","T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let user_edgeMessage:String = "ImgMssuite model top"
fileprivate let dataManagerMsg:String = "aspect"

/*: "imageUri" :*/
fileprivate let showMakePageValue:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let main_giftMessage:String = "tylabel"

/*: "pic" :*/
fileprivate let notiUpName:[Character] = ["p","i","c"]

/*: "isBurn" :*/
fileprivate let user_momentMsg:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let main_succeedKitMenuId:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let showBorderFormat:String = "paramequal"

/*: "audioLength" :*/
fileprivate let k_cellColorContent:[Character] = ["a","u","d"]
fileprivate let dataStatusPath:String = "index actualioLength"

/*: "len" :*/
fileprivate let appFemaleMsg:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let main_equalSizeId:String = "no systemaudioS"

/*: "senderId" :*/
fileprivate let mainMakeFormat:String = "color"
fileprivate let appPointMsg:[Character] = ["e","n","d","e","r","I","d"]

/*: "AudioMsg" :*/
fileprivate let const_viewUrl:String = "Audiwindow name equal"

/*: "content" :*/
fileprivate let app_partyValue:String = "cobuttonebutton"

/*: "TxtMsg" :*/
fileprivate let user_lineName:String = "TxtMsgmodel select data result"

/*: "message" :*/
fileprivate let notiEqualStr:String = "mefilterage"

/*: "quoteMsgId" :*/
fileprivate let constBarPath:[Character] = ["q","u"]
fileprivate let data_keyValue:[Character] = ["o","t","e","M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let show_hiddenName:String = "qequalte"
fileprivate let const_afterPath:[Character] = ["o"]

/*: "rYMsgType" :*/
fileprivate let mainDataShareTitle:[Character] = ["r","Y"]
fileprivate let data_topVoiceTitle:String = "MsgTypemake head self wait model"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let show_infoName:String = "key view title bottom privateGJ:Ses"
fileprivate let main_showId:String = "image model truetoGree"
fileprivate let kStrokeResultLastFormat:String = "equalstom"

/*: "changeDirection" :*/
fileprivate let showLiveKey:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t"]
fileprivate let notiCoverBeginModelUrl:String = "iovideo"

/*: "system" :*/
fileprivate let show_sizeAddStr:String = "user"
fileprivate let main_labPath:[Character] = ["y","s","t","e","m"]

/*: "title" :*/
fileprivate let user_cardName:[Character] = ["t","i","t","l","e"]

/*: "id" :*/
fileprivate let main_bottomMsg:[Character] = ["i","d"]

/*: "icon" :*/
fileprivate let mainViewUrl:[Character] = ["i","c","o","n"]

/*: "all" :*/
fileprivate let appDirectionId:String = "playerll"

/*: "icon_chats_pcr" :*/
fileprivate let kContentId:[Character] = ["i","c","o","n","_","c","h","a","t"]
fileprivate let showDetailId:String = "else datas_pcr"

/*: "status" :*/
fileprivate let data_photoValue:String = "erroratus"

/*: "msgIncome" :*/
fileprivate let k_cellUrl:String = "in and hide index minmsgInc"
fileprivate let show_tempValue:String = "omstatus"

/*: "msgId" :*/
fileprivate let user_currentId:String = "msgIdrandom name"

/*: "EquationPopCustomReflectable" :*/
fileprivate let user_kitValue:[Character] = ["T","M","s","g","T","r","a","n"]
fileprivate let appToolUrl:String = "slaup"

/*: "TMsgTranslatedContent" :*/
fileprivate let kMaleIndexValue:[Character] = ["T","M","s","g","T","r","a","n","s","l","a","t","e","d","C","o","n","t","e","n","t"]

/*: "formatType" :*/
fileprivate let show_acceptName:[Character] = ["f","o","r","m","a","t","T","y","p","e"]

/*: "plain" :*/
fileprivate let kAppName:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let const_videoUrl:[Character] = ["i","m","a","g","e","D"]
fileprivate let dataErasePath:[Character] = ["a","t","a"]

/*: "VideoMsg" :*/
fileprivate let user_toUrl:[Character] = ["V","i","d","e","o","M","s"]
fileprivate let user_viewMessage:[Character] = ["g"]

/*: "match" :*/
fileprivate let user_sourceColorMakeText:String = "matcpath"

/*: "mf" :*/
fileprivate let app_screenMessage:String = "mequal"

/*: "userIntro" :*/
fileprivate let k_valueMessage:String = "userInclick table case make self"
fileprivate let data_barValue:String = "TRO"

/*: "targetId" :*/
fileprivate let mainCardData:[Character] = ["t","a","r","g","e","t","I"]
fileprivate let user_arrayLeadingName:String = "app"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let mainCustomTitle:String = "resumeresumeresumeresume"
fileprivate let appLeaveTitle:String = "var let zone camerad HH:mm"
fileprivate let noti_afterMessage:String = ":sspush group corner to label"

/*: "sendTime" :*/
fileprivate let data_appKey:String = "sendTimmedium near to"
fileprivate let kObjectPath:String = "to"

/*: "isRead" :*/
fileprivate let mainPlayDomainText:String = "else image dataisRead"

/*: "audioData" :*/
fileprivate let data_labJawHeadKey:[Character] = ["a","u"]
fileprivate let data_toMsg:[Character] = ["d","i","o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let kStatusValue:String = "audioUriif name min eye"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  KitThen.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum ScreenColumnConvertible: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - KitThen

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class KitThen: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func render() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ReloadViewCellData? {
    public class func phoneMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ReloadViewCellData? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = streetSmart(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: DrawMsgListener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            DrawMsgListener.shared.atomicSum85(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ReloadViewCellData? {
    public class func streetSmart(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ReloadViewCellData? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] = (String(main_lightId))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(constAddMessage.lowercased()): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = stud(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SourceCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = OverModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? EqualModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(dataIndexName) + String(constLayerPath.suffix(5)))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(user_strokeText.replacingOccurrences(of: "channel", with: "r"))] = [
            //: "uid": StatisticalTableReactiveCompatible.share.loginUserMode.userID,
            (String(mainOriginLeadingMsg)): StatisticalTableReactiveCompatible.share.loginUserMode.userID,
            //: "nickname": StatisticalTableReactiveCompatible.share.loginUserMode.nickname,
            String(bytes: constFeeMessage.reversed(), encoding: .utf8)!: StatisticalTableReactiveCompatible.share.loginUserMode.nickname,
            //: "headPic": StatisticalTableReactiveCompatible.share.loginUserMode.headPic,
            String(bytes: user_errorStr.map{$0^213}, encoding: .utf8)!: StatisticalTableReactiveCompatible.share.loginUserMode.headPic,
            //: "age": StatisticalTableReactiveCompatible.share.loginUserMode.age,
            String(bytes: show_reportLayerMsg.map{viewAlong(cell: $0)}, encoding: .utf8)!: StatisticalTableReactiveCompatible.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(constAddMessage.lowercased()): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = stud(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (appManagerData.replacingOccurrences(of: "count", with: "f")) {
            //: var data: ReloadViewCellData
            var data: ReloadViewCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(notiAcceptTitle))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = VideoCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ImageCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ImageCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OverModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (String(main_clickHostIfTitle.suffix(8))) {
            //: let data = ClientCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ClientCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OverModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (appSpaceData.replacingOccurrences(of: "path", with: "t")) {
            //: var data: ReloadViewCellData
            var data: ReloadViewCellData
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(notiAcceptTitle))] as? String == "1" { // 群聊
                //: data = PerspectiveThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = PerspectiveThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = AttributeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = AttributeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OverModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (const_senseTitleStr.replacingOccurrences(of: "app", with: "im")) {
            //: let data = ItemCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ItemCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OverModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (constDirectionValue.replacingOccurrences(of: "label", with: "vi")) {
            //: let data = SmartThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = SmartThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OverModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (String(dataSearchFormat)) {
            //: let data = PresentationReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = PresentationReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OverModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(appManagerData.replacingOccurrences(of: "count", with: "f"))] != nil && (msgInfo[(kViewFormat.replacingOccurrences(of: "list", with: "a") + "geType")] as? Int) == 4 {
            //: let data = AttributeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = AttributeCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? OverModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: ReloadViewCellData) -> ReloadViewCellData? {
    public class func topData(withData cellData: ReloadViewCellData) -> ReloadViewCellData? {
        // 图片
        //: if let reCellData = cellData as? ItemCellData {
        if let reCellData = cellData as? ItemCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(MixInReactiveCompatible.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(MixInReactiveCompatible.basicShow())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(mainInfoTitleKey))] = (String(user_edgeMessage.prefix(5)) + dataManagerMsg.replacingOccurrences(of: "aspect", with: "g"))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] = (const_senseTitleStr.replacingOccurrences(of: "app", with: "im"))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(showMakePageValue))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataIndexName) + String(constLayerPath.suffix(5)))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(main_giftMessage.replacingOccurrences(of: "label", with: "pe"))] = (String(user_edgeMessage.prefix(5)) + dataManagerMsg.replacingOccurrences(of: "aspect", with: "g"))
            //: params["pic"] = filePath
            params[(String(notiUpName))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(user_momentMsg))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(user_momentMsg))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(main_succeedKitMenuId))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(main_succeedKitMenuId))] = "0"
            }

            //: dic["params"] = params
            dic[(showBorderFormat.replacingOccurrences(of: "equal", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = streetSmart(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? SmartThen {
        if let reCellData = cellData as? SmartThen {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? PresentationReactiveCompatible {
        if let reCellData = cellData as? PresentationReactiveCompatible {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = UserTitleInfoManager.analogDigitalConverter(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(k_cellColorContent) + String(dataStatusPath.suffix(8)))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(appFemaleMsg))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(main_equalSizeId.suffix(6)) + "andbox")] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(main_equalSizeId.suffix(6)) + "andbox")] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] = (String(dataSearchFormat))
            //: msgInfo["senderId"] = StatisticalTableReactiveCompatible.share.loginUserMode.userID
            msgInfo[(mainMakeFormat.replacingOccurrences(of: "color", with: "s") + String(appPointMsg))] = StatisticalTableReactiveCompatible.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataIndexName) + String(constLayerPath.suffix(5)))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(main_giftMessage.replacingOccurrences(of: "label", with: "pe"))] = (String(const_viewUrl.prefix(4)) + "oMsg")
            //: dic["params"] = params
            dic[(showBorderFormat.replacingOccurrences(of: "equal", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = streetSmart(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? AttributeCellData {
        if let reCellData = cellData as? AttributeCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(app_partyValue.replacingOccurrences(of: "button", with: "nt"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] = (appSpaceData.replacingOccurrences(of: "path", with: "t"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(dataIndexName) + String(constLayerPath.suffix(5)))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(main_giftMessage.replacingOccurrences(of: "label", with: "pe"))] = (String(user_lineName.prefix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(notiEqualStr.replacingOccurrences(of: "filter", with: "ss"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.add() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(constBarPath) + String(data_keyValue))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(show_hiddenName.replacingOccurrences(of: "equal", with: "uo") + "MsgInf" + String(const_afterPath))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(showBorderFormat.replacingOccurrences(of: "equal", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = streetSmart(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ReloadViewCellData? {
    public class func flyByNightUnpaidWorker(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ReloadViewCellData? {
        //: var cellData: ReloadViewCellData?
        var cellData: ReloadViewCellData?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(constAddMessage.lowercased())] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(dataIndexName) + String(constLayerPath.suffix(5)))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(user_strokeText.replacingOccurrences(of: "channel", with: "r"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? OverModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == StatisticalTableReactiveCompatible.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == StatisticalTableReactiveCompatible.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == StatisticalTableReactiveCompatible.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == StatisticalTableReactiveCompatible.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(mainDataShareTitle) + String(data_topVoiceTitle.prefix(7)))] as? String == (String(show_infoName.suffix(6)) + "s:Au" + String(main_showId.suffix(6)) + "t:TxtC" + kStrokeResultLastFormat.replacingOccurrences(of: "equal", with: "u")) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(showLiveKey) + notiCoverBeginModelUrl.replacingOccurrences(of: "video", with: "n"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = map(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (show_sizeAddStr.replacingOccurrences(of: "user", with: "s") + String(main_labPath)) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = OverModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? EqualModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SourceCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(app_partyValue.replacingOccurrences(of: "button", with: "nt"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = ItemCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ItemCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = SmartThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = SmartThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = VideoCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = ImageCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ImageCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = rein(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(constAddMessage.lowercased())] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(dataIndexName) + String(constLayerPath.suffix(5)))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? MainModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = ClientCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ClientCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = PresentationReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = PresentationReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? PresentationReactiveCompatible {
            if let audioCellData = cellData as? PresentationReactiveCompatible {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = greetFormat(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = VoiceReactiveCompatible.sizeOf(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = VoiceReactiveCompatible.keyDb(botchAd(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = AbsorbCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(String(user_cardName))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(app_partyValue.replacingOccurrences(of: "button", with: "nt"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(const_senseTitleStr.replacingOccurrences(of: "app", with: "im"))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = MonthReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = PerspectiveThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = PerspectiveThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = AttributeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = AttributeCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? AccessModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(String(mainViewUrl))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: StatisticalTableReactiveCompatible.share.loginUserMode.headPic ?? ""
                        StatisticalTableReactiveCompatible.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = MainModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(app_partyValue.replacingOccurrences(of: "button", with: "nt"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(constAddMessage.lowercased())] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = StrengthModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (appDirectionId.replacingOccurrences(of: "player", with: "a"))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(kContentId) + String(showDetailId.suffix(5)))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.tillName(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(data_photoValue.replacingOccurrences(of: "error", with: "st"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(k_cellUrl.suffix(6)) + show_tempValue.replacingOccurrences(of: "status", with: "e"))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(kViewFormat.replacingOccurrences(of: "list", with: "a") + "geType")] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(user_currentId.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(user_kitValue) + appToolUrl.replacingOccurrences(of: "up", with: "te") + "Status")] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = EquationPopCustomReflectable(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(kMaleIndexValue))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = StorageResponseModel.orientation()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = StorageResponseModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = ByModel.doing()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = ByModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = Model.textModel()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func map(_ extraDic: [String: Any]) -> ScreenColumnConvertible {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(constAddMessage.lowercased())] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(dataIndexName) + String(constLayerPath.suffix(5)))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? OverModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(show_acceptName))] as? String == (String(kAppName)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(showMakePageValue))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(const_videoUrl) + String(dataErasePath))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(user_toUrl) + String(user_viewMessage)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = voteIn(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(user_sourceColorMakeText.replacingOccurrences(of: "path", with: "h"))] as? Int) == 1 || (msgInfo?[(user_sourceColorMakeText.replacingOccurrences(of: "path", with: "h"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (String(main_lightId)) || (dic?[(kViewFormat.replacingOccurrences(of: "list", with: "a") + "geType")] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(show_acceptName))] as? String == "mf") && ((dic?[(kViewFormat.replacingOccurrences(of: "list", with: "a") + "geType")] as? Int) == 15 || (dic?[(kViewFormat.replacingOccurrences(of: "list", with: "a") + "geType")] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (String(k_valueMessage.prefix(6)) + data_barValue.lowercased()) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String == (String(main_clickHostIfTitle.suffix(8))) || (msgInfo?[(kViewFormat.replacingOccurrences(of: "list", with: "a") + "geType")] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func botchAd(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.tillName(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(constAddMessage.lowercased())] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(dataIndexName) + String(constLayerPath.suffix(5)))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(user_strokeText.replacingOccurrences(of: "channel", with: "r"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(mainMakeFormat.replacingOccurrences(of: "color", with: "s") + String(appPointMsg))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
        if senderId == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(mainMakeFormat.replacingOccurrences(of: "color", with: "s") + String(appPointMsg))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(mainCardData) + user_arrayLeadingName.replacingOccurrences(of: "app", with: "d"))] = message.userID ?? (user?[(String(mainCardData) + user_arrayLeadingName.replacingOccurrences(of: "app", with: "d"))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(mainMakeFormat.replacingOccurrences(of: "color", with: "s") + String(appPointMsg))] = "\(msgInfo?[(String(mainOriginLeadingMsg))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(mainCardData) + user_arrayLeadingName.replacingOccurrences(of: "app", with: "d"))] = "\(msgInfo?[(String(mainOriginLeadingMsg))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(user_currentId.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(user_currentId.prefix(5)))] = "\(msgInfo?[(String(user_currentId.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.tillName(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(user_currentId.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(user_currentId.prefix(5)))] = "\(customDic[(String(user_currentId.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(user_currentId.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(main_equalSizeId.suffix(6)) + "andbox")) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(main_equalSizeId.suffix(6)) + "andbox")] = "\(msgInfo?[(String(main_equalSizeId.suffix(6)) + "andbox")] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(k_cellColorContent) + String(dataStatusPath.suffix(8)))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(k_cellColorContent) + String(dataStatusPath.suffix(8)))] = "\(msgInfo?[(String(k_cellColorContent) + String(dataStatusPath.suffix(8)))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (mainCustomTitle.replacingOccurrences(of: "resume", with: "y") + "-MM-d" + String(appLeaveTitle.suffix(7)) + String(noti_afterMessage.prefix(3)))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(data_appKey.prefix(7)) + kObjectPath.replacingOccurrences(of: "to", with: "e"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == StatisticalTableReactiveCompatible.share.loginUserMode.userID ? "1" : "0"
        info?[(String(mainPlayDomainText.suffix(6)))] = senderId == StatisticalTableReactiveCompatible.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func greetFormat(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = botchAd(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(user_currentId.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func rein(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func stud(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func voteIn(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(dataFirstObserverMsg) + String(const_topHiddenUrl))] as? String, msgType == (String(dataSearchFormat)) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(mainInfoTitleKey))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(const_viewUrl.prefix(4)) + "oMsg")
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(data_labJawHeadKey) + String(data_toMsg))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(kStatusValue.prefix(8)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
