
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_norMakeNameStr:[UInt8] = [0xdd,0xda,0xdd,0xc0,0x9c,0xd7,0xdb,0xd0,0xd1,0xc6,0x8e,0x9d,0x94,0xdc,0xd5,0xc7,0x94,0xda,0xdb,0xc0,0x94,0xd6,0xd1,0xd1,0xda,0x94,0xdd,0xd9,0xc4,0xd8,0xd1,0xd9,0xd1,0xda,0xc0,0xd1,0xd0]

private func dataInside(at num: UInt8) -> UInt8 {
    return num ^ 180
}

/*: "Followers" :*/
fileprivate let appPortionText:[Character] = ["F","o","l","l","o","w"]
fileprivate let dataModelEqualValue:String = "styles"

/*: "Following" :*/
fileprivate let appToStr:String = "make self make letFoll"
fileprivate let show_buttonFormat:String = "OWING"

/*: "#E6E6E6" :*/
fileprivate let k_leadingStr:[Character] = ["#","E"]
fileprivate let mainPathColorManagerName:[Character] = ["6","E","6","E","6"]

/*: "btn_message_more" :*/
fileprivate let kEraseFormat:[Character] = ["b","t","n"]
fileprivate let kDataValue:[Character] = ["_","m","e","s","s","a","g","e","_","m","o","r","e"]

/*: "UID:  :*/
fileprivate let k_messageId:[Character] = ["U","I","D",":"," "]

/*: "level_ :*/
fileprivate let app_clickThemeUrl:String = "with control user equallevel_"

/*: "Gift" :*/
fileprivate let showPopMsg:[UInt8] = [0x4a,0x64,0x6b,0x79]

private func statuteNameBar(quick num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "Mike" :*/
fileprivate let const_mmId:[UInt8] = [0x8f,0xab,0xad,0xa7]

fileprivate func trueManager(please num: UInt8) -> UInt8 {
    let value = Int(num) + 190
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Follow" :*/
fileprivate let showViewManagerPath:[UInt8] = [0xe1,0xa,0x7,0x7,0xa,0x12]

fileprivate func scaleOfMeasurement(gift num: UInt8) -> UInt8 {
    let value = Int(num) - 155
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "gift_voice_card" :*/
fileprivate let showTimeValue:[Character] = ["g","i","f","t","_","v","o","i"]
fileprivate let notiLanguageContent:[Character] = ["c","e","_","c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let userMenuId:String = "self of requestbtn_p"
fileprivate let k_pushName:String = "height blockoice_nor"

/*: "btn_party_voice_pre" :*/
fileprivate let main_frameKey:String = "to upbtn_p"
fileprivate let k_plusModelMessage:[Character] = ["c","e","_","p","r","e"]

/*: "attentionUid" :*/
fileprivate let mainSizeData:String = "alabellabele"
fileprivate let appRecordUrl:String = "in temp title for sensenUid"

/*: "source" :*/
fileprivate let const_rawAddMsg:String = "allurce"

/*: "Kick out" :*/
fileprivate let showImageUrl:String = "Kicview extension return elect format"
fileprivate let constHiddenName:String = "k outself else scale extension"

/*: "Unmute" :*/
fileprivate let noti_enableEqualUrl:String = "Unmutepath string in extension"

/*: "Mute" :*/
fileprivate let kLinkStr:[Character] = ["M","u","t","e"]

/*: "Kick off" :*/
fileprivate let notiContextBetweenMessage:[Character] = ["K","i"]
fileprivate let show_screenCenterValue:String = "empty return add sectionck off"

/*: "Block" :*/
fileprivate let showMicTitle:String = "Blockself file path"

/*: "Report" :*/
fileprivate let noti_labPath:String = "Reportreturn attention"

/*: "#F6F6F6" :*/
fileprivate let mainModelName:[Character] = ["#","F","6","F","6","F","6"]

/*: "Confirm to kick out the room?" :*/
fileprivate let app_addScaleValue:String = "thin useConf"
fileprivate let show_viewStr:String = "to kselect collection"
fileprivate let show_modelPath:String = "the instance within"

/*: "Cancel" :*/
fileprivate let noti_mediumKey:String = "Cancelself self view cell height"

/*: "Sure" :*/
fileprivate let kManagerPicText:[Character] = ["S","u","r","e"]

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let appReloadId:[UInt8] = [0x3f,0x65,0x74,0x75,0x6d,0x20,0x65,0x68,0x74,0x20,0x6c,0x65,0x63,0x6e,0x61,0x63,0x20,0x6f,0x74,0x20,0x74,0x6e,0x61,0x77,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x75,0x73,0x20,0x75,0x6f,0x79,0x20,0x65,0x72,0x41]

/*: "Confirm" :*/
fileprivate let app_locationCostHeightValue:String = "screen detailConfirm"

/*: "The mute has been lifted~" :*/
fileprivate let const_clearMessage:[Character] = ["T","h","e"," ","m","u","t","e"," ","h","a"]
fileprivate let app_frameFormat:[Character] = ["s"," ","b","e","e","n"," ","l","i","f","t","e","d","~"]

/*: "Confirm to kick off the seat?" :*/
fileprivate let data_addMessage:String = "name removeConf"
fileprivate let userPlusUrl:String = "right bottom kick"
fileprivate let showListenConversationFormat:String = "THE"
fileprivate let main_labelMessage:String = "white observer gift indicator seat?"

/*: "OK" :*/
fileprivate let constLiveUrl:String = "frameK"

/*: "Shielding Success" :*/
fileprivate let kSizeText:String = "self content info blade selfShiel"
fileprivate let data_barValue:String = "dikeyg"
fileprivate let const_makeId:String = "ceerror"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  VanguardReactiveCompatible.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol ImageObjectProtocol: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func impairmentView(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func centerStopUid(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func leadAcross(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func userTotal(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class VanguardReactiveCompatible: UIView {
    //: var popView: TalkingPopView?
    var popView: VendorView?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: DataInsetTarget?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(DrawSaloonReactiveCompatible.signShared().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = PathUserTransformable()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: ImageObjectProtocol?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
        if uid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        someConstraint()
        //: setupSubViewsConstraint()
        hiddenTitle()
        //: reqUserCardInfo()
        pathInfo()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_norMakeNameStr.map{dataInside(at: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .colorTitle(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.shouldHiddenAppear()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(removeWith), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .colorTitle(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.blindConstraint()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .colorTitle(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.shouldHiddenAppear()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .shouldHiddenAppear()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(appPortionText) + dataModelEqualValue.replacingOccurrences(of: "style", with: "er")).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.colorTitle(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .blindConstraint()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.colorTitle(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .shouldHiddenAppear()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(appToStr.suffix(4)) + show_buttonFormat.lowercased()).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.colorTitle(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .blindConstraint()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.colorTitle(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(k_leadingStr) + String(mainPathColorManagerName)))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(kEraseFormat) + String(kDataValue))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(station), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: RequestTitleView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = RequestTitleView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension VanguardReactiveCompatible {
    //: func reqUserCardInfo() {
    func pathInfo() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        ActionReactiveCompatible.bull(uid: self.uid, streamerUid: liveUserId, roomId: DrawSaloonReactiveCompatible.signShared().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<PathUserTransformable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.repositionData()
            }
        }
    }

    //: func setUserCardData() {
    func repositionData() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(k_messageId)) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.visualCommunicationUser(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.BundleImageNamed(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.barName(name: (String(app_clickThemeUrl.suffix(6))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            adjust(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func adjust(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: showPopMsg.map{statuteNameBar(quick: $0)}, encoding: .utf8)!)
            //: if liveUserId == StatisticalTableReactiveCompatible.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == StatisticalTableReactiveCompatible.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: const_mmId.map{trueManager(please: $0)}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: showViewManagerPath.map{scaleOfMeasurement(gift: $0)}, encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.readModeAchromaticColour(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: showPopMsg.map{statuteNameBar(quick: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.barName(name: (String(showTimeValue) + String(notiLanguageContent))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: const_mmId.map{trueManager(please: $0)}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.barName(name: (String(userMenuId.suffix(5)) + "arty_v" + String(k_pushName.suffix(8)))), for: .normal)
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.barName(name: (String(main_frameKey.suffix(5)) + "arty_voi" + String(k_plusModelMessage))), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.pingfangMediumFont(fontSize: 15)
                btn.titleLabel?.font = UIFont.finishFont(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(detailSender(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(k_leadingStr) + String(mainPathColorManagerName)))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func detailSender(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: showPopMsg.map{statuteNameBar(quick: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.centerStopUid(uid: self.userModel.uid)
            //: self.dismiss()
            self.blockRequest()
        //: case "Mike":
        case String(bytes: const_mmId.map{trueManager(please: $0)}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.leadAcross(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.blockRequest()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.impairmentView(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.blockRequest()
        //: case "Follow":
        case String(bytes: showViewManagerPath.map{scaleOfMeasurement(gift: $0)}, encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            pastIcon(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func pastIcon(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        container(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.userTotal(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func container(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(mainSizeData.replacingOccurrences(of: "label", with: "t") + "ntio" + String(appRecordUrl.suffix(4)))] = uid
        //: dict["source"] = "2"
        dict[(const_rawAddMsg.replacingOccurrences(of: "all", with: "so"))] = "2"
        //: ChatContentReactiveCompatible.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        ChatContentReactiveCompatible.nearTitle(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func removeWith() {
        //: EditPushManager.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        EditPushManager.share.suddenVideo(uid: self.userModel.uid)
        //: self.dismiss()
        self.blockRequest()
    }
}

//: extension TalkingVoiceRoomCardView {
extension VanguardReactiveCompatible {
    //: @objc func moreBtnClick() {
    @objc func station() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == StatisticalTableReactiveCompatible.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == StatisticalTableReactiveCompatible.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(showImageUrl.prefix(3)) + String(constHiddenName.prefix(5))).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(noti_enableEqualUrl.prefix(6))).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(kLinkStr)).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(notiContextBetweenMessage) + String(show_screenCenterValue.suffix(6))).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(showMicTitle.prefix(5))).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(noti_labPath.prefix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.FileBlock.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = NowMenuView.airman(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(mainModelName)))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func phone() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = VendorView(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName)
        //: popView?.initWithView(view: self)
        popView?.authorSNameStart(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? MixInReactiveCompatible.getWindow())
        popView?.socioEconomicClassOrientation(view: self.currentViewController()?.view ?? MixInReactiveCompatible.nameOf())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func blockRequest() {
        //: popView?.dismissView()
        popView?.cover()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension VanguardReactiveCompatible: InstalmentViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func rowGift(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(showImageUrl.prefix(3)) + String(constHiddenName.prefix(5))).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            BlockThen.rangeConfig(title: nil, message: (String(app_addScaleValue.suffix(4)) + "irm " + String(show_viewStr.prefix(4)) + "ick out " + String(show_modelPath.prefix(4)) + "room?").localized, leftBtnTitle: (String(noti_mediumKey.prefix(6))).localized, rightBtnTitle: (String(kManagerPicText)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                BeanNameProgressHUD.nameShow()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                FirstEqualReqTool.managerBackShared(targetUid: self.userModel.uid, roomId: DrawSaloonReactiveCompatible.signShared().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    BeanNameProgressHUD.doingBlock()
                    //: self.dismiss()
                    self.blockRequest()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(noti_enableEqualUrl.prefix(6))).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            BlockThen.rangeConfig(title: nil, message: String(bytes: appReloadId.reversed(), encoding: .utf8)!.localized, leftBtnTitle: (String(noti_mediumKey.prefix(6))).localized, rightBtnTitle: (String(app_locationCostHeightValue.suffix(7))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                BeanNameProgressHUD.nameShow()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                FirstEqualReqTool.executeTool(targetUid: self.userModel.uid, roomID: DrawSaloonReactiveCompatible.signShared().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    BeanNameProgressHUD.doingBlock()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.taproom(showMsg: (String(const_clearMessage) + String(app_frameFormat)).localized)
                    }
                    //: self.dismiss()
                    self.blockRequest()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(kLinkStr)).localized: // 设置禁言
            //: muteView.showView()
            muteView.resourceView()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.blockRequest()

        //: case "Kick off".localized: /// 踢下麦
        case (String(notiContextBetweenMessage) + String(show_screenCenterValue.suffix(6))).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            BlockThen.rangeConfig(title: nil, message: (String(data_addMessage.suffix(4)) + "irm to" + String(userPlusUrl.suffix(5)) + " off " + showListenConversationFormat.lowercased() + String(main_labelMessage.suffix(6))).localized, leftBtnTitle: (String(noti_mediumKey.prefix(6))).localized, rightBtnTitle: (String(kManagerPicText)).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                DrawSaloonReactiveCompatible.signShared().trivia(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.blockRequest()
            }

        //: case "Report".localized:
        case (String(noti_labPath.prefix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = SizeThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.pathView(view: nil)
        //: case "Block".localized:
        case (String(showMicTitle.prefix(5))).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            BlockThen.rangeConfig(title: nil, message: constReplacePath, leftBtnTitle: (String(noti_mediumKey.prefix(6))).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                BlockThen.buttonIn()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                BlockThen.buttonIn()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                UserFirstReactiveCompatible.bindEqual(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.taproom(showMsg: (String(kSizeText.suffix(5)) + data_barValue.replacingOccurrences(of: "key", with: "n") + " Suc" + const_makeId.replacingOccurrences(of: "error", with: "ss")).localized)
                        //: self.dismiss()
                        self.blockRequest()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension VanguardReactiveCompatible {
    // 添加视图
    //: private func setupSubviews() {
    private func someConstraint() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func hiddenTitle() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(appUseMessage / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-appUseMessage / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(appUseMessage / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.currentViewController()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.blockRequest()
        }
    }
}
