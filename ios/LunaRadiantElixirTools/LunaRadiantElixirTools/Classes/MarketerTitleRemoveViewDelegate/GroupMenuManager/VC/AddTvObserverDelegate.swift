
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_makeFormat:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let main_videoStatusId:[UInt8] = [0x39,0x25,0x28]

private func topLet(pop num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "headPic" :*/
fileprivate let constEffectMsg:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let appIconPath:String = "nicdata"

/*: "birthday" :*/
fileprivate let kScriptTitle:[Character] = ["b","i","r","t","h"]
fileprivate let dataColorMsg:[Character] = ["d","a","y"]

/*: "videoUrl" :*/
fileprivate let k_jumpValue:String = "strength"
fileprivate let showTimePropertyContent:String = "content dataideoUrl"

/*: "cover" :*/
fileprivate let main_infoMsg:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let noti_suiteTitle:String = "LOCATION"

/*: "cmd" :*/
fileprivate let mainWrapUrl:[UInt8] = [0x2f,0x21,0x28]

private func indicatorValue(share num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "refuseCall" :*/
fileprivate let mainLeadingName:[Character] = ["r","e","f","u","s","e","C","a","l","l"]

/*: "data" :*/
fileprivate let show_backAtValue:[UInt8] = [0xe9,0xec,0xf9,0xec]

private func titleRow(equal num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "Call interruption" :*/
fileprivate let show_progressImageWithValue:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let data_appearBlockKey:String = "item adultstopCall"

/*: "logId" :*/
fileprivate let mainLabMsg:[UInt8] = [0xcf,0xcc,0xc4,0xea,0xc7]

/*: "cancelCall" :*/
fileprivate let const_imageText:String = "valuen"
fileprivate let mainNoMessage:String = "celCallself for message view"

/*: "No answer" :*/
fileprivate let dataConstraintHomeText:String = "Nohandle event in since title"
fileprivate let notiDignityKey:String = " answertab view target status"

/*: "duration" :*/
fileprivate let noti_colorAddUrl:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let app_centerContent:String = "She hin user title of info"
fileprivate let notiSubListData:[Character] = ["a","s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let kAdBottomMsg:[Character] = ["c","a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let noti_quoteKey:String = "post shared current cellHe has"
fileprivate let user_onEffectData:String = "g upplay app enable else true"

/*: "endType" :*/
fileprivate let userViewTextPath:String = "eerror"
fileprivate let constCellFormat:String = "dTypesection height actual"

/*: "stopSex" :*/
fileprivate let userManagerPath:String = "keep"
fileprivate let app_titleDatePath:String = "add nametopSex"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let dataVideoFormat:[UInt8] = [0xce,0xe9,0xf4,0xf2,0xe1,0xe1,0xee,0xe4,0xee,0xe2,0xe9,0xf3,0xa7,0xe0,0xe8,0xeb,0xe3,0xa7,0xe4,0xe8,0xee,0xe9,0xf4,0xab,0xa7,0xe4,0xe6,0xeb,0xeb,0xa7,0xef,0xe6,0xf4,0xa7,0xe2,0xe9,0xe3,0xe2,0xe3]

private func appLine(view num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "The other hung up the phone" :*/
fileprivate let constEnterPath:String = "The make make resolution"
fileprivate let showRequestSharePath:String = "hunsucceed"
fileprivate let user_oldPlayerStr:[Character] = [" ","u","p"," ","t"]
fileprivate let notiParentText:String = "he pbar today"

/*: "type" :*/
fileprivate let k_bySelectData:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let appTextPopName:String = "else self buttonjumpUrl"

/*: "Cancel" :*/
fileprivate let data_addValue:String = "Cancelif by last asset"

/*: "Get gold coins" :*/
fileprivate let show_liveMessage:String = "style"
fileprivate let data_toStr:[Character] = ["e","t"," ","g","o","l","d"," ","c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let data_infoName:String = "of reply trackaddBean"

/*: "refuseSex" :*/
fileprivate let notiEmptyMsg:[Character] = ["r"]
fileprivate let const_modelTitle:String = "efviews"

/*: "onAcceptCall" :*/
fileprivate let notiUserValue:[Character] = ["o","n","A","c","c","e","p"]
fileprivate let show_containerUrl:[Character] = ["t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let appNameSumervalFormat:String = "acceptCalvar m return lab"
fileprivate let mainOfData:String = "up"

/*: "Calls are cancelled" :*/
fileprivate let app_viewTopText:String = "label list service text letCall"
fileprivate let dataGuideText:String = "scale"
fileprivate let show_rowText:String = "cornerncelled"

/*: "The call has ended" :*/
fileprivate let notiColorSystemVoiceData:String = "content file actual content inThe call"
fileprivate let data_writeUrl:[Character] = [" ","h","a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AddTvObserverDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let const_onMsg = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class AddTvObserverDelegate: DropBaseViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: EqualChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: StatusReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: ToFileThen? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: PresentationView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        MakeSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MakeSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MakeSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_makeFormat.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! VideoReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.publicTransport()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            reAuth()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            saveFor(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currInfoVC = nil
            MakeSocketDelegate.shared.currInfoVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.equalStart()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        targetValue()
        //: setupSubViewsConstraint()
        timeConstraint()
        //: initNotificationObserver()
        sight()
        //: req_callGetUserInfo()
        referHeadConfirmation()
        //: timeoutRules()
        finishRules()
        //: setUpBGMPlayer()
        label()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.fee()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if StatisticalTableReactiveCompatible.share.appConfigMode.disableShootScreen {
            if StatisticalTableReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ClientShieldView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isInfo = false
        MakeSocketDelegate.shared.isInfo = false
        //: self.stopVCAllActivties()
        self.errorRelease()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: BirthdayView = {
        //: let v = TalkingBGVideoView()
        let v = BirthdayView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.service(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: ModeInfoPlayerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = ModeInfoPlayerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: EffectView = {
        //: let v = TalkingVideoWindowView()
        let v = EffectView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = appUseMessage - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if CapacityLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: user_keyId + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.viewBody()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: ToCloudDelegate = {
        //: let m = TalkingVideoChatManager()
        let m = ToCloudDelegate()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension AddTvObserverDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func referHeadConfirmation() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ToCloudDelegate.bibliography(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: main_videoStatusId.map{topLet(pop: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(constEffectMsg))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(appIconPath.replacingOccurrences(of: "data", with: "k") + "name")].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(String(kScriptTitle) + String(dataColorMsg))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(k_jumpValue.replacingOccurrences(of: "strength", with: "v") + String(showTimePropertyContent.suffix(7)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(main_infoMsg))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(noti_suiteTitle.lowercased())].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.dataWith()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.anyShared()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func anyShared() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = KeyCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = KeyCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.lanceAll(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.openCamera(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension AddTvObserverDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func reAuth() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        UpPermissionTool.pathActionEqual { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.alongShow(showMsg: kScreenMsg)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MakeSocketDelegate.shared.buildExecute(info: [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (String(mainLeadingName)), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: main_videoStatusId.map{topLet(pop: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.shouldAdd()
                //: return
                return
            }

            //: self.startPreview()
            self.fee()
            //: self.stopBGM()
            self.contentChoice()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = StatusReactiveCompatible.maine(type: .Response)
            //: self.startBGM()
            self.publicTransport()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func fee() {
        //: self.videoManager.startPreview()
        self.videoManager.fingertipBack()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func errorRelease() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.contentChoice()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        TextVideoThen.danmuSaveLaborResources()
        //: TalkingSocketManager.shared.callerDelegate = nil
        MakeSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        MakeSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        MakeSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func shouldAdd(animated: Bool = true) {
        //: self.stopBGM()
        self.contentChoice()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func viewBody() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.ticket()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.anyShared()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func presentCapacity() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.viewStart()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.anyShared()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.sort()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func sort() {
        //: guard StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = PresentationView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.might()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension AddTvObserverDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func sight() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(that(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(progressTo(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: userLogFormat,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(bindSub(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: main_screenContent,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        const_onMsg.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func that(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            shouldAdd()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func bindSub(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard MakeSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        MakeSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MakeSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MakeSocketDelegate.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.picture()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func progressTo(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.anyShared()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension AddTvObserverDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.alongShow(showMsg: (String(show_progressImageWithValue)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                MakeSocketDelegate.shared.buildExecute(info: [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (String(data_appearBlockKey.suffix(8))), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: mainLabMsg.map{$0^163}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                MakeSocketDelegate.shared.buildExecute(info: [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (const_imageText.replacingOccurrences(of: "value", with: "ca") + String(mainNoMessage.prefix(7))), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: mainLabMsg.map{$0^163}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MakeSocketDelegate.shared.buildExecute(info: [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (String(mainLeadingName)), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: main_videoStatusId.map{topLet(pop: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.numbereractionDestroy()
                //: TalkingSocketManager.shared.currInfoVC = nil
                MakeSocketDelegate.shared.currInfoVC = nil
            }
            //: popCurrentViewController()
            shouldAdd()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension AddTvObserverDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func label() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? RenameEqualBreedConstantTarget.Call : RenameEqualBreedConstantTarget.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = StatusReactiveCompatible.maine(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func publicTransport() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.limitTo()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func contentChoice() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.anBackground()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension AddTvObserverDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func finishRules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? data_acceptFormat : self.chatModel?.screen()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            enableService()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.enableService()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func enableService() {
        //: guard !TalkingSocketManager.shared.isInfo else { return }
        guard !MakeSocketDelegate.shared.isInfo else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.alongShow(showMsg: (String(dataConstraintHomeText.prefix(2)) + String(notiDignityKey.prefix(7))).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (const_imageText.replacingOccurrences(of: "value", with: "ca") + String(mainNoMessage.prefix(7))), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: mainLabMsg.map{$0^163}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (String(mainLeadingName)), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: main_videoStatusId.map{topLet(pop: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        MakeSocketDelegate.shared.buildExecute(info: socketDict)
        //: popCurrentViewController()
        shouldAdd()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension AddTvObserverDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func titleImage(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: mainLabMsg.map{$0^163}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(noti_colorAddUrl))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.startEmpty(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.dominoEffect()
            //: createTalkRoom()
            presentCapacity()
            //: self.stopBGM()
            self.contentChoice()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func saveFor(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.ticket()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.service(show: false)
        //: self.stopBGM()
        self.contentChoice()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.dominoEffect()
        //: self.createTalkRoom()
        self.presentCapacity()
    }
}

// MARK: - TitleObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension AddTvObserverDelegate: TitleObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func addDown(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(app_centerContent.prefix(5)) + String(notiSubListData)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(kAdBottomMsg))].stringValue == FalloutCompatibleValue.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(noti_quoteKey.suffix(6)) + " hun" + String(user_onEffectData.prefix(4))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.alongShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.numbereractionDestroy()
            //: TalkingSocketManager.shared.currInfoVC = nil
            MakeSocketDelegate.shared.currInfoVC = nil
        }

        //: popCurrentViewController()
        shouldAdd()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func lineUpRemove(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        saveFor(logId: json[String(bytes: mainLabMsg.map{$0^163}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func socketOrCollection(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(userViewTextPath.replacingOccurrences(of: "error", with: "n") + String(constCellFormat.prefix(5)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(app_centerContent.prefix(5)) + String(notiSubListData)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(userManagerPath.replacingOccurrences(of: "keep", with: "s") + String(app_titleDatePath.suffix(6)))].stringValue == FalloutCompatibleValue.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(noti_quoteKey.suffix(6)) + " hun" + String(user_onEffectData.prefix(4))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: dataVideoFormat.map{appLine(view: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(constEnterPath.prefix(4)) + "other " + showRequestSharePath.replacingOccurrences(of: "succeed", with: "g") + String(user_oldPlayerStr) + String(notiParentText.prefix(4)) + "hone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.alongShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.numbereractionDestroy()
            //: TalkingSocketManager.shared.currInfoVC = nil
            MakeSocketDelegate.shared.currInfoVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.contentWithDismiss()
        //: popCurrentViewController()
        shouldAdd()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func commit(msg: String, data: [String: Any]) {
        //: guard StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(k_bySelectData))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(appTextPopName.suffix(7)))] as? String else { return }
            //: let httpsUrl = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ExitConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: EditPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            EditPushManager.share.methodConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            BlockThen.rangeConfig(title: nil,
                                         //: message: msg,
                                         message: msg,
                                         //: leftBtnTitle: "Cancel".localized,
                                         leftBtnTitle: (String(data_addValue.prefix(6))).localized,
                                         //: rightBtnTitle: "Get gold coins".localized) {
                                         rightBtnTitle: (show_liveMessage.replacingOccurrences(of: "style", with: "G") + String(data_toStr)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                BlockThen.buttonIn()
                //: } rightBlock: {
            } rightBlock: {
                //: EditPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                EditPushManager.share.statusOf(clickEvent: appNoRecordTitle, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func aidMini(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.portion(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func managerData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(data_infoName.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.terminalView(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.constraintOf(point: addBean)
        }
    }
}

// MARK: - BeanObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension AddTvObserverDelegate: BeanObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func reject(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(app_centerContent.prefix(5)) + String(notiSubListData)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(notiEmptyMsg) + const_modelTitle.replacingOccurrences(of: "view", with: "u") + "eSex")].stringValue == FalloutCompatibleValue.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(noti_quoteKey.suffix(6)) + " hun" + String(user_onEffectData.prefix(4))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.alongShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.numbereractionDestroy()
            //: TalkingSocketManager.shared.currInfoVC = nil
            MakeSocketDelegate.shared.currInfoVC = nil
        }

        //: popCurrentViewController()
        shouldAdd()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func viewObject(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        saveFor(logId: json[String(bytes: mainLabMsg.map{$0^163}, encoding: .utf8)!].intValue)
    }
}

// MARK: - PopObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension AddTvObserverDelegate: PopObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func toCurrent(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(mainLeadingName)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.alongShow(showMsg: (String(dataConstraintHomeText.prefix(2)) + String(notiDignityKey.prefix(7))).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.numbereractionDestroy()
                //: TalkingSocketManager.shared.currInfoVC = nil
                MakeSocketDelegate.shared.currInfoVC = nil
            }
            //: popCurrentViewController()
            shouldAdd()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(notiUserValue) + String(show_containerUrl)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue else { return }
            //: EditPushManager.share.func__jumpToWebRecharge(sufficient: false)
            EditPushManager.share.statusOf(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(notiUserValue) + String(show_containerUrl)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.alongShow(showMsg: msg)
            //: popCurrentViewController()
            shouldAdd()
        }
    }
}

// MARK: - ReportViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension AddTvObserverDelegate: ReportViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func colorEnable() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        MakeSocketDelegate.shared.buildExecute(info: [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (String(appNameSumervalFormat.prefix(9)) + mainOfData.replacingOccurrences(of: "up", with: "l")), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: main_videoStatusId.map{topLet(pop: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func game() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.alongShow(showMsg: (String(app_viewTopText.suffix(4)) + "s are " + dataGuideText.replacingOccurrences(of: "scale", with: "c") + show_rowText.replacingOccurrences(of: "corner", with: "a")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            MakeSocketDelegate.shared.buildExecute(info: [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (const_imageText.replacingOccurrences(of: "value", with: "ca") + String(mainNoMessage.prefix(7))), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: mainLabMsg.map{$0^163}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.alongShow(showMsg: (String(app_viewTopText.suffix(4)) + "s are " + dataGuideText.replacingOccurrences(of: "scale", with: "c") + show_rowText.replacingOccurrences(of: "corner", with: "a")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            MakeSocketDelegate.shared.buildExecute(info: [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (String(mainLeadingName)), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: main_videoStatusId.map{topLet(pop: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.alongShow(showMsg: (String(notiColorSystemVoiceData.suffix(8)) + String(data_writeUrl)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            MakeSocketDelegate.shared.buildExecute(info: [String(bytes: mainWrapUrl.map{indicatorValue(share: $0)}, encoding: .utf8)!: (String(data_appearBlockKey.suffix(8))), String(bytes: show_backAtValue.map{titleRow(equal: $0)}, encoding: .utf8)!: [String(bytes: mainLabMsg.map{$0^163}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        shouldAdd()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func transmutationPolaroidLandCameraShouldForepartSumeractionView(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.publish(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func componentSearched(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.service(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.assign(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.anyShared()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func restrictionWindow() {
        //: TalkingSocketManager.shared.currInfoVC = self
        MakeSocketDelegate.shared.currInfoVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = ToFileThen.beyondBuild()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.dataMode()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.modusVivendiRemove()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        shouldAdd()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func story() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension AddTvObserverDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func targetValue() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.darkView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func timeConstraint() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
