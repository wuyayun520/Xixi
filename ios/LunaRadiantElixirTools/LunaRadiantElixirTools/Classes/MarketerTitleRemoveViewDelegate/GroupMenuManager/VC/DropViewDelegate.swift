
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_userFormat:[UInt8] = [0xcf,0xc8,0xcf,0xd2,0x8e,0xc5,0xc9,0xc2,0xc3,0xd4,0x9c,0x8f,0x86,0xce,0xc7,0xd5,0x86,0xc8,0xc9,0xd2,0x86,0xc4,0xc3,0xc3,0xc8,0x86,0xcf,0xcb,0xd6,0xca,0xc3,0xcb,0xc3,0xc8,0xd2,0xc3,0xc2]

private func viewText(menu num: UInt8) -> UInt8 {
    return num ^ 166
}

/*: "uid" :*/
fileprivate let notiImageName:[UInt8] = [0x85,0x79,0x74]

fileprivate func rawShared(to num: UInt8) -> UInt8 {
    let value = Int(num) + 240
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let app_giftData:[Character] = ["h","e"]
fileprivate let const_actualContent:[Character] = ["a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let const_allKey:String = "nlanguageck"

/*: "birthday" :*/
fileprivate let notiHeadFormat:String = "birshaded"
fileprivate let appCollectionKey:[Character] = ["a","y"]

/*: "videoUrl" :*/
fileprivate let appSkinUrl:String = "videoUtype equal"
fileprivate let dataPlayTextUrl:[Character] = ["r","l"]

/*: "location" :*/
fileprivate let const_readValue:[Character] = ["l","o","c"]
fileprivate let notiHiddenUrl:[Character] = ["a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let show_pathAppReturnId:[UInt8] = [0x3d,0x47,0x3e]

fileprivate func shareCell(bar num: UInt8) -> UInt8 {
    let value = Int(num) - 218
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let userLoadPath:String = "fillef"
fileprivate let dataEqualModelRightMessage:String = "useCalllog let progress let make"

/*: "data" :*/
fileprivate let constTitleStr:[UInt8] = [0xcf,0xca,0xdf,0xca]

private func makeManagerVar(feature num: UInt8) -> UInt8 {
    return num ^ 171
}

/*: "Call interruption" :*/
fileprivate let constForceName:String = "enter true bottom tableCall"
fileprivate let const_dateMsg:[Character] = ["e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let noti_topFormat:String = "size wherestopCal"
fileprivate let main_toUrl:String = "item"

/*: "logId" :*/
fileprivate let const_cellId:[UInt8] = [0xa0,0xa3,0xab,0x85,0xa8]

private func backMake(layer num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "cancelCall" :*/
fileprivate let const_titleUrl:[Character] = ["c","a","n","c","e"]
fileprivate let constHangSuccessMsg:[Character] = ["l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let notiUserText:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let show_arrayMsg:String = "D"
fileprivate let constImageManagerMessage:[Character] = ["u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let userDataText:[Character] = ["S","h","e"," ","h","a","s"," ","h"]
fileprivate let k_statusValue:String = "ung upcase model post image"

/*: "cancelSex" :*/
fileprivate let dataKeySourceStr:[Character] = ["c","a","n","c","e","l","S"]
fileprivate let main_viewData:String = "euser"

/*: "He has hung up" :*/
fileprivate let constToEnterName:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"]
fileprivate let main_inputAddMessage:[Character] = [" ","u","p"]

/*: "endType" :*/
fileprivate let noti_tableData:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let notiErrMessage:String = "stopSemake image from"
fileprivate let kWhiteName:String = "X"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let noti_infoTitle:[UInt8] = [0x60,0x85,0x8a,0x8c,0x7d,0x7d,0x80,0x7a,0x80,0x7c,0x85,0x8b,0x37,0x7e,0x86,0x83,0x7b,0x37,0x7a,0x86,0x80,0x85,0x8a,0x43,0x37,0x7a,0x78,0x83,0x83,0x37,0x7f,0x78,0x8a,0x37,0x7c,0x85,0x7b,0x7c,0x7b]

fileprivate func cellStatus(model num: UInt8) -> UInt8 {
    let value = Int(num) - 23
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let mainSearchionData:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"]
fileprivate let dataNameFormat:String = "normal"
fileprivate let main_calendarId:[Character] = ["p","h","o","n","e"]

/*: "type" :*/
fileprivate let app_feedText:String = "tycelle"

/*: "jumpUrl" :*/
fileprivate let showUserMsg:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let mainQueryItemCookieName:String = "info makeCancel"

/*: "Get gold coins" :*/
fileprivate let showPathFormat:String = "Get goself equal container case guard"
fileprivate let main_redText:String = "oselecteds"

/*: "addBean" :*/
fileprivate let main_modelTitle:String = "photo"
fileprivate let mainTransformMessage:String = "size canceldBean"

/*: "refuseSex" :*/
fileprivate let constQuoteTitle:[Character] = ["r","e","f","u","s","e","S","e"]
fileprivate let userVoiceStr:[Character] = ["x"]

/*: "onAcceptCall" :*/
fileprivate let data_listThanName:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let data_directionMsg:[Character] = ["a","c","c","e","p","t","C"]
fileprivate let appEmptyText:[Character] = ["a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let notiViewMsg:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c","e","l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let mainUserMsg:[Character] = ["T","h","e"," ","c","a","l","l"]
fileprivate let dataModelName:[Character] = [" ","h","a","s"," ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DropViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let const_sampleMsg = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class DropViewDelegate: DropBaseViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: EqualChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: StatusReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: UserMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        MakeSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MakeSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MakeSocketDelegate.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_userFormat.map{viewText(menu: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! VideoReactiveCompatible
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.locationEqual()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            adjacent()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currInfoVC = nil
            MakeSocketDelegate.shared.currInfoVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.sampleData()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        space()
        //: setupSubViewsConstraint()
        confinementPull()
        //: initNotificationObserver()
        viewNose()
        //: req_callGetUserInfo()
        imageInfo()
        //: timeoutRules()
        byData()
        //: resumeTalk()
        afterBy()
        //: setUpBGMPlayer()
        nor()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if StatisticalTableReactiveCompatible.share.appConfigMode.disableShootScreen {
            if StatisticalTableReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = ClientShieldView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isInfo = false
        MakeSocketDelegate.shared.isInfo = false
        //: self.stopVCAllActivties()
        self.activties()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: TableDrawView = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = TableDrawView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: MakeBlockCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = MakeBlockCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension DropViewDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func imageInfo() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        ToCloudDelegate.bibliography(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: notiImageName.map{rawShared(to: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(app_giftData) + String(const_actualContent))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(const_allKey.replacingOccurrences(of: "language", with: "i") + "name")].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(notiHeadFormat.replacingOccurrences(of: "shade", with: "th") + String(appCollectionKey))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(appSkinUrl.prefix(6)) + String(dataPlayTextUrl))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(const_readValue) + String(notiHiddenUrl))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.viewCard()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension DropViewDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func adjacent() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        UpPermissionTool.imageBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.alongShow(showMsg: kScreenMsg)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MakeSocketDelegate.shared.buildExecute(info: [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (userLoadPath.replacingOccurrences(of: "fill", with: "r") + String(dataEqualModelRightMessage.prefix(7))), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: notiImageName.map{rawShared(to: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.fromWith()
                //: return
                return
            }

            //: self.stopBGM()
            self.viewStop()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = StatusReactiveCompatible.maine(type: .Response)
            //: self.startBGM()
            self.locationEqual()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func activties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.viewStop()
        //: TalkingSocketManager.shared.callerDelegate = nil
        MakeSocketDelegate.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        MakeSocketDelegate.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        MakeSocketDelegate.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func fromWith(animated: Bool = true) {
        //: self.stopBGM()
        self.viewStop()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func atData() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.clickAppear(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func afterBy() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            atData()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension DropViewDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func viewNose() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(getIntervaloLabWillPicClick(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(requestTo(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: main_screenContent,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        const_sampleMsg.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func getIntervaloLabWillPicClick(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            fromWith()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func requestTo(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard MakeSocketDelegate.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        MakeSocketDelegate.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        MakeSocketDelegate.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        MakeSocketDelegate.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.ginmillNetStrokeDrunkForthcomingTalkEqualBy(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension DropViewDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.alongShow(showMsg: (String(constForceName.suffix(4)) + " int" + String(const_dateMsg)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                MakeSocketDelegate.shared.buildExecute(info: [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (String(noti_topFormat.suffix(7)) + main_toUrl.replacingOccurrences(of: "item", with: "l")), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: const_cellId.map{backMake(layer: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                MakeSocketDelegate.shared.buildExecute(info: [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (String(const_titleUrl) + String(constHangSuccessMsg)), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: const_cellId.map{backMake(layer: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                MakeSocketDelegate.shared.buildExecute(info: [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (userLoadPath.replacingOccurrences(of: "fill", with: "r") + String(dataEqualModelRightMessage.prefix(7))), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: notiImageName.map{rawShared(to: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.numbereractionDestroy()
                //: TalkingSocketManager.shared.currInfoVC = nil
                MakeSocketDelegate.shared.currInfoVC = nil
            }
            //: popCurrentViewController()
            fromWith()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.adopt(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension DropViewDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func nor() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? RenameEqualBreedConstantTarget.Call : RenameEqualBreedConstantTarget.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = StatusReactiveCompatible.maine(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func locationEqual() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.limitTo()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func viewStop() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.anBackground()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension DropViewDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func byData() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? data_acceptFormat : self.chatModel?.screen()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            purchase()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.purchase()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func purchase() {
        //: guard !TalkingSocketManager.shared.isInfo else { return }
        guard !MakeSocketDelegate.shared.isInfo else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.alongShow(showMsg: (String(notiUserText)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (String(const_titleUrl) + String(constHangSuccessMsg)), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: const_cellId.map{backMake(layer: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (userLoadPath.replacingOccurrences(of: "fill", with: "r") + String(dataEqualModelRightMessage.prefix(7))), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: notiImageName.map{rawShared(to: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        MakeSocketDelegate.shared.buildExecute(info: socketDict)
        //: popCurrentViewController()
        fromWith()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension DropViewDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func candid(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: const_cellId.map{backMake(layer: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(show_arrayMsg.lowercased() + String(constImageManagerMessage))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.restore(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.transactionTargetMonth()
            //: createTalkRoom()
            atData()
            //: self.stopBGM()
            self.viewStop()
        }
    }
}

// MARK: - TitleObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension DropViewDelegate: TitleObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func addDown(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userDataText) + String(k_statusValue.prefix(6))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(dataKeySourceStr) + main_viewData.replacingOccurrences(of: "user", with: "x"))].stringValue == FalloutCompatibleValue.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(constToEnterName) + String(main_inputAddMessage)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.alongShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.numbereractionDestroy()
            //: TalkingSocketManager.shared.currInfoVC = nil
            MakeSocketDelegate.shared.currInfoVC = nil
        }

        //: popCurrentViewController()
        fromWith()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func lineUpRemove(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: const_cellId.map{backMake(layer: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.viewStop()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.transactionTargetMonth()
        //: self.createTalkRoom()
        self.atData()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func socketOrCollection(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(noti_tableData))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(userDataText) + String(k_statusValue.prefix(6))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(notiErrMessage.prefix(6)) + kWhiteName.lowercased())].stringValue == FalloutCompatibleValue.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(constToEnterName) + String(main_inputAddMessage)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: noti_infoTitle.map{cellStatus(model: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(mainSearchionData) + dataNameFormat.replacingOccurrences(of: "normal", with: " ") + String(main_calendarId)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.alongShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.numbereractionDestroy()
            //: TalkingSocketManager.shared.currInfoVC = nil
            MakeSocketDelegate.shared.currInfoVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.statusDismiss()
        //: self.popCurrentViewController()
        self.fromWith()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: data_intervalId, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func commit(msg: String, data: [String: Any]) {
        //: guard StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(app_feedText.replacingOccurrences(of: "cell", with: "p"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(showUserMsg))] as? String else { return }
            //: let httpsUrl = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ExitConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: EditPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            EditPushManager.share.methodConfig(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            BlockThen.rangeConfig(title: nil,
                                         //: message: msg,
                                         message: msg,
                                         //: leftBtnTitle: "Cancel".localized,
                                         leftBtnTitle: (String(mainQueryItemCookieName.suffix(6))).localized,
                                         //: rightBtnTitle: "Get gold coins".localized) {
                                         rightBtnTitle: (String(showPathFormat.prefix(6)) + "ld c" + main_redText.replacingOccurrences(of: "selected", with: "in")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                BlockThen.buttonIn()
                //: } rightBlock: {
            } rightBlock: {
                //: EditPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                EditPushManager.share.statusOf(clickEvent: appNoRecordTitle, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func managerData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(main_modelTitle.replacingOccurrences(of: "photo", with: "ad") + String(mainTransformMessage.suffix(5)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.showInPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.ego(point: addBean)
        }
    }
}

// MARK: - BeanObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension DropViewDelegate: BeanObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func reject(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userDataText) + String(k_statusValue.prefix(6))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(constQuoteTitle) + String(userVoiceStr))].stringValue == FalloutCompatibleValue.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(constToEnterName) + String(main_inputAddMessage)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.alongShow(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.numbereractionDestroy()
            //: TalkingSocketManager.shared.currInfoVC = nil
            MakeSocketDelegate.shared.currInfoVC = nil
        }
        //: popCurrentViewController()
        fromWith()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func viewObject(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: const_cellId.map{backMake(layer: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.viewStop()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.transactionTargetMonth()
        //: self.createTalkRoom()
        self.atData()
    }
}

// MARK: - PopObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension DropViewDelegate: PopObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func toCurrent(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (userLoadPath.replacingOccurrences(of: "fill", with: "r") + String(dataEqualModelRightMessage.prefix(7))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.alongShow(showMsg: (String(notiUserText)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.numbereractionDestroy()
                //: TalkingSocketManager.shared.currInfoVC = nil
                MakeSocketDelegate.shared.currInfoVC = nil
            }
            //: self.popCurrentViewController()
            self.fromWith()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(data_listThanName)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue else { return }
            //: EditPushManager.share.func__jumpToWebRecharge(sufficient: false)
            EditPushManager.share.statusOf(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(data_listThanName)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.alongShow(showMsg: msg)
            //: self.popCurrentViewController()
            self.fromWith()
        }
    }
}

// MARK: - EnableViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension DropViewDelegate: EnableViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func totaleractionInsideAddress() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        MakeSocketDelegate.shared.buildExecute(info: [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (String(data_directionMsg) + String(appEmptyText)), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: notiImageName.map{rawShared(to: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func sizeTitle() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.alongShow(showMsg: (String(notiViewMsg)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            MakeSocketDelegate.shared.buildExecute(info: [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (String(const_titleUrl) + String(constHangSuccessMsg)), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: const_cellId.map{backMake(layer: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.alongShow(showMsg: (String(notiViewMsg)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            MakeSocketDelegate.shared.buildExecute(info: [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (userLoadPath.replacingOccurrences(of: "fill", with: "r") + String(dataEqualModelRightMessage.prefix(7))), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: notiImageName.map{rawShared(to: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.alongShow(showMsg: (String(mainUserMsg) + String(dataModelName)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            MakeSocketDelegate.shared.buildExecute(info: [String(bytes: show_pathAppReturnId.map{shareCell(bar: $0)}, encoding: .utf8)!: (String(noti_topFormat.suffix(7)) + main_toUrl.replacingOccurrences(of: "item", with: "l")), String(bytes: constTitleStr.map{makeManagerVar(feature: $0)}, encoding: .utf8)!: [String(bytes: const_cellId.map{backMake(layer: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.statusDismiss()
        //: self.popCurrentViewController()
        self.fromWith()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func succeedMute(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.mute(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func callerUp(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.disk(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func underway() {
        //: TalkingSocketManager.shared.currInfoVC = self
        MakeSocketDelegate.shared.currInfoVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = UserMiniView.upAcrossColor()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.resign()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.errorEnableLine(talkingTime)
        //: popCurrentViewController()
        fromWith()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func lodgeInTime(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.errorEnableLine(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func smoothInUp(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.getOut(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension DropViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func space() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func confinementPull() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
