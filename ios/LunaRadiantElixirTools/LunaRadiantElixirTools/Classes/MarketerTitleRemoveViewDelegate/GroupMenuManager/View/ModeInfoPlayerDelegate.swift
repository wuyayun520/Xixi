
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let kRequestAddUrl:[UInt8] = [0xc3,0xc4,0xc3,0xde,0x82,0xc9,0xc5,0xce,0xcf,0xd8,0x90,0x83,0x8a,0xc2,0xcb,0xd9,0x8a,0xc4,0xc5,0xde,0x8a,0xc8,0xcf,0xcf,0xc4,0x8a,0xc3,0xc7,0xda,0xc6,0xcf,0xc7,0xcf,0xc4,0xde,0xcf,0xce]

private func nameSection(club num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "icon_home_position" :*/
fileprivate let userInputToMessage:String = "ishouldn"
fileprivate let main_talkKey:String = "shared else_pos"

/*: "btn_video_minimize" :*/
fileprivate let appKindKey:String = "btn_vito if blue"
fileprivate let user_observerMessage:String = "nlengthize"

/*: "btn_video_drop_nor" :*/
fileprivate let userMakeMsg:String = "key pic return path popularbtn_v"
fileprivate let constVideoValue:String = "dsensep"
fileprivate let dataInputName:[Character] = ["_","n","o","r"]

/*: "btn_video_call_nor" :*/
fileprivate let app_managerKitTitle:[Character] = ["b","t","n","_","v","i","d","e","o","_","c"]
fileprivate let kGiftMessage:String = "segmentl"
fileprivate let mainEmptyStartFormat:String = "l_norvideo var script"

/*: "transform.scale" :*/
fileprivate let appManagerText:[Character] = ["t","r","a","n","s","f","o","r","m"]
fileprivate let notiDayValue:String = "audience"
fileprivate let user_showGiftId:String = "infoale"

/*: "animation" :*/
fileprivate let userViewFormat:String = "anselection"
fileprivate let app_barUrl:String = "end"

/*: "icon_videocall_female_answer_bg" :*/
fileprivate let user_showFormat:[UInt8] = [0x47,0x4d,0x41,0x40,0x71,0x58,0x47,0x4a,0x4b,0x41,0x4d,0x4f,0x42,0x42,0x71,0x48,0x4b,0x43,0x4f,0x42,0x4b,0x71,0x4f,0x40,0x5d,0x59,0x4b,0x5c,0x71,0x4c,0x49]

private func iconPath(action num: UInt8) -> UInt8 {
    return num ^ 46
}

/*: "icon_videocall_female_answer" :*/
fileprivate let userColorValue:String = "IC"
fileprivate let noti_equalName:String = "on_vidself fill else"
fileprivate let userGiftIndexKey:String = "ll_feself key data"
fileprivate let k_contentId:String = "nswlabelr"

/*: "icon_calltalk_points" :*/
fileprivate let const_mMessage:String = "icon_ctop kind crop return type"
fileprivate let app_managerValue:String = "last bottom view ifalk_po"

/*: "get json error" :*/
fileprivate let mainEqualUrl:String = "manager filter textget "
fileprivate let show_leadingTitle:String = "OR"

/*: "btn_video_turn_nor" :*/
fileprivate let data_pathUrl:[Character] = ["b","t","n","_","v","i","d","e","o","_","t","u","r","n","_","n"]
fileprivate let constRedName:[Character] = ["o","r"]

/*: "btn_video_turn_pre" :*/
fileprivate let data_userTitle:String = "btn_table right effect"
fileprivate let constCloudName:String = "o_turn_prview with count tap"
fileprivate let show_tipId:[Character] = ["e"]

/*: "btn_video_start_nor" :*/
fileprivate let data_popKey:String = "btn_vret lab add"
fileprivate let noti_touchName:String = "text skip action cell miniideo_"

/*: "btn_video_start_pre" :*/
fileprivate let k_frameMsg:[Character] = ["b","t","n","_","v","i","d","e","o","_","s","t","a"]
fileprivate let data_showMessage:String = "provider costrt_pre"

/*: "icon_complaint" :*/
fileprivate let noti_miniTitle:[Character] = ["i","c","o","n","_","c"]
fileprivate let user_bindMessage:String = "modelpl"

/*: "showBullet" :*/
fileprivate let appImageKey:String = "SHOW"
fileprivate let appCellHiddenMessage:String = "ehead"

/*: "icon_videoCall_coin_nor" :*/
fileprivate let user_coverKey:String = "color selficon_v"
fileprivate let appUpUrl:String = "iimage"
fileprivate let const_labMessage:String = "eoCalreturn return transform manager allow"
fileprivate let user_viewModelPath:String = "n_norlayer back"

/*: "btn_video_gift_nor" :*/
fileprivate let noti_layerContent:[Character] = ["b","t","n","_","v","i","d","e","o","_","g","i"]
fileprivate let notiDoingFormat:String = "return push labft_nor"

/*: "   " :*/
fileprivate let notiHiddenMsg:String = "blockblockblock"

/*: "Say something...     " :*/
fileprivate let constTitleValue:[Character] = ["S","a","y"," ","s","o","m","e","t","h","i","n","g",".",".","."," "]
fileprivate let app_headModelPushTitle:String = "    "

/*: "waiting for the other party to accept the video invitation..." :*/
fileprivate let kAtToFormat:[UInt8] = [0x2e,0x2e,0x2e,0x6e,0x6f,0x69,0x74,0x61,0x74,0x69,0x76,0x6e,0x69,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x68,0x74,0x20,0x74,0x70,0x65,0x63,0x63,0x61,0x20,0x6f,0x74,0x20,0x79,0x74,0x72,0x61,0x70,0x20,0x72,0x65,0x68,0x74,0x6f,0x20,0x65,0x68,0x74,0x20,0x72,0x6f,0x66,0x20,0x67,0x6e,0x69,0x74,0x69,0x61,0x77]

/*: "Invite you to a video call..." :*/
fileprivate let appViewStr:[Character] = ["I","n","v","i","t","e"," ","y","o","u"," ","t","o"," ","a"]
fileprivate let data_equalTitle:String = " videname height layer false"

/*: "yyyy-MM-dd" :*/
fileprivate let appBeginModelLetStr:String = "should valueyyyy-MM"
fileprivate let userModeFinishStr:[Character] = ["-","d","d"]

/*: "00:00" :*/
fileprivate let app_smartFormat:String = "count:count"

/*: " +" :*/
fileprivate let mainTopValue:String = " app"

/*: "%@ points/min" :*/
fileprivate let dataBlindId:[Character] = ["%","@"," ","p","o","i","n","t","s","/","m","i","n"]

/*: "Camera must be on" :*/
fileprivate let notiPushInfoText:[Character] = ["C","a","m","e","r","a"," ","m","u"]
fileprivate let appActionStr:String = "self color false let messagest be on"

/*: "msgId" :*/
fileprivate let constValueContent:[UInt8] = [0xf1,0xef,0xfb,0xd5,0xf8]

private func inModel(action num: UInt8) -> UInt8 {
    return num ^ 156
}

/*: "senderUid" :*/
fileprivate let constLinkMessage:[UInt8] = [0x4d,0x3f,0x48,0x3e,0x3f,0x4c,0x2f,0x43,0x3e]

fileprivate func imageGo(fatal num: UInt8) -> UInt8 {
    let value = Int(num) + 38
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "toUid" :*/
fileprivate let const_pathValue:String = "toUidcell page class fragment"

/*: "nickname" :*/
fileprivate let mainCurrentSourceId:[UInt8] = [0xba,0xb5,0xaf,0xb7,0xba,0xad,0xb9,0xb1]

fileprivate func writerSTitle(lab num: UInt8) -> UInt8 {
    let value = Int(num) - 76
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let data_countItemRawPath:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "isMySender" :*/
fileprivate let showLayerTitle:[UInt8] = [0xcf,0xd9,0xb3,0xdf,0xb9,0xcb,0xd4,0xca,0xcb,0xd8]

fileprivate func mediumAction(cover num: UInt8) -> UInt8 {
    let value = Int(num) - 102
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "hasFace" :*/
fileprivate let data_pageUrl:String = "hasFacbackground i location"
fileprivate let dataWithDoingName:String = "scale"

/*: "Automatically answer for you in %@s" :*/
fileprivate let notiResultTitle:[UInt8] = [0x9a,0xae,0xaf,0xb4,0xb6,0xba,0xaf,0xb2,0xb8,0xba,0xb7,0xb7,0xa2,0xfb,0xba,0xb5,0xa8,0xac,0xbe,0xa9,0xfb,0xbd,0xb4,0xa9,0xfb,0xa2,0xb4,0xae,0xfb,0xb2,0xb5,0xfb,0xfe,0x9b,0xa8]

/*: "giftId" :*/
fileprivate let appBackgroundFormat:String = "name"
fileprivate let userLengthMsg:[Character] = ["i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let dataAddMsg:String = "giftNumself color round"

/*: "from" :*/
fileprivate let app_rowData:String = "FROM"

/*: "videoCall" :*/
fileprivate let dataRestoreTitle:String = "videoCalltrue button equal let"

/*: "programSort" :*/
fileprivate let showUserId:[Character] = ["p","r","o","g","r","a","m"]
fileprivate let userPathKey:String = "tip loadSort"

/*: "pkgItemsetId" :*/
fileprivate let noti_monthValue:[Character] = ["p","k","g","I","t","e","m","s"]
fileprivate let k_modelMessage:String = "var universal background sharedetId"

/*: "msgInfo" :*/
fileprivate let const_viewId:String = "bar she size viewmsgIn"
fileprivate let kLinePath:[Character] = ["f","o"]

/*: "msgType" :*/
fileprivate let noti_equalUrl:[Character] = ["m","s","g","T","y","p","e"]

/*: "gift" :*/
fileprivate let notiVideoTableValue:[Character] = ["g","i","f","t"]

/*: "totalMfCoin" :*/
fileprivate let const_textPath:[Character] = ["t","o","t","a","l"]
fileprivate let appMakeName:String = "path if color text normalMfCoin"

/*: "cellData" :*/
fileprivate let userViewText:[UInt8] = [0x1f,0x19,0x10,0x10,0x38,0x1d,0x8,0x1d]

private func sexText(selected num: UInt8) -> UInt8 {
    return num ^ 124
}

/*: "%02i:%02i" :*/
fileprivate let userBackgroundPath:[Character] = ["%","0","2","i",":","%"]
fileprivate let notiAppKey:String = "transition tab top return not02i"

/*: "..." :*/
fileprivate let app_giftValue:String = "datadatadata"

/*: ".." :*/
fileprivate let notiShareFormat:String = "modemode"

/*: "FaceActionEvent_Interval_12_frame" :*/
fileprivate let user_diskKey:[UInt8] = [0x29,0xe,0xc,0xa,0x2e,0xc,0x1b,0x6,0x0,0x1,0x2a,0x19,0xa,0x1,0x1b,0x30,0x26,0x1,0x1b,0xa,0x1d,0x19,0xe,0x3,0x30,0x5e,0x5d,0x30,0x9,0x1d,0xe,0x2,0xa]

private func toHidden(name num: UInt8) -> UInt8 {
    return num ^ 111
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ModeInfoPlayerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import UIKit
import UIKit

//: protocol Talking1v1VideoInteractionViewDelegate: NSObjectProtocol {
protocol ReportViewDelegate: NSObjectProtocol {
    /// 发送方结束通话
    //: func interactionView_shouldEndCall()
    func game()

    /// 接听视频通话
    //: func interactionView_shouldAcceptCall()
    func colorEnable()

    /// 旋转摄像头
    /// - Parameter isFront: 是否前置（默认前置）
    //: func interactionView_shouldChangeCamera(isFront: Bool)
    func transmutationPolaroidLandCameraShouldForepartSumeractionView(isFront: Bool)

    /// 禁用/开启摄像头
    /// - Parameter forbidden: 是否禁用摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool)
    func componentSearched(forbidden: Bool)

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow()
    func restrictionWindow()

    /// 投诉成功
    //: func interactionView_reportSucceed()
    func story()
}

//: class Talking1v1VideoInteractionView: UIView {
class ModeInfoPlayerDelegate: UIView {
    //: let place = (ScreenWidth-actualWidth(w: 45)*5)/6
    let place = (appUseMessage - actualWidth(w: 45) * 5) / 6
    //: weak var delegate: Talking1v1VideoInteractionViewDelegate?
    weak var delegate: ReportViewDelegate?
    //: private var chatModel = TalkingVideoChatModel()
    private var chatModel = EqualChatModel() // 引用类型，整个视频通话共用同一个model
    //: private var waitTimer: Timer?
    private var waitTimer: Timer? // 等待接听动画定时器
    //: private var startTime: TimeInterval?
    private var startTime: TimeInterval? // 开始的时间戳记 since1970
    //: private var countTimer: Timer?
    private var countTimer: Timer? // 通话时间定时器
    //: private var freeCountTimer: Timer?
    private var freeCountTimer: Timer? // 免费通话剩余时间定时器
//    var isShowRecharge  = false                         // 是否展示充值页中
    //: var isShowInput  = false
    var isShowInput = false // 是否在聊天中
    //: private var autoAcceptTimer: DispatchSourceTimer?
    private var autoAcceptTimer: DispatchSourceTimer? // 自动接听定时器

    //: init(frame: CGRect, chatModel: TalkingVideoChatModel?) {
    init(frame: CGRect, chatModel: EqualChatModel?) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        legacyBroad()
        //: setupSubViewsConstraint()
        viewWithoutConfinementConstraint()
        //: if chatModel != nil {
        if chatModel != nil {
            //: self.chatModel = chatModel!
            self.chatModel = chatModel!
            //: self.startTime = chatModel!.startTime
            self.startTime = chatModel!.startTime
        }
        //: showViewsViaStage()
        dominoEffect()
        //: bindInteraction()
        cohereSumeractionInteraction()
        //: initNotificationObserver()
        exceptFace()
        //: initCachEffectData()
        equalStart()
        //: isShowHangupBtn()
        executeAdd()

        //: TalkingVideoDanmuManager.shared().delegate = self
        TextVideoThen.infix().delegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: kRequestAddUrl.map{nameSection(club: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: if waitTimer != nil {
        if waitTimer != nil {
            //: waitTimer?.invalidate()
            waitTimer?.invalidate()
            //: waitTimer = nil
            waitTimer = nil
        }
        //: if countTimer != nil {
        if countTimer != nil {
            //: countTimer?.invalidate()
            countTimer?.invalidate()
            //: countTimer = nil
            countTimer = nil
        }
        //: if freeCountTimer != nil {
        if freeCountTimer != nil {
            //: freeCountTimer?.invalidate()
            freeCountTimer?.invalidate()
            //: freeCountTimer = nil
            freeCountTimer = nil
        }
        //: cancelAutoAcceptTimer()
        who()
        //: player.stopPlay()
        player.docket()
        //: invalidateIdleTimer()
        allAcross()
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    // 点击展示/隐藏交互按钮
    //: private lazy var touchHidenBtn: UIButton = {
    private lazy var touchHidenBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(touchHidenBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(itemColor), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像背景
    //: private lazy var pairInfoBg: UIView = {
    private lazy var pairInfoBg: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        v.backgroundColor = UIColor(white: 0, alpha: 0.4)
        //: v.layer.cornerRadius = actualWidth(w: 50)/2
        v.layer.cornerRadius = actualWidth(w: 50) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var locationBtn: TalkingButton = {
    private lazy var locationBtn: TalkingItemButton = {
        //: let btn = TalkingButton()
        let btn = TalkingItemButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_home_position"), for: .normal)
        btn.setImage(UIImage.barName(name: (userInputToMessage.replacingOccurrences(of: "should", with: "co") + "_home" + String(main_talkKey.suffix(4)) + "ition")), for: .normal)
        //: btn.spaceBetweenTitleAndImage = 2
        btn.spaceBetweenTitleAndImage = 2
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 12)
        btn.titleLabel?.font = UIFont.statusDataMove(fontSize: 12)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = CapacityLanguageManager.shared.direction == .rightToLeft ? .right : .left
        //: return btn
        return btn
        //: }()
    }()

    // 接收方头像
    //: private lazy var pairPicV: UIImageView = {
    private lazy var pairPicV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = actualWidth(w: 40)/2
        v.layer.cornerRadius = actualWidth(w: 40) / 2
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: v.layer.borderWidth = 1
        v.layer.borderWidth = 1
        //: v.layer.borderColor = UIColor.white.cgColor
        v.layer.borderColor = UIColor.white.cgColor
        //: return v
        return v
        //: }()
    }()

    // 接收方昵称
    //: private lazy var pairNameLab: UILabel = {
    private lazy var pairNameLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 17)
        lab.font = UIFont.finishFont(fontSize: 17)
        //: return lab
        return lab
        //: }()
    }()

    // 呼叫中描述
    //: private lazy var desLab: UILabel = {
    private lazy var desLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.statusDataMove(fontSize: 17)
        //: lab.numberOfLines = 0
        lab.numberOfLines = 0
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    // 最小化按钮
    //: private lazy var minimizeBtn: UIButton = {
    private lazy var minimizeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_minimize"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(appKindKey.prefix(6)) + "deo_mi" + user_observerMessage.replacingOccurrences(of: "length", with: "im"))), for: .normal)
        //: btn.addTarget(self, action: #selector(minimizeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(moveLab), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(userMakeMsg.suffix(5)) + "ideo_" + constVideoValue.replacingOccurrences(of: "sense", with: "ro") + String(dataInputName))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(cellClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_call_nor"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(app_managerKitTitle) + kGiftMessage.replacingOccurrences(of: "segment", with: "a") + String(mainEmptyStartFormat.prefix(5)))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(down), for: .touchUpInside)

        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(appManagerText) + notiDayValue.replacingOccurrences(of: "audience", with: ".") + user_showGiftId.replacingOccurrences(of: "info", with: "sc"))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80/70), y: actualWidth(w: 80/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 80 / 70), y: actualWidth(w: 80 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (userViewFormat.replacingOccurrences(of: "selection", with: "im") + "atio" + app_barUrl.replacingOccurrences(of: "end", with: "n")))

        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBGBtn: UIButton = {
    private lazy var femaleAcceptBGBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer_bg"), for: .normal)
        btn.setBackgroundImage(UIImage.barName(name: String(bytes: user_showFormat.map{iconPath(action: $0)}, encoding: .utf8)!), for: .normal)
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (String(appManagerText) + notiDayValue.replacingOccurrences(of: "audience", with: ".") + user_showGiftId.replacingOccurrences(of: "info", with: "sc"))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1.02, y: 1.02)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 0.95, y: 0.95))]
        //: keyAnimation.duration = 0.6
        keyAnimation.duration = 0.6
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (userViewFormat.replacingOccurrences(of: "selection", with: "im") + "atio" + app_barUrl.replacingOccurrences(of: "end", with: "n")))
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听按钮
    //: private lazy var femaleAcceptBtn: UIButton = {
    private lazy var femaleAcceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videocall_female_answer"), for: .normal)
        btn.setImage(UIImage.barName(name: (userColorValue.lowercased() + String(noti_equalName.prefix(6)) + "eoca" + String(userGiftIndexKey.prefix(5)) + "male_a" + k_contentId.replacingOccurrences(of: "label", with: "e"))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(down), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 女性接听积分提示
    //: private lazy var femaleAcceptTipsBtn: TalkingButton = {
    private lazy var femaleAcceptTipsBtn: TalkingItemButton = {
        //: let btn = TalkingButton()
        let btn = TalkingItemButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_calltalk_points"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(const_mMessage.prefix(6)) + "allt" + String(app_managerValue.suffix(6)) + "ints")), for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 15)
        btn.titleLabel?.font = UIFont.statusDataMove(fontSize: 15)
        //: btn.isUserInteractionEnabled = false
        btn.isUserInteractionEnabled = false
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var acceptPlayer: SVGAPlayer = {
    lazy var acceptPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .CallTalk_Accept)
        let url = RequestThen.default.littleness(type: .CallTalk_Accept)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(mainEqualUrl.suffix(4)) + "json err" + show_leadingTitle.lowercased()))
        }
        //: if LanguageManager.shared.direction == .rightToLeft {
        if CapacityLanguageManager.shared.direction == .rightToLeft {
            //: player.transform = CGAffineTransform(scaleX: -1, y: 1)
            player.transform = CGAffineTransform(scaleX: -1, y: 1)
        }
        //: return player
        return player
        //: }()
    }()

    // 旋转摄像头按钮
    //: private lazy var rotateCameraBtn: UIButton = {
    private lazy var rotateCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_turn_nor"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(data_pathUrl) + String(constRedName))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_turn_pre"), for: .selected)
        btn.setImage(UIImage.barName(name: (String(data_userTitle.prefix(4)) + "vide" + String(constCloudName.prefix(9)) + String(show_tipId))), for: .selected)
        //: btn.addTarget(self, action: #selector(rotateCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(currentCell), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 开启/关闭摄像头按钮
    //: private lazy var openCameraBtn: UIButton = {
    private lazy var openCameraBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_start_nor"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(data_popKey.prefix(5)) + String(noti_touchName.suffix(5)) + "start_nor")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_start_pre"), for: .selected)
        btn.setImage(UIImage.barName(name: (String(k_frameMsg) + String(data_showMessage.suffix(6)))), for: .selected)
        //: btn.addTarget(self, action: #selector(openCameraButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(cineCamera), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 投诉按钮（男性）
    //: private lazy var reportBtn: UIButton = {
    private lazy var reportBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_complaint"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(noti_miniTitle) + user_bindMessage.replacingOccurrences(of: "model", with: "om") + "aint")), for: .normal)
        //: btn.addTarget(self, action: #selector(reportButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(informClick), for: .touchUpInside)
        //: let data = JSON(StatisticalTableReactiveCompatible.share.appConfigMode.videoReport)
        let data = JSON(StatisticalTableReactiveCompatible.share.appConfigMode.videoReport)
        //: if data["showBullet"].boolValue == true {
        if data[(appImageKey.lowercased() + "Bull" + appCellHiddenMessage.replacingOccurrences(of: "head", with: "t"))].boolValue == true {
            //: btn.isEnabled = true
            btn.isEnabled = true
            //: } else {
        } else {
            //: btn.isEnabled = false
            btn.isEnabled = false
        }
        //: return btn
        return btn
        //: }()
    }()

    // 充值按钮
    //: private lazy var rechargeBtn: UIButton = {
    private lazy var rechargeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videoCall_coin_nor"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(user_coverKey.suffix(6)) + appUpUrl.replacingOccurrences(of: "image", with: "d") + String(const_labMessage.prefix(5)) + "l_coi" + String(user_viewModelPath.prefix(5)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_videoCall_coin_nor"), for: .selected)
        btn.setImage(UIImage.barName(name: (String(user_coverKey.suffix(6)) + appUpUrl.replacingOccurrences(of: "image", with: "d") + String(const_labMessage.prefix(5)) + "l_coi" + String(user_viewModelPath.prefix(5)))), for: .selected)
        //: btn.addTarget(self, action: #selector(rechargeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(streamTime), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // 送礼按钮
    //: private lazy var sendGiftBtn: UIButton = {
    private lazy var sendGiftBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .normal)
        btn.setImage(UIImage.barName(name: (String(noti_layerContent) + String(notiDoingFormat.suffix(6)))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_gift_nor"), for: .selected)
        btn.setImage(UIImage.barName(name: (String(noti_layerContent) + String(notiDoingFormat.suffix(6)))), for: .selected)
        //: btn.addTarget(self, action: #selector(sendGiftButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(counteraction), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var timeLab: UILabel = {
    private lazy var timeLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 18)
        lab.font = UIFont.finishFont(fontSize: 18)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var freeTimeView: TalkingFreeCallRechargeTipsView = {
    private lazy var freeTimeView: UserReactiveCompatible = {
        //: let view = TalkingFreeCallRechargeTipsView()
        let view = UserReactiveCompatible()
        //: view.backgroundColor = .clear
        view.backgroundColor = .clear
        //: view.touchRechargeBtnBlock = { [weak self] in
        view.touchRechargeBtnBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
//            self.isShowRecharge = true
        }
        //: return view
        return view
        //: }()
    }()

    //: lazy var giftView: TalkingChatGiftView = {
    lazy var giftView: ItemDataSource = {
        //: let giftV = TalkingChatGiftView.init(style: .call)
        let giftV = ItemDataSource(style: .call)
        //: giftV.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        giftV.frame = CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName)
        //: return giftV
        return giftV
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: RequestMakeView = {
        //: let effectView = TalkingPrivateChatAnimatView()
        let effectView = RequestMakeView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var giftTrackView: TalkingGiftTrackView = {
    lazy var giftTrackView: MotionTrackView = {
        //: let trackView = TalkingGiftTrackView()
        let trackView = MotionTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: lazy var pointAnimView: TalkingPointAnimationView = {
    lazy var pointAnimView: TopAnimationView = {
        //: let pointView = TalkingPointAnimationView()
        let pointView = TopAnimationView()
        //: pointView.isUserInteractionEnabled = false
        pointView.isUserInteractionEnabled = false
        //: return pointView
        return pointView
        //: }()
    }()

    // 视频播放器
    //: private lazy var player: TalkingVideoPlayerManager = {
    private lazy var player: ScreenPlayListener = {
        //: let player = TalkingVideoPlayerManager()
        let player = ScreenPlayListener()
        //: player.setMute(bEnable: true)
        player.modelObserver(bEnable: true)
        //: return player
        return player
        //: }()
    }()

    // 视频封面
    //: private lazy var coverImg: UIImageView = {
    private lazy var coverImg: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    /// 1.7.2 聊天消息
    //: private lazy var chatBtn: UIButton = {
    private lazy var chatBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.backgroundColor = UIColor.init(white: 0, alpha: 0.25)
        btn.backgroundColor = UIColor(white: 0, alpha: 0.25)
        //: btn.layer.cornerRadius = 17
        btn.layer.cornerRadius = 17
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.setTitle("   "+"Say something...     ".localized, for: .normal)
        btn.setTitle("   " + (String(constTitleValue) + app_headModelPushTitle.capitalized).localized, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangRugularFont(fontSize: 14)
        btn.titleLabel?.font = UIFont.statusDataMove(fontSize: 14)
        //: btn.setTitleColor(UIColor.init(white: 1, alpha: 0.8), for: .normal)
        btn.setTitleColor(UIColor(white: 1, alpha: 0.8), for: .normal)
        //: btn.contentHorizontalAlignment = LanguageManager.shared.direction == .rightToLeft ? .right:.left
        btn.contentHorizontalAlignment = CapacityLanguageManager.shared.direction == .rightToLeft ? .right : .left
        //: btn.addTarget(self, action: #selector(commentBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(today), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var danmuView: TalkingVideoDanmuMsgListTableView = {
    private lazy var danmuView: SlateTableView = {
        //: let view = TalkingVideoDanmuMsgListTableView.init()
        let view = SlateTableView()
        //: view.touchTableGesblock = { [weak self] in
        view.touchTableGesblock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.touchHidenBtnClick()
            self.itemColor()
        }
        //: return view
        return view
        //: }()
    }()

    //: private lazy var commInputView: TalkingVideoCallInputView = {
    private lazy var commInputView: EditViewDelegate = {
        //: let text = TalkingVideoCallInputView.init()
        let text = EditViewDelegate()
        //: text.delegate = self
        text.delegate = self
        //: text.isHidden = true
        text.isHidden = true
        //: return text
        return text
        //: }()
    }()

    // 自动接听文案描述（女性）
    //: private lazy var autoAcceptDesLab: UILabel = {
    private lazy var autoAcceptDesLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangRugularFont(fontSize: 17)
        lab.font = UIFont.statusDataMove(fontSize: 17)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: return lab
        return lab
        //: }()
    }()
}

// MARK: - Event

//: extension Talking1v1VideoInteractionView {
extension ModeInfoPlayerDelegate {
    /// 展示视图状态
    //: func showViewsViaStage() {
    func dominoEffect() {
        //: switch chatModel.videoStage {
        switch chatModel.videoStage {
        //: case .Waiting:
        case .Waiting:
            //: desLab.text = "waiting for the other party to accept the video invitation...".localized
            desLab.text = String(bytes: kAtToFormat.reversed(), encoding: .utf8)!.localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
        //: case .Recieved:
        case .Recieved:
            //: desLab.text = "Invite you to a video call...".localized
            desLab.text = (String(appViewStr) + String(data_equalTitle.prefix(5)) + "o call...").localized
            //: desLab.isHidden = false
            desLab.isHidden = false
            //: pairInfoBg.isHidden = false
            pairInfoBg.isHidden = false
            //: minimizeBtn.isHidden = true
            minimizeBtn.isHidden = true
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
                //: acceptBtn.isHidden = false
                acceptBtn.isHidden = false
                //: femaleAcceptBtn.isHidden = true
                femaleAcceptBtn.isHidden = true
                //: femaleAcceptTipsBtn.isHidden = true
                femaleAcceptTipsBtn.isHidden = true
                //: acceptPlayer.isHidden = true
                acceptPlayer.isHidden = true
                //: femaleAcceptBGBtn.isHidden = true
                femaleAcceptBGBtn.isHidden = true
                //: rotateCameraBtn.isHidden = true
                rotateCameraBtn.isHidden = true
                //: openCameraBtn.isHidden = true
                openCameraBtn.isHidden = true
                //: } else {
            } else {
                //: acceptBtn.isHidden = true
                acceptBtn.isHidden = true
                //: femaleAcceptBtn.isHidden = false
                femaleAcceptBtn.isHidden = false
                //: femaleAcceptTipsBtn.isHidden = false
                femaleAcceptTipsBtn.isHidden = false
                //: femaleAcceptBGBtn.isHidden = false
                femaleAcceptBGBtn.isHidden = false
                //: let date = Defaults.object(forKey: TalkingCallShowTag)
                let date = data_onEndFormat.object(forKey: main_statusName)
                //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                let today = NSDate.packthread(date: Date(), dateFormat: (String(appBeginModelLetStr.suffix(7)) + String(userModeFinishStr)))
                //: if date == nil || (date as! String) != today {
                if date == nil || (date as! String) != today {
                    //: Defaults.set(today, forKey: TalkingCallShowTag)
                    data_onEndFormat.set(today, forKey: main_statusName)
                    //: acceptPlayer.isHidden = false
                    acceptPlayer.isHidden = false
                    //: } else {
                } else {
                    //: acceptPlayer.isHidden = true
                    acceptPlayer.isHidden = true
                }
                //: autoAcceptDesLab.isHidden = false
                autoAcceptDesLab.isHidden = false
                // 直播时自动接听
                //: if StatisticalTableReactiveCompatible.share.appConfigMode.autoAnswer == 2 &&
                if StatisticalTableReactiveCompatible.share.appConfigMode.autoAnswer == 2 &&
                    //: NEED_RESTORE_LIVE == true {
                    noti_numberManagerUrl == true
                {
                    //: showOrHidenAutoRecieved(autoAccept: true)
                    dishTheDirtCapacity(autoAccept: true)
                    //: } else {
                } else {
                    //: showOrHidenAutoRecieved()
                    dishTheDirtCapacity()
                }
            }
            //: reportBtn.isHidden = true
            reportBtn.isHidden = true
            //: sendGiftBtn.isHidden = true
            sendGiftBtn.isHidden = true
            //: rechargeBtn.isHidden = true
            rechargeBtn.isHidden = true
            //: touchHidenBtn.isEnabled = false
            touchHidenBtn.isEnabled = false
            //: timeLab.isHidden = true
            timeLab.isHidden = true
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: chatBtn.isHidden = true
            chatBtn.isHidden = true
            //: danmuView.isHidden = true
            danmuView.isHidden = true
        //: case .Calling:
        case .Calling:
            //: desLab.text = nil
            desLab.text = nil
            //: desLab.isHidden = true
            desLab.isHidden = true
            //: pairInfoBg.isHidden = true
            pairInfoBg.isHidden = true
            //: minimizeBtn.isHidden = false
            minimizeBtn.isHidden = false
            //: hangupBtn.isHidden = false
            hangupBtn.isHidden = false
            //: acceptBtn.isHidden = true
            acceptBtn.isHidden = true
            //: femaleAcceptBtn.isHidden = true
            femaleAcceptBtn.isHidden = true
            //: femaleAcceptTipsBtn.isHidden = true
            femaleAcceptTipsBtn.isHidden = true
            //: acceptPlayer.isHidden = true
            acceptPlayer.isHidden = true
            //: femaleAcceptBGBtn.isHidden = true
            femaleAcceptBGBtn.isHidden = true
            //: rotateCameraBtn.isHidden = false
            rotateCameraBtn.isHidden = false
            //: openCameraBtn.isHidden = false
            openCameraBtn.isHidden = false
            //: reportBtn.isHidden = !reportBtn.isEnabled || StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue
            reportBtn.isHidden = !reportBtn.isEnabled || StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.female.rawValue
            //: sendGiftBtn.isHidden = false
            sendGiftBtn.isHidden = false
            //: rechargeBtn.isHidden = false
            rechargeBtn.isHidden = false
            //: touchHidenBtn.isEnabled = true
            touchHidenBtn.isEnabled = true
            //: timeLab.isHidden = false
            timeLab.isHidden = false
            //: chatBtn.isHidden = false
            chatBtn.isHidden = false
            //: danmuView.isHidden = false
            danmuView.isHidden = false
            //: autoAcceptDesLab.isHidden = true
            autoAcceptDesLab.isHidden = true
            //: if startTime != nil {
            if startTime != nil {
                //: changeTalkTime()
                postTo()
                //: } else {
            } else {
                //: timeLab.text = "00:00"
                timeLab.text = (app_smartFormat.replacingOccurrences(of: "count", with: "00"))
            }
            //: startTalkCount()
            buttonPath()
            //: touchHiddenTimer()
            harbour()
            //: if TalkingSocketManager.shared.isFreeCall {
            if MakeSocketDelegate.shared.isFreeCall {
                //: setFreeView()
                outOfSchool()
                //: setFreeCountTimer()
                skimEnableTimer()
            }
            //: if reportBtn.isHidden {
            if reportBtn.isHidden {
                //: sendGiftBtn.snp.remakeConstraints { make in
                sendGiftBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview().offset(-10)
                    make.trailing.equalToSuperview().offset(-10)
                    //: make.bottom.equalTo(reportBtn)
                    make.bottom.equalTo(reportBtn)
                    //: make.size.equalTo(reportBtn)
                    make.size.equalTo(reportBtn)
                }
            }
        }

        //: updateLayout()
        pauseZone()
        //: afterGetInfo()
        dataWith()
    }

    /// 刷新通话视图数据
    //: func afterGetInfo() {
    func dataWith() {
        //: pairNameLab.text = chatModel.pairNickName
        pairNameLab.text = chatModel.pairNickName
        //: locationBtn.isHidden = chatModel.location.count <= 0
        locationBtn.isHidden = chatModel.location.count <= 0
        //: locationBtn.setTitle("\(chatModel.location)", for: .normal)
        locationBtn.setTitle("\(chatModel.location)", for: .normal)
        //: femaleAcceptTipsBtn.setTitle(" +" + "%@ points/min".localizedArguments(chatModel.points), for: .normal)
        femaleAcceptTipsBtn.setTitle(" +" + (String(dataBlindId)).arguments(chatModel.points), for: .normal)
        //: let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        let optionsInfo = [KingfisherOptionsInfoItem.processor(RoundCornerImageProcessor(cornerRadius: actualWidth(w: 20)))]
        //: pairPicV.setUrlImage(urlStr: chatModel.pairHeadPic, options: optionsInfo)
        pairPicV.mentalImageDownRelease(urlStr: chatModel.pairHeadPic, options: optionsInfo)

        // 男性展示视频封面 【非通话中】
        //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
            //: coverImg.isHidden = false
            coverImg.isHidden = false
            //: coverImg.setUrlImage(urlStr: chatModel.pairHeadPic)
            coverImg.sinceMoment(urlStr: chatModel.pairHeadPic)
            //: if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
            if chatModel.videoStage != .Calling, chatModel.videoUrl.count > 0 {
                //: player.delegate = self
                player.delegate = self
                //: player.playerWithUrlAndVideoView(url: chatModel.videoUrl, view: self)
                player.primrosePath(url: chatModel.videoUrl, view: self)
            }
        }
        //: if chatModel.videoStage == .Calling {
        if chatModel.videoStage == .Calling {
            //: coverImg.isHidden = true
            coverImg.isHidden = true
            //: player.stopPlay()
            player.docket()
            //: player.removeVideoWidget()
            player.eye()
        }
    }

    /// 最小化按钮事件
    //: @objc private func minimizeButtonClick() {
    @objc private func moveLab() {
        //: delegate?.interactionView_shouldDisplayMiniWindow()
        delegate?.restrictionWindow()
    }

    /// 添加视频通话画面窗口
    /// - Parameter view: 视频画面窗口
    //: func addWindowView(view: UIView) {
    func darkView(view: UIView) {
        //: insertSubview(view, belowSubview: pairPicV)
        insertSubview(view, belowSubview: pairPicV)
    }

    /// 挂断按钮事件
    //: @objc private func hangupButtonClick() {
    @objc private func cellClick() {
        //: delegate?.interactionView_shouldEndCall()
        delegate?.game()
    }

    /// 接听按钮事件
    //: @objc private func acceptButtonClick() {
    @objc private func down() {
        //: self.cancelAutoAcceptTimer()
        self.who()
        //: delegate?.interactionView_shouldAcceptCall()
        delegate?.colorEnable()
    }

    /// 旋转摄像头事件
    //: @objc private func rotateCameraButtonClick() {
    @objc private func currentCell() {
        //: if openCameraBtn.isSelected == true {
        if openCameraBtn.isSelected == true { // 腾讯美颜 && 摄像头关闭时
            //: func__showStatusBarErrorMsg(showMsg: "Camera must be on".localized)
            alongShow(showMsg: (String(notiPushInfoText) + String(appActionStr.suffix(8))).localized)
            //: return
            return
        }
        //: rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        rotateCameraBtn.isSelected = !rotateCameraBtn.isSelected
        //: delegate?.interactionView_shouldChangeCamera(isFront: !rotateCameraBtn.isSelected)
        delegate?.transmutationPolaroidLandCameraShouldForepartSumeractionView(isFront: !rotateCameraBtn.isSelected)
    }

    /// 是否禁用摄像头事件
    //: @objc private func openCameraButtonClick() {
    @objc private func cineCamera() {
        //: openCameraBtn.isSelected = !openCameraBtn.isSelected
        openCameraBtn.isSelected = !openCameraBtn.isSelected
        //: delegate?.interactionView_shouldForbiddenCamera(forbidden: openCameraBtn.isSelected)
        delegate?.componentSearched(forbidden: openCameraBtn.isSelected)
    }

    /// 举报按钮点击事件（男性）
    //: @objc private func reportButtonClick() {
    @objc private func informClick() {
        //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        let reportView = SizeThen(frame: UIScreen.main.bounds, type: .reportVideo, rId: String(chatModel.logId))
        //: reportView.showReportViewIn(view: nil)
        reportView.pathView(view: nil)
        //: reportView.reportHideBlock = { [weak self] succeed in
        reportView.reportHideBlock = { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            // 投诉成功
            //: self.reportBtn.isEnabled = false
            self.reportBtn.isEnabled = false
            //: self.reportBtn.isHidden = true
            self.reportBtn.isHidden = true
            //: self.delegate?.interactionView_reportSucceed()
            self.delegate?.story()
        }
    }

    /// 送礼按钮事件
    //: @objc private func sendGiftButtonClick() {
    @objc private func counteraction() {
        //: func__sendGift()
        breeze()
    }

    /// 充值按钮事件
    //: @objc private func rechargeBtnClick() {
    @objc private func streamTime() {
//        self.isShowRecharge = true
        //: EditPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
        EditPushManager.share.addFace(webViewType: .RechargeHalfPage)
    }

    //: @objc private func touchHidenBtnClick() {
    @objc private func itemColor() {
        //: changeInteractionShowState()
        changeState()
        //: if isShowInput {
        if isShowInput {
            //: dismissClick()
            equalClick()
        }
    }

    /// 点击展示/隐藏通话中的按钮
    //: @objc private func changeInteractionShowState() {
    @objc private func changeState() {
        //: let isHidden = !rotateCameraBtn.isHidden
        let isHidden = !rotateCameraBtn.isHidden
        //: minimizeBtn.isHidden = isHidden
        minimizeBtn.isHidden = isHidden
        //: rotateCameraBtn.isHidden = isHidden
        rotateCameraBtn.isHidden = isHidden
        //: openCameraBtn.isHidden = isHidden
        openCameraBtn.isHidden = isHidden
        //: reportBtn.isHidden = (reportBtn.isEnabled ? isHidden:true) ||  StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue
        reportBtn.isHidden = (reportBtn.isEnabled ? isHidden : true) || StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.female.rawValue
        //: sendGiftBtn.isHidden = isHidden
        sendGiftBtn.isHidden = isHidden
        //: rechargeBtn.isHidden = isHidden
        rechargeBtn.isHidden = isHidden
        //: hangupBtn.isHidden = isHidden
        hangupBtn.isHidden = isHidden
        //: timeLab.isHidden = isHidden
        timeLab.isHidden = isHidden
        //: if TalkingSocketManager.shared.isFreeCall {
        if MakeSocketDelegate.shared.isFreeCall {
            //: self.freeTimeView.isHidden = isHidden
            self.freeTimeView.isHidden = isHidden
        }
        //: if isHidden {
        if isHidden {
            //: invalidateIdleTimer()
            allAcross()
            //: } else {
        } else {
            //: touchHiddenTimer()
            harbour()
        }
    }

    /// 去电，x秒后才显示挂断按钮
    //: private func isShowHangupBtn() {
    private func executeAdd() {
        //: if chatModel.isCaller && chatModel.videoStage == .Waiting && StatisticalTableReactiveCompatible.share.appUserConfigMode.callCancelBtnDelayTime > 0 && StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if chatModel.isCaller, chatModel.videoStage == .Waiting, StatisticalTableReactiveCompatible.share.appUserConfigMode.callCancelBtnDelayTime > 0, StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
            //: hangupBtn.isHidden = true
            hangupBtn.isHidden = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now()+Double(StatisticalTableReactiveCompatible.share.appUserConfigMode.callCancelBtnDelayTime)) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + Double(StatisticalTableReactiveCompatible.share.appUserConfigMode.callCancelBtnDelayTime)) {
                //: self.hangupBtn.isHidden = false
                self.hangupBtn.isHidden = false
            }
        }
    }

    /// 取消GCD自动接听定时器
    //: private func cancelAutoAcceptTimer() {
    private func who() {
        //: if autoAcceptTimer != nil {
        if autoAcceptTimer != nil {
            //: autoAcceptTimer?.cancel()
            autoAcceptTimer?.cancel()
            //: autoAcceptTimer = nil
            autoAcceptTimer = nil
        }
    }
}

// MARK: - TextVideoThen

//: extension Talking1v1VideoInteractionView: TalkingVideoDanmuManagerDelegate {
extension ModeInfoPlayerDelegate: SaveReactiveCompatible {
    //: func func__DanmuRecvNewModel(Msg: TalkingVideoCallDanmuModel) {
    func choice(Msg: EffectHandyJSON) {
        //: danmuView.addNewMsg(msgModel: Msg)
        danmuView.effectModel(msgModel: Msg)
    }

    //: @objc private func commentBtnClick() {
    @objc private func today() {
        /// 拉起弹幕评论
        //: self.bringSubviewToFront(commInputView)
        self.bringSubviewToFront(commInputView)
        //: commInputView.updatePlaceholder()
        commInputView.allowContent()
    }
}

// MARK: - 发表弹幕代理

//: extension Talking1v1VideoInteractionView: TalkingVideoCallInputViewDelegate {
extension ModeInfoPlayerDelegate: MakeViewDelegate {
    //: @objc func dismissClick() {
    @objc func equalClick() {
        //: self.isShowInput = false
        self.isShowInput = false
//        commInputView.inputTextView.text = ""
        //: commInputView.resignkeyBoard()
        commInputView.videoInsideBoard()
    }

    //: func func__sendTextMsg(msgStr: String) {
    func funcChapterRoute(msgStr: String) {
        //: self.isShowInput = false
        self.isShowInput = false
        //: sendToTextMsg(msgStr: msgStr)
        shadowOf(msgStr: msgStr)
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func betweenBottom(heightToBottom: CGFloat) {
        //: if heightToBottom > 0 {
        if heightToBottom > 0 {
            //: self.isShowInput = true
            self.isShowInput = true
        }
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self).offset(-heightToBottom)
            make.bottom.equalTo(self).offset(-heightToBottom)
        }
        //: danmuView.snp.updateConstraints { make in
        danmuView.snp.updateConstraints { make in
            //: let height = heightToBottom == 0 ? -10 : -(heightToBottom-chatBtn.height+commInputView.height-kDeviceSafeBottomHeight+10)
            let height = heightToBottom == 0 ? -10 : -(heightToBottom - chatBtn.height + commInputView.height - constBarStr + 10)
            //: make.bottom.equalTo(chatBtn.snp.top).offset(height)
            make.bottom.equalTo(chatBtn.snp.top).offset(height)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func mixUp(height: CGFloat) {
        //: commInputView.snp.updateConstraints { make in
        commInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }

        //: self.needsUpdateConstraints()
        self.needsUpdateConstraints()
        //: self.updateConstraintsIfNeeded()
        self.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.layoutIfNeeded()
            self.layoutIfNeeded()
            //: })
        })
    }

    /// 发送文本消息
    //: func sendToTextMsg(msgStr: String) {
    func shadowOf(msgStr: String) {
        //: TalkingVideoDanmuManager.uploadToTextMsg(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, errorModel in
        TextVideoThen.sufficient(logId: "\(chatModel.logId)", content: msgStr) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: let json = JSON(result!)
                let json = JSON(result!)
                //: let msgId = json["msgId"].string
                let msgId = json[String(bytes: constValueContent.map{inModel(action: $0)}, encoding: .utf8)!].string
                //: var toUid: String?
                var toUid: String?
                //: if self.chatModel.iUid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
                if self.chatModel.iUid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
                    //: toUid = self.chatModel.pairUid
                    toUid = self.chatModel.pairUid
                    //: } else {
                } else {
                    //: toUid = self.chatModel.iUid
                    toUid = self.chatModel.iUid
                }
                //: let data = ["msgId": msgId, "senderUid": StatisticalTableReactiveCompatible.share.loginUserMode.userID, "toUid": toUid, "nickname": StatisticalTableReactiveCompatible.share.loginUserMode.nickname, "content": msgStr, "isMySender": "1"]
                let data = [String(bytes: constValueContent.map{inModel(action: $0)}, encoding: .utf8)!: msgId, String(bytes: constLinkMessage.map{imageGo(fatal: $0)}, encoding: .utf8)!: StatisticalTableReactiveCompatible.share.loginUserMode.userID, (String(const_pathValue.prefix(5))): toUid, String(bytes: mainCurrentSourceId.map{writerSTitle(lab: $0)}, encoding: .utf8)!: StatisticalTableReactiveCompatible.share.loginUserMode.nickname, String(bytes: data_countItemRawPath.reversed(), encoding: .utf8)!: msgStr, String(bytes: showLayerTitle.map{mediumAction(cover: $0)}, encoding: .utf8)!: "1"]
                //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data as [String: Any])
                TextVideoThen.infix().toMsg(msg: data as [String: Any])
                //: self.rotateCameraBtn.isHidden = true
                self.rotateCameraBtn.isHidden = true
                //: self.changeInteractionShowState()
                self.changeState()
                //: self.commInputView.inputTextView.attributedText = nil
                self.commInputView.inputTextView.attributedText = nil
            }
        }
    }
}

// MARK: - DenominationReactiveCompatible【视频播放器】

//: extension Talking1v1VideoInteractionView: TalkingVideoPlayerDelegate {
extension ModeInfoPlayerDelegate: DenominationReactiveCompatible {
    //: func func__playerStatus(player: TalkingVideoPlayerManager, status: VideoPlayerStatus) {
    func serviceComponent(player _: ScreenPlayListener, status: DigitizerPlayerStatus) {
        //: if status == .Playing {
        if status == .Playing {
            //: self.player.setRenderMode(renderMode: .FILL_SCREEN)
            self.player.modusVivendi(renderMode: .FILL_SCREEN)
            //: self.coverImg.isHidden = true
            self.coverImg.isHidden = true
        }
    }

    //: func func__playerDuration(player: TalkingVideoPlayerManager, duration: Int, currentTime: Int) {}
    func re(player _: ScreenPlayListener, duration _: Int, currentTime _: Int) {}

    //: func func__playerProgress(player: TalkingVideoPlayerManager, progress: CGFloat) {}
    func pastAction(player _: ScreenPlayListener, progress _: CGFloat) {}
}

// MARK: - 通知事件

//: extension Talking1v1VideoInteractionView {
extension ModeInfoPlayerDelegate {
    /// 商汤美颜是否捕获到人脸通知（女性）
    //: @objc private func catchFaceResultNotification(notification: NSNotification) {
    @objc private func invisible(notification: NSNotification) {
        // 女性 && 等待接听
        //: guard StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.female.rawValue else { return }
        //: guard self.chatModel.videoStage == .Recieved else { return }
        guard self.chatModel.videoStage == .Recieved else { return }

        //: let userInfo = notification.userInfo as! [String: AnyObject]
        let userInfo = notification.userInfo as! [String: AnyObject]
        //: let hasFace = (userInfo["hasFace"] as! NSNumber).description.boolValue
        let hasFace = (userInfo[(String(data_pageUrl.prefix(6)) + dataWithDoingName.replacingOccurrences(of: "scale", with: "e"))] as! NSNumber).description.boolValue
        //: showOrHidenAutoRecieved(autoAccept: hasFace)
        dishTheDirtCapacity(autoAccept: hasFace)
    }

    /// 是否展示自动接听按钮
    /// - Parameter autoAccept: 是否自动接听（默认：false）
    //: private func showOrHidenAutoRecieved(autoAccept: Bool = false) {
    private func dishTheDirtCapacity(autoAccept: Bool = false) {
        //: var isActive = false
        var isActive = false

        //: if StatisticalTableReactiveCompatible.share.appConfigMode.autoAnswer == 1 {
        if StatisticalTableReactiveCompatible.share.appConfigMode.autoAnswer == 1 {
            //: let timeDifference = Date().timeIntervalSince1970 - (TalkingApplication.shared as! TalkingApplication).lastEventTime
            let timeDifference = Date().timeIntervalSince1970 - (ViewKingfisherCompatible.shared as! ViewKingfisherCompatible).lastEventTime
            //: if timeDifference < 5 {
            if timeDifference < 5 {
                //: isActive = true
                isActive = true
            }
        }

        //: DispatchQueue.main.async { [weak self] in
        DispatchQueue.main.async { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if autoAccept || isActive {
            if autoAccept || isActive { // 自动接听
                //: self.autoAcceptDesLab.isHidden = false
                self.autoAcceptDesLab.isHidden = false

                //: var countdown = StatisticalTableReactiveCompatible.share.appConfigMode.autoAnswerCountdownTime
                var countdown = StatisticalTableReactiveCompatible.share.appConfigMode.autoAnswerCountdownTime
                // 防止多次初始化
                //: guard self.autoAcceptTimer == nil else { return }
                guard self.autoAcceptTimer == nil else { return }
                //: self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                self.autoAcceptTimer = DispatchSource.makeTimerSource(queue: DispatchQueue.global())
                //: self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                self.autoAcceptTimer?.schedule(deadline: .now(), repeating: .seconds(1))
                //: self.autoAcceptTimer?.setEventHandler {
                self.autoAcceptTimer?.setEventHandler {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: guard countdown > 0 else {
                        guard countdown > 0 else {
                            //: if self.chatModel.videoStage == .Recieved {
                            if self.chatModel.videoStage == .Recieved {
                                //: self.acceptButtonClick()
                                self.down()
                            }
                            //: return
                            return
                        }
                        //: self.autoAcceptDesLab.text = "Automatically answer for you in %@s".localizedArguments(countdown)
                        self.autoAcceptDesLab.text = String(bytes: notiResultTitle.map{$0^219}, encoding: .utf8)!.arguments(countdown)
                        //: countdown -= 1
                        countdown -= 1
                    }
                }
                //: self.autoAcceptTimer?.resume()
                self.autoAcceptTimer?.resume()
            }
        }
    }

    /// 半屏充值页退下
    //: @objc private func changeRechargeStatus() {
    @objc private func carryEnableAll() {}
}

// MARK: -

//: extension Talking1v1VideoInteractionView {
extension ModeInfoPlayerDelegate {
    //: func showPointAnimaData(point: String) {
    func terminalView(point: String) {
        //: addSubview(pointAnimView)
        addSubview(pointAnimView)
        //: pointAnimView.snp.remakeConstraints { make in
        pointAnimView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
            make.size.equalTo(CGSize(width: actualWidth(w: 100), height: actualWidth(w: 100)))
        }
        //: pointAnimView.setPointData(point: point)
        pointAnimView.afterwards(point: point)
    }
}

// MARK: - 送礼

//: extension Talking1v1VideoInteractionView {
extension ModeInfoPlayerDelegate {
    //: func func__sendGift() {
    func breeze() {
        //: TalkingChatGiftManager.share.func__sendGiftEvent {
        TableUpReactiveCompatible.share.viewAcross {
            //: self.func__showGiftChoiceView()
            self.fileDevise()
        }
    }

    //: func func__showGiftChoiceView() {
    func fileDevise() {
        //: giftView.updateGiftInfo(needReload: true, mf_coin: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin)
        giftView.obscure(needReload: true, mf_coin: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin)
        //: window?.addSubview(giftView)
        window?.addSubview(giftView)
        //: giftView.showView()
        giftView.rawBeauty()
        //: giftView.sendActionBlock = { [weak self] (_ giftModel: TalkingRoomGiftModel, _ num: String) in
        giftView.sendActionBlock = { [weak self] (_ giftModel: GroupModelType, _ num: String) in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.func__sendGiftMsg(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
            self.worldView(giftId: giftModel.gid, giftNum: num, pkgItemsetId: giftModel.pkgItemsetId, programSort: nil)
        }
    }

    //: func func_giftViewDismiss() {
    func contentWithDismiss() {
        //: giftView.dismissView()
        giftView.trademark()
    }

    //: func func__sendGiftMsg(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
    func worldView(giftId: String, giftNum: String, pkgItemsetId: String?, programSort: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            alongShow(showMsg: app_netValue)
            //: return
            return
        }
        //: var params: [String: Any] = [:]
        var params: [String: Any] = [:]
        //: if chatModel.iUid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
            //: params["toUid"] = chatModel.pairUid
            params[(String(const_pathValue.prefix(5)))] = chatModel.pairUid
            //: } else {
        } else {
            //: params["toUid"] = chatModel.iUid
            params[(String(const_pathValue.prefix(5)))] = chatModel.iUid
        }
        //: params["giftId"] = giftId
        params[(appBackgroundFormat.replacingOccurrences(of: "name", with: "g") + String(userLengthMsg))] = giftId
        //: params["giftNum"] = giftNum
        params[(String(dataAddMsg.prefix(7)))] = giftNum
        //: params["from"] = "videoCall"
        params[(app_rowData.lowercased())] = (String(dataRestoreTitle.prefix(9)))
        //: if programSort != nil {
        if programSort != nil {
            //: params["programSort"] = programSort
            params[(String(showUserId) + String(userPathKey.suffix(4)))] = programSort
        }
        //: if pkgItemsetId != nil {
        if pkgItemsetId != nil {
            //: params["pkgItemsetId"] = pkgItemsetId
            params[(String(noti_monthValue) + String(k_modelMessage.suffix(4)))] = pkgItemsetId
        }

        //: ChatContentReactiveCompatible.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ChatContentReactiveCompatible.receiveParam(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.afterOf(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if pkgItemsetId != nil {
            if pkgItemsetId != nil {
                //: self.giftView.bags_removeGiftPackage(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.giftView.stateFor(pkgItemsetId: pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! [String: Any?])
            self.noService(extral: result as! [String: Any?])
            //: })
        })
    }

    //: func func__insertGiftMessageWithExtral(extral: [String: Any?]) {
    func noService(extral: [String: Any?]) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: [String: Any] = extralInfo["msgInfo"] as! [String: Any]
        var msgInfoDic: [String: Any] = extralInfo[(String(const_viewId.suffix(5)) + String(kLinePath))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(noti_equalUrl))] = (String(notiVideoTableValue))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(const_viewId.suffix(5)) + String(kLinePath))] = msgInfoDic

        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        changeDoing(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(const_textPath) + String(appMakeName.suffix(6)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(const_textPath) + String(appMakeName.suffix(6)))] as! NSNumber
            //: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: giftView.updateGiftInfo(needReload: false, mf_coin: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin)
        giftView.obscure(needReload: false, mf_coin: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin)

        //: var toUid = ""
        var toUid = ""
        //: if chatModel.iUid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
            //: toUid = chatModel.pairUid
            toUid = chatModel.pairUid
            //: } else {
        } else {
            //: toUid = chatModel.iUid
            toUid = chatModel.iUid
        }

        //: let cellData = TUISocialChatManager.insertTXMessage(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)
        let cellData = KitThen.phoneMessage(withExtral: extralInfo, toUid: toUid, isSender: true, isTip: false)

        //: NotificationCenter.default.post(name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION, object: self, userInfo: ["cellData": cellData])
        NotificationCenter.default.post(name: dataBarTitle, object: self, userInfo: [String(bytes: userViewText.map{sexText(selected: $0)}, encoding: .utf8)!: cellData])
    }

    //: func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    func afterOf(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard StatisticalTableReactiveCompatible.share.loginUserMode.status != 1 else {
            guard StatisticalTableReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    alongShow(showMsg: errorStr)
                }
                //: return
                return
            }
            //: EditPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            EditPushManager.share.statusOf(clickEvent: user_scaleMessage, sufficient: false)
            //: giftView.dismissView()
            giftView.trademark()
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = DateView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName))
            //: view.show()
            view.reShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                alongShow(showMsg: errorStr)
            }
        }
    }

    //: func initCachEffectData() {
    func equalStart() {
        //: var targetId = ""
        var targetId = ""
        //: if chatModel.iUid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
        if chatModel.iUid == StatisticalTableReactiveCompatible.share.loginUserMode.userID {
            //: targetId = chatModel.pairUid
            targetId = chatModel.pairUid
            //: } else {
        } else {
            //: targetId = chatModel.iUid
            targetId = chatModel.iUid
        }
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.fileSend(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: [String: Any]) {
    func changeDoing(giftDic: [String: Any]) {
        //: let giftModel = giftEffectView.changeGiftToModel(dict: giftDic) ?? TalkingGiftMsgAnimatModel()
        let giftModel = giftEffectView.closeCustomDict(dict: giftDic) ?? GiftHandyJSON()
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in TableUpReactiveCompatible.share.underFinish() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [MakeTransformable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.allInput(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.everyErrorDict(dict: giftDic)
    }

    //: func addGiftEffectModelArr(giftArr: [Any]) {
    func portion(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in TableUpReactiveCompatible.share.underFinish() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [MakeTransformable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.allInput(model: giftModel as! GiftHandyJSON)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.lagniappe(modelArr: giftArr)
    }
}

// MARK: - 通话计时器

//: extension Talking1v1VideoInteractionView {
extension ModeInfoPlayerDelegate {
    /// 开始通话计时
    //: private func startTalkCount() {
    private func buttonPath() {
        //: guard countTimer == nil else { return }
        guard countTimer == nil else { return }

        //: if startTime == nil {
        if startTime == nil {
            //: let interval = Date().timeIntervalSince1970
            let interval = Date().timeIntervalSince1970
            //: startTime = interval
            startTime = interval
            //: chatModel.startTime = startTime
            chatModel.startTime = startTime
        }
        //: countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        countTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeTalkTime()
            self.postTo()
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 免费视频剩余时间倒计时
    //: private func setFreeCountTimer() {
    private func skimEnableTimer() {
        //: guard freeCountTimer == nil else { return }
        guard freeCountTimer == nil else { return }
        //: var  index = 60
        var index = 60
        //: freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
        freeCountTimer = Timer.scheduledTimer(withTimeInterval: 1, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: index -= 1
            index -= 1
            //: self.freeTimeView.setTipsMessage(count: index)
            self.freeTimeView.totaluition(count: index)
            //: }, repeats: true)
        }, repeats: true)
    }

    // 展示通话时间
    //: private func changeTalkTime() {
    private func postTo() {
        //: let duration = getCurrentTalkingTime()
        let duration = birthTime()
        //: let min = floor(duration/60)
        let min = floor(duration / 60)
        //: let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        let sec = floor(duration.truncatingRemainder(dividingBy: 60))
        //: timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
        timeLab.text = String(format: "%02i:%02i", Int(min), Int(sec))
    }

    /// 恢复通话，更新通话时间
    /// - Parameter startTime: 通话时间
    //: func updateStartTime(startTime: TimeInterval) {
    func startEmpty(startTime: TimeInterval) {
        //: self.startTime = startTime
        self.startTime = startTime
        //: changeTalkTime()
        postTo()
    }

    /// 获取当前通话时间
    /// - Returns: 秒
    //: func getCurrentTalkingTime() -> Double {
    func birthTime() -> Double {
        //: guard chatModel.videoStage == .Calling else { return 0 }
        guard chatModel.videoStage == .Calling else { return 0 }
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let duration = floor(interval-(startTime ?? 0))
        let duration = floor(interval - (startTime ?? 0))
        //: return duration
        return duration
    }
}

// MARK: - 【通话中】触屏隐藏交互按钮逻辑

//: extension Talking1v1VideoInteractionView {
extension ModeInfoPlayerDelegate {
    /// 触摸隐藏开启延时
    //: private func touchHiddenTimer() {
    private func harbour() {
        //: invalidateIdleTimer()
        allAcross()
        //: perform(#selector(hideInteractionView), afterDelay: 10)
        perform(#selector(modelEqualPicture), afterDelay: 10)
    }

    /// 销毁延迟
    //: private func invalidateIdleTimer() {
    private func allAcross() {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(hideInteractionView), object: nil)
        NSObject.cancelPreviousPerformRequests(withTarget: self, selector: #selector(modelEqualPicture), object: nil)
    }

    /// 延时到时隐藏视图
    //: @objc private func hideInteractionView() {
    @objc private func modelEqualPicture() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.changeInteractionShowState()
            self.changeState()
        }
    }
}

// MARK: - Layout

//: extension Talking1v1VideoInteractionView {
extension ModeInfoPlayerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func legacyBroad() {
        //: addSubview(giftEffectView)
        addSubview(giftEffectView)
        //: insertSubview(giftTrackView, belowSubview: giftEffectView)
        insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: addSubview(coverImg)
        addSubview(coverImg)
        //: addSubview(touchHidenBtn)
        addSubview(touchHidenBtn)
        //: addSubview(pairInfoBg)
        addSubview(pairInfoBg)
        //: pairInfoBg.addSubview(pairPicV)
        pairInfoBg.addSubview(pairPicV)
        //: pairInfoBg.addSubview(pairNameLab)
        pairInfoBg.addSubview(pairNameLab)
        //: pairInfoBg.addSubview(locationBtn)
        pairInfoBg.addSubview(locationBtn)
        //: addSubview(desLab)
        addSubview(desLab)
        //: addSubview(minimizeBtn)
        addSubview(minimizeBtn)
        //: addSubview(hangupBtn)
        addSubview(hangupBtn)
        //: addSubview(rotateCameraBtn)
        addSubview(rotateCameraBtn)
        //: addSubview(openCameraBtn)
        addSubview(openCameraBtn)
        //: addSubview(sendGiftBtn)
        addSubview(sendGiftBtn)
        //: addSubview(rechargeBtn)
        addSubview(rechargeBtn)
        //: addSubview(timeLab)
        addSubview(timeLab)
        //: addSubview(reportBtn)
        addSubview(reportBtn)
        //: addSubview(acceptBtn)
        addSubview(acceptBtn)
        //: addSubview(chatBtn)
        addSubview(chatBtn)
        //: addSubview(danmuView)
        addSubview(danmuView)
        //: addSubview(commInputView)
        addSubview(commInputView)
        //: addSubview(femaleAcceptBGBtn)
        addSubview(femaleAcceptBGBtn)
        //: addSubview(femaleAcceptBtn)
        addSubview(femaleAcceptBtn)
        //: femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        femaleAcceptBtn.addSubview(femaleAcceptTipsBtn)
        //: addSubview(acceptPlayer)
        addSubview(acceptPlayer)
        //: addSubview(autoAcceptDesLab)
        addSubview(autoAcceptDesLab)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func viewWithoutConfinementConstraint() {
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(self)
            make.leading.trailing.top.bottom.equalTo(self)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(self)
            make.leading.equalTo(self)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - constBarStr)
        }
        //: coverImg.snp.makeConstraints { make in
        coverImg.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: touchHidenBtn.snp.makeConstraints { make in
        touchHidenBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: pairInfoBg.snp.makeConstraints { make in
        pairInfoBg.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+16)
            make.top.equalTo(user_keyId + 16)
            //: make.leading.equalTo(15)
            make.leading.equalTo(15)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
            make.size.equalTo(CGSize(width: actualWidth(w: 128), height: actualWidth(w: 50)))
        }
        //: pairPicV.snp.makeConstraints { make in
        pairPicV.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 5))
            make.leading.equalTo(actualWidth(w: 5))
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
            make.size.equalTo(CGSize(width: actualWidth(w: 40), height: actualWidth(w: 40)))
        }
        //: pairNameLab.snp.makeConstraints { make in
        pairNameLab.snp.makeConstraints { make in
            //: make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            make.leading.equalTo(pairPicV.snp.trailing).offset(8)
            //: make.top.equalTo(7)
            make.top.equalTo(7)
            //: make.height.equalTo(17)
            make.height.equalTo(17)
            //: make.trailing.equalToSuperview().offset(-7)
            make.trailing.equalToSuperview().offset(-7)
        }
        //: locationBtn.snp.makeConstraints { make in
        locationBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(pairNameLab)
            make.leading.equalTo(pairNameLab)
            //: make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            make.top.equalTo(pairNameLab.snp.bottom).offset(4)
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
        }
        //: desLab.snp.makeConstraints { make in
        desLab.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight+86)
            make.top.equalToSuperview().offset(user_keyId + 86)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
        }
        //: minimizeBtn.snp.makeConstraints { make in
        minimizeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+10)
            make.top.equalTo(user_keyId + 10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.height.equalTo(actualWidth(w: 42))
            make.width.height.equalTo(actualWidth(w: 42))
        }

        //: acceptBtn.snp.makeConstraints { make in
        acceptBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(hangupBtn)
            make.bottom.equalTo(hangupBtn)
            //: make.size.equalTo(hangupBtn)
            make.size.equalTo(hangupBtn)
        }
        //: femaleAcceptBtn.snp.makeConstraints { make in
        femaleAcceptBtn.snp.makeConstraints { make in
            //: make.edges.equalTo(hangupBtn)
            make.edges.equalTo(hangupBtn)
        }
        //: femaleAcceptTipsBtn.snp.makeConstraints { make in
        femaleAcceptTipsBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-9)
            make.bottom.equalTo(-9)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }
        //: timeLab.snp.makeConstraints { make in
        timeLab.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(StatusBarHeight+20)
            make.top.equalTo(user_keyId + 20)
        }
        //: autoAcceptDesLab.snp.makeConstraints { make in
        autoAcceptDesLab.snp.makeConstraints { make in
            //: make.center.width.equalToSuperview()
            make.center.width.equalToSuperview()
        }
        //: chatBtn.snp.makeConstraints { make in
        chatBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-constBarStr)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.width.equalTo(actualWidth(w: 157))
            make.width.equalTo(actualWidth(w: 157))
            //: make.height.equalTo(actualWidth(w: 34))
            make.height.equalTo(actualWidth(w: 34))
        }
        //: danmuView.snp.makeConstraints { make in
        danmuView.snp.makeConstraints { make in
            //: make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            make.bottom.equalTo(chatBtn.snp.top).offset(-10)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.height.equalTo(VideoMsgTableViewHeight)
            make.height.equalTo(show_detailId)
            //: make.width.equalTo(VideoMsgTableViewWidth)
            make.width.equalTo(show_failVoiceData)
        }
        //: reportBtn.snp.makeConstraints { make in
        reportBtn.snp.makeConstraints { make in
            //: make.trailing.equalToSuperview().offset(-10)
            make.trailing.equalToSuperview().offset(-10)
            //: make.centerY.equalTo(chatBtn)
            make.centerY.equalTo(chatBtn)
            //: make.size.equalTo(actualWidth(w: 33))
            make.size.equalTo(actualWidth(w: 33))
        }
        //: sendGiftBtn.snp.makeConstraints { make in
        sendGiftBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            make.trailing.equalTo(reportBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }
        //: rechargeBtn.snp.makeConstraints { make in
        rechargeBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            make.trailing.equalTo(sendGiftBtn.snp.leading).offset(-10)
            //: make.bottom.equalTo(reportBtn)
            make.bottom.equalTo(reportBtn)
            //: make.size.equalTo(reportBtn)
            make.size.equalTo(reportBtn)
        }

        //: hangupBtn.snp.makeConstraints { make in
        hangupBtn.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
            make.bottom.equalTo(-constBarStr - 161)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }
        //: commInputView.snp.makeConstraints { make in
        commInputView.snp.makeConstraints { make in
            //: make.bottom.equalTo(self)
            make.bottom.equalTo(self)
            //: make.leading.trailing.equalTo(0)
            make.leading.trailing.equalTo(0)
            //: make.height.equalTo(actualWidth(w: 55))
            make.height.equalTo(actualWidth(w: 55))
        }
    }

    /// 更新约束
    //: private func updateLayout() {
    private func pauseZone() {
        //: if chatModel.videoStage == .Recieved {
        if chatModel.videoStage == .Recieved {
            //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2-42-70)
                    make.leading.equalTo(appUseMessage / 2 - 42 - 70)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-constBarStr - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: acceptBtn.snp.remakeConstraints { make in
                acceptBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+42)
                    make.leading.equalTo(appUseMessage / 2 + 42)
                    //: make.bottom.equalTo(hangupBtn)
                    make.bottom.equalTo(hangupBtn)
                    //: make.size.equalTo(hangupBtn)
                    make.size.equalTo(hangupBtn)
                }
                //: } else {
            } else {
                //: hangupBtn.snp.remakeConstraints { make in
                hangupBtn.snp.remakeConstraints { make in
                    //: make.leading.equalTo(34)
                    make.leading.equalTo(34)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                    make.bottom.equalTo(-constBarStr - 161)
                    //: make.size.equalTo(70)
                    make.size.equalTo(70)
                }
                //: femaleAcceptBGBtn.snp.remakeConstraints { make in
                femaleAcceptBGBtn.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-33)
                    make.trailing.equalTo(-33)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-147)
                    make.bottom.equalTo(-constBarStr - 147)
                    //: make.size.equalTo(CGSize.init(width: 208, height: 99))
                    make.size.equalTo(CGSize(width: 208, height: 99))
                }
                //: femaleAcceptBtn.snp.remakeConstraints { make in
                femaleAcceptBtn.snp.remakeConstraints { make in
                    //: make.centerX.equalTo(femaleAcceptBGBtn)
                    make.centerX.equalTo(femaleAcceptBGBtn)
                    //: make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    make.bottom.equalTo(femaleAcceptBGBtn.snp.bottom).offset(-15)
                    //: make.size.equalTo(CGSize.init(width: 180, height: 70))
                    make.size.equalTo(CGSize(width: 180, height: 70))
                }
                //: acceptPlayer.snp.remakeConstraints { make in
                acceptPlayer.snp.remakeConstraints { make in
                    //: make.trailing.equalTo(-8)
                    make.trailing.equalTo(-8)
                    //: make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    make.bottom.equalTo(femaleAcceptBtn.snp.bottom).offset(10)
                    //: make.size.equalTo(85)
                    make.size.equalTo(85)
                }
                //: openCameraBtn.snp.makeConstraints { make in
                openCameraBtn.snp.makeConstraints { make in
                    //: make.leading.equalTo(ScreenWidth/2+12)
                    make.leading.equalTo(appUseMessage / 2 + 12)
                    //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                    make.bottom.equalTo(-constBarStr - 15)
                    //: make.size.equalTo(actualWidth(w: 45))
                    make.size.equalTo(actualWidth(w: 45))
                }
                //: rotateCameraBtn.snp.makeConstraints { make in
                rotateCameraBtn.snp.makeConstraints { make in
                    //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                    //: make.centerY.equalTo(openCameraBtn)
                    make.centerY.equalTo(openCameraBtn)
                    //: make.size.equalTo(openCameraBtn)
                    make.size.equalTo(openCameraBtn)
                }
            }
            //: } else if chatModel.videoStage == .Calling {
        } else if chatModel.videoStage == .Calling {
            //: hangupBtn.snp.remakeConstraints { make in
            hangupBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(rotateCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: openCameraBtn.snp.remakeConstraints { make in
            openCameraBtn.snp.remakeConstraints { make in
                //: make.top.equalTo(StatusBarHeight+actualHeight(h: 25)+actualHeight(h: 170)+26+35+9)
                make.top.equalTo(user_keyId + actualHeight(h: 25) + actualHeight(h: 170) + 26 + 35 + 9)
                //: make.trailing.equalToSuperview().offset(-10)
                make.trailing.equalToSuperview().offset(-10)
                //: make.size.equalTo(actualWidth(w: 33))
                make.size.equalTo(actualWidth(w: 33))
            }
            //: rotateCameraBtn.snp.remakeConstraints { make in
            rotateCameraBtn.snp.remakeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-10)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
            //: } else {
        } else {
            //: hangupBtn.snp.makeConstraints { make in
            hangupBtn.snp.makeConstraints { make in
                //: make.centerX.equalToSuperview()
                make.centerX.equalToSuperview()
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-161)
                make.bottom.equalTo(-constBarStr - 161)
                //: make.size.equalTo(actualWidth(w: 70))
                make.size.equalTo(actualWidth(w: 70))
            }
            //: openCameraBtn.snp.makeConstraints { make in
            openCameraBtn.snp.makeConstraints { make in
                //: make.leading.equalTo(ScreenWidth/2+12)
                make.leading.equalTo(appUseMessage / 2 + 12)
                //: make.bottom.equalTo(-kDeviceSafeBottomHeight-15)
                make.bottom.equalTo(-constBarStr - 15)
                //: make.size.equalTo(actualWidth(w: 45))
                make.size.equalTo(actualWidth(w: 45))
            }
            //: rotateCameraBtn.snp.makeConstraints { make in
            rotateCameraBtn.snp.makeConstraints { make in
                //: make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                make.trailing.equalTo(openCameraBtn.snp.leading).offset(-25)
                //: make.centerY.equalTo(openCameraBtn)
                make.centerY.equalTo(openCameraBtn)
                //: make.size.equalTo(openCameraBtn)
                make.size.equalTo(openCameraBtn)
            }
        }
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func cohereSumeractionInteraction() {
        // 视频等待...动画
        //: waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
        waitTimer = Timer.scheduledTimer(withTimeInterval: 0.5, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard var text = self.desLab.text else { return }
            guard var text = self.desLab.text else { return }

            //: if text.hasSuffix("...") {
            if text.hasSuffix("...") {
                //: text = text.replacingOccurrences(of: "...", with: ".")
                text = text.replacingOccurrences(of: "...", with: ".")
                //: } else if text.hasSuffix("..") {
            } else if text.hasSuffix("..") {
                //: text = text.replacingOccurrences(of: "..", with: "...")
                text = text.replacingOccurrences(of: "..", with: "...")
                //: } else {
            } else {
                //: text = text.replacingOccurrences(of: ".", with: "..")
                text = text.replacingOccurrences(of: ".", with: "..")
            }
            //: self.desLab.text = text
            self.desLab.text = text

            //: }, repeats: true)
        }, repeats: true)
        //: waitTimer?.fire()
        waitTimer?.fire()
    }

    //: private func setFreeView() {
    private func outOfSchool() {
        //: addSubview(freeTimeView)
        addSubview(freeTimeView)
        //: freeTimeView.snp.remakeConstraints { make in
        freeTimeView.snp.remakeConstraints { make in
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            make.top.equalTo(timeLab.snp.bottom).offset(actualHeight(h: 299))
            //: make.height.equalTo(111)
            make.height.equalTo(111)
            //: make.width.equalTo(126)
            make.width.equalTo(126)
        }
    }

    /// 添加通知
    //: private func initNotificationObserver() {
    private func exceptFace() {
        // 女性用户 && 使用商汤 && 人脸检测自动接听
        //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue &&
        if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.female.rawValue,
           //: SenseTime_Use == true &&
           userLimitText == true,
           //: StatisticalTableReactiveCompatible.share.appConfigMode.autoAnswer == 1 {
           StatisticalTableReactiveCompatible.share.appConfigMode.autoAnswer == 1
        {
            /// 监听 是否捕获到人脸
            //: NotificationCenter.default.addObserver(self,
            NotificationCenter.default.addObserver(self,
                                                   //: selector: #selector(catchFaceResultNotification(notification:)),
                                                   selector: #selector(invisible(notification:)),
                                                   //: name: NSNotification.Name(rawValue: "FaceActionEvent_Interval_12_frame"),
                                                   name: NSNotification.Name(rawValue: String(bytes: user_diskKey.map{toHidden(name: $0)}, encoding: .utf8)!),
                                                   //: object: nil)
                                                   object: nil)
        }

        //: NotificationCenter.default.addObserver(self, selector: #selector(changeRechargeStatus), name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(carryEnableAll), name: app_exploreBarContent, object: nil)
    }
}
