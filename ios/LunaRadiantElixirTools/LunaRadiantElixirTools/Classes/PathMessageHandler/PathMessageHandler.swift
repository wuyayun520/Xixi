
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userEffectTitle:[UInt8] = [0xdc,0xdb,0xdc,0xc1,0x9d,0xd6,0xda,0xd1,0xd0,0xc7,0x8f,0x9c,0x95,0xdd,0xd4,0xc6,0x95,0xdb,0xda,0xc1,0x95,0xd7,0xd0,0xd0,0xdb,0x95,0xdc,0xd8,0xc5,0xd9,0xd0,0xd8,0xd0,0xdb,0xc1,0xd0,0xd1]

private func viewTranslateGift(data num: UInt8) -> UInt8 {
    return num ^ 181
}

/*: "Load..." :*/
fileprivate let const_makeUrl:[Character] = ["L","o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let constWithStatusViewKey:String = "text model self self returnSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let notiTitleValue:[UInt8] = [0xac,0x90,0x90,0x94,0xb0,0x8b,0x8b,0x88,0xca,0xaa,0x85,0x90,0x8d,0x92,0x81,0xb0,0x8b,0xae,0x97,0xcc,0xc3,0x96,0x81,0x87,0x8c,0x85,0x96,0x83,0x81,0xc3,0xcd]

private func filterPlayer(target num: UInt8) -> UInt8 {
    return num ^ 228
}

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let appQuoteId:[UInt8] = [0xbd,0xa3,0xa4,0xae,0xa5,0xbd,0xe4,0xa5,0xa4,0x9a,0xab,0xad,0xaf,0x99,0xa2,0xa5,0xbd,0xec,0xec,0xa5,0xa4,0x9a,0xab,0xad,0xaf,0x99,0xa2,0xa5,0xbd,0xe2,0xe3,0xf1]

/*: "jsEvent(onPageShow):  :*/
fileprivate let appExternalKey:String = "jsEvenmode self"
fileprivate let const_overName:String = "geSholet make in cell model"

/*: --- :*/
fileprivate let constBackgroundName:String = "frameframeframe"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let notiGameUrl:[UInt8] = [0x1e,0x0,0x7,0xd,0x6,0x1e,0x47,0x6,0x7,0x39,0x8,0xe,0xc,0x21,0x0,0xd,0xc,0x4f,0x4f,0x6,0x7,0x39,0x8,0xe,0xc,0x21,0x0,0xd,0xc,0x41,0x40,0x52]

/*: "jsEvent(onPageHide):  :*/
fileprivate let show_translateStr:[Character] = ["j","s","E","v","e","n","t","("]
fileprivate let const_modeTitle:[Character] = ["o","n","P","a","g","e"]
fileprivate let showMagnitudeMessage:[Character] = ["H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let app_clickSizeFormat:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let main_titleMsg:String = "canGoBaguard path lab top"
fileprivate let showMakeData:String = "infok"

/*: "PHPSESSID= :*/
fileprivate let dataViewByLabelKey:String = "phpsessi"
fileprivate let const_succeedFrameName:String = "layer mode main text equalD="

/*: ";uid= :*/
fileprivate let showTempText:String = ";uid=to string status else as"

/*: "Cookie" :*/
fileprivate let user_collectionLineUrl:[Character] = ["C","o","o","k","i","e"]

/*: "nav_back_black_nor" :*/
fileprivate let const_valueText:String = "var render center past addnav_"
fileprivate let const_normalPath:String = "LACK"
fileprivate let mainTableName:String = "phone status user main message_nor"

/*: "ico_intimate_list_sm" :*/
fileprivate let k_namePath:String = "ico_ifill model to"
fileprivate let mainColorMakeTitle:String = "ate_lisequal path"
fileprivate let showModelUserMsg:String = "follow somethingt_sm"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let data_intervalValue:[UInt8] = [0x3b,0x30,0x3c,0x2a,0x32,0x3a,0x31,0x2b,0x71,0x38,0x3a,0x2b,0x1a,0x33,0x3a,0x32,0x3a,0x31,0x2b,0x2c,0x1d,0x26,0xb,0x3e,0x38,0x11,0x3e,0x32,0x3a,0x77,0x78,0x3d,0x30,0x3b,0x26,0x78,0x76,0x4,0x6f,0x2,0x71,0x2c,0x2b,0x26,0x33,0x3a,0x71,0x3d,0x3e,0x3c,0x34,0x38,0x2d,0x30,0x2a,0x31,0x3b,0x62,0x78,0x2d,0x38,0x3d,0x3e,0x77,0x6f,0x73,0x6f,0x73,0x6f,0x73,0x6f,0x76,0x78]

private func backgroundTool(model num: UInt8) -> UInt8 {
    return num ^ 95
}

/*: "Alert" :*/
fileprivate let const_countName:String = "Alertreturn to in"

/*: "OK" :*/
fileprivate let appIndexStr:String = "Oabout"

/*: "Cancle" :*/
fileprivate let user_hairStr:String = "Canclenormal model"

/*: "Done" :*/
fileprivate let app_beautyFormat:String = "string cookie trueDone"

/*: "js call method name =  :*/
fileprivate let userBeginFormat:String = "view if main resultjs ca"
fileprivate let constImageTitle:String = "thod ninput rank as to"
fileprivate let show_infoDeviceKey:String = "ame = share user"

/*: , message =  :*/
fileprivate let k_normalName:String = ", meview bar gift hidden false"
fileprivate let showRawContent:String = " app "

/*: "http://" :*/
fileprivate let appMediumMessage:String = "self ifhttp://"

/*: "https://" :*/
fileprivate let data_pointKey:String = "https:scale normal equal"
fileprivate let showLabId:[Character] = ["/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let notiStatusPath:[UInt8] = [0x68,0x7f,0x6c,0x3e,0x7d,0x71,0x71,0x75,0x77,0x7b,0x50,0x7f,0x73,0x7b,0x6d,0x3e,0x23,0x3e,0x7a,0x71,0x7d,0x6b,0x73,0x7b,0x70,0x6a,0x30,0x7d,0x71,0x71,0x75,0x77,0x7b,0x30,0x6d,0x6e,0x72,0x77,0x6a,0x36,0x39,0x25,0x3e,0x39,0x37,0x30,0x73,0x7f,0x6e,0x36,0x78,0x6b,0x70,0x7d,0x6a,0x77,0x71,0x70,0x36,0x7d,0x71,0x71,0x75,0x77,0x7b,0x37,0x3e,0x65,0x3e,0x6c,0x7b,0x6a,0x6b,0x6c,0x70,0x3e,0x7d,0x71,0x71,0x75,0x77,0x7b,0x30,0x6d,0x6e,0x72,0x77,0x6a,0x36,0x39,0x23,0x39,0x37,0x45,0x2e,0x43,0x3e,0x63,0x3e,0x37,0x25,0x14]

private func halfButtonFrank(manager num: UInt8) -> UInt8 {
    return num ^ 30
}

/*: "uid= :*/
fileprivate let k_pathUrl:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let app_touchFormat:[Character] = [";","d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let const_contentSucceedTitle:String = ";pcorner if equal for edit"
fileprivate let user_bagValue:[Character] = ["a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let main_modelPageMessage:[Character] = ["d","o","c","u","m"]
fileprivate let user_transactionName:String = "entitle"
fileprivate let show_sexAfterData:String = "self model view cookie.coo"

/*: ';\n" :*/
fileprivate let showThenName:[Character] = ["\'",";","\n"]

/*: ;domin= :*/
fileprivate let k_managerFromStr:String = ";domincase title self button add"
fileprivate let notiWithData:[Character] = ["="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let notiLineFormat:[UInt8] = [0xf2,0xfd,0xbb,0xb3,0xf8,0xf4,0xf4,0xf0,0xf2,0xfe,0xd5,0xfa,0xf6,0xfe,0xe8,0xb5,0xf2,0xf5,0xff,0xfe,0xe3,0xd4,0xfd,0xb3,0xbc,0xcb,0xd3,0xcb,0xc8,0xde,0xc8,0xc8,0xd2,0xdf,0xbc,0xb2,0xbb,0xa6,0xa6,0xbb,0xb6,0xaa,0xb2,0xbb,0xe0,0xbb,0xff,0xf4,0xf8,0xee,0xf6,0xfe,0xf5,0xef,0xb5,0xf8,0xf4,0xf4,0xf0,0xf2,0xfe,0xa6,0xbc]

private func listMake(add num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: '; };\n" :*/
fileprivate let userAboutMessage:[Character] = ["\'",";"," ","}",";","\n"]

/*: '" :*/
fileprivate let showSizeName:String = "\'"

/*: "h5Domain =  :*/
fileprivate let noti_giftNarrowAtStr:[Character] = ["h","5","D","o","m","a","i","n"," ","="," "]

/*: "getAllCookies =  :*/
fileprivate let notiKindIconMessage:[Character] = ["g","e","t","A","l","l","C","o"]
fileprivate let data_clickPath:String = "commit if count modelokies = "

/*: "PHPSESSID" :*/
fileprivate let k_areaUrl:String = "PHPScontent"
fileprivate let appVoiceBindName:[Character] = ["S","I","D"]

/*: "uid" :*/
fileprivate let const_pageKey:String = "udata"

/*: "appCookies =  :*/
fileprivate let constIndexCenterFormat:String = "voice any pageappCooki"
fileprivate let dataObjectKey:String = "index keyes = "

/*: "UID" :*/
fileprivate let constToData:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let user_angleFormat:String = "npartyig"
fileprivate let k_viewPath:[Character] = ["a","t","o","r"]
fileprivate let showMapSizeValue:[Character] = [".","u","s","e","r","A","g","e","n","t"]

/*: "xunwu_iphone" :*/
fileprivate let data_appKey:String = "xlistwu"
fileprivate let k_methodStr:[Character] = ["_","i","p","h","o","n","e"]

/*: "  :*/
fileprivate let app_bottomAddMsg:String = "manager"

/*: " xunwu_device_id/ :*/
fileprivate let showReasonMsg:String = " xunwview start filter color"
fileprivate let user_indexValue:String = "vice_center collection name fee"
fileprivate let user_handleData:String = "id/container moment section tool quote"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let app_infoViewTitle:[Character] = ["x","u","n","w","u","_","i","p","h","o","n"]
fileprivate let userImageTitle:String = "kit input error selfe xunwu"
fileprivate let show_transformFormat:[Character] = ["_","v","e","r","s","i","o","n","/"]

/*:  xunwu_package/ :*/
fileprivate let mainObserverUrl:String = "item black xunwu"
fileprivate let mainPhotoFormat:String = "age/hidden file center"

/*:  xunwu_bundleId/ :*/
fileprivate let data_sizeMessage:[Character] = [" ","x","u","n","w","u","_","b","u","n"]
fileprivate let app_appearName:[Character] = ["d","l","e","I","d","/"]

/*:   webkit/1" :*/
fileprivate let user_textShowTitle:String = "  webtitle view"
fileprivate let app_colorViewFormat:String = "event message text pathkit/1"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PathMessageHandler.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class PathMessageHandler: PopViewController {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: ExitConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: StemBasicType?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: StemBasicType?, webConfig: ExitConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userEffectTitle.map{viewTranslateGift(data: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        uncolored()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        indexRecord()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(const_makeUrl))
        //: addNotification()
        overdoClick()
        //: func__checkConfig()
        iconApp()
        //: func__setUpWebView()
        isoclinalView()
        //: func__registerObserver()
        toModeObserver()
        //: func__beginStartRequest()
        requestFrom()
        //: func_checkAppleIAP()
        funcScope()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: app_exploreBarContent, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            allConfig(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.readModeAchromaticColour(), .font: UIFont.colorTitle(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(constWithStatusViewKey.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension PathMessageHandler {
    /// 添加通知
    //: private func addNotification() {
    private func overdoClick() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(margin),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: userAppName,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(uncolored),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func margin() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiTitleValue.map{filterPlayer(target: $0)}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func uncolored() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: appQuoteId.map{$0^202}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(appExternalKey.prefix(6)) + "t(onPa" + String(const_overName.prefix(5)) + "w): ") + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func indexRecord() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiGameUrl.map{$0^105}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(show_translateStr) + String(const_modeTitle) + String(showMagnitudeMessage)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension PathMessageHandler {
    //: func func__registerObserver() {
    func toModeObserver() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(app_clickSizeFormat)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(main_titleMsg.prefix(7)) + showMakeData.replacingOccurrences(of: "info", with: "c")))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(ofDiscounts), name: data_intervalId, object: nil)
    }

    //: func func__setUpWebView() {
    func isoclinalView() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        tidings()
        //: func__addBridgeMethod()
        addMethod()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.messageText()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            BeanNameProgressHUD.betweenEqualClick(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(user_keyId)
            }
        }
    }

    //: func func__beginStartRequest() {
    func requestFrom() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.chemistryLab()
            }
            //: } else {
        } else {
            //: func__startRequest()
            chemistryLab()
        }
    }

    //: func func__startRequest() {
    func chemistryLab() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = StatisticalTableReactiveCompatible.share.loginUid
        let uid = StatisticalTableReactiveCompatible.share.loginUid
        //: let ssid = StatisticalTableReactiveCompatible.share.loginSessionId
        let ssid = StatisticalTableReactiveCompatible.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (dataViewByLabelKey.uppercased() + String(const_succeedFrameName.suffix(2))) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(showTempText.prefix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(user_collectionLineUrl)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        pending()
        //: func__initRightButton()
        observerFormat()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func funcScope() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            ManagingDirectorTransactionObserver.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.afterType(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            ManagingDirectorTransactionObserver.shared.requestSection()
        }
    }

    //: func func__initLeftButton() {
    func lineament() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.barName(name: (String(const_valueText.suffix(4)) + "back_b" + const_normalPath.lowercased() + String(mainTableName.suffix(4)))), style: .plain, target: self, action: #selector(addSender(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func observerFormat() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(StemBasicType.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.barName(name: (String(k_namePath.prefix(5)) + "ntim" + String(mainColorMakeTitle.prefix(7)) + String(showModelUserMsg.suffix(4)))), style: .plain, target: self, action: #selector(imageAction))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func addSender(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        routeTo()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.currentViewController() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: PathMessageHandler.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! PathMessageHandler).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! PathMessageHandler).uncolored()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func iconApp() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func modeEnable(sucess: Bool) {
        //: ProgressHUD.dismiss()
        BeanNameProgressHUD.doingBlock()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func pending() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: data_intervalValue.map{backgroundTool(model: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func followPing(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: EditPushManager.share.func__pushToWebVC(urlStr: url)
            EditPushManager.share.methodConfig(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func sizeGesture(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func ofDiscounts() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: data_intervalId, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func imageAction() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = LeadingUpView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName))
        //: view.show()
        view.viewTo()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension PathMessageHandler: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            BeanNameProgressHUD.betweenEqualClick(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            modeEnable(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        lineament()
        //: func__clearJSBgColor()
        pending()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            modeEnable(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            modeEnable(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(const_countName.prefix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(user_hairStr.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(app_beautyFormat.suffix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension PathMessageHandler: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(userBeginFormat.suffix(5)) + "ll me" + String(constImageTitle.prefix(6)) + String(show_infoDeviceKey.prefix(6))) + "\(message.name)" + (String(k_normalName.prefix(4)) + "ssage" + showRawContent.replacingOccurrences(of: "app", with: "=")) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.libretto(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func addMethod() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in FileMirrorPath.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func routeTo() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in FileMirrorPath.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension PathMessageHandler {
    //: func func__addUserScript() {
    func popularEffectRange() {
        //: var htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(appMediumMessage.suffix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(appMediumMessage.suffix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(data_pointKey.prefix(6)) + String(showLabId))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(data_pointKey.prefix(6)) + String(showLabId)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = StatisticalTableReactiveCompatible.share.loginUserMode.userID
        let tempUid: String = StatisticalTableReactiveCompatible.share.loginUserMode.userID
        //: let tempSessionID: String = StatisticalTableReactiveCompatible.share.loginSessionId
        let tempSessionID: String = StatisticalTableReactiveCompatible.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: notiStatusPath.map{halfButtonFrank(manager: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_pathUrl)) + "\(tempUid)" + (String(app_touchFormat)) + "\(htmlDomain)" + (String(const_contentSucceedTitle.prefix(2)) + String(user_bagValue))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(main_modelPageMessage) + user_transactionName.replacingOccurrences(of: "title", with: "t") + String(show_sexAfterData.suffix(4)) + "kie=\'") + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (dataViewByLabelKey.uppercased() + String(const_succeedFrameName.suffix(2))) + "\(tempSessionID)" + (String(k_managerFromStr.prefix(6)) + String(notiWithData)) + "\(htmlDomain)" + (String(const_contentSucceedTitle.prefix(2)) + String(user_bagValue))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(main_modelPageMessage) + user_transactionName.replacingOccurrences(of: "title", with: "t") + String(show_sexAfterData.suffix(4)) + "kie=\'") + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: notiLineFormat.map{listMake(add: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_pathUrl)) + "\(tempUid)" + (String(app_touchFormat)) + "\(htmlDomain)" + (String(const_contentSucceedTitle.prefix(2)) + String(user_bagValue))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(main_modelPageMessage) + user_transactionName.replacingOccurrences(of: "title", with: "t") + String(show_sexAfterData.suffix(4)) + "kie=\'") + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(k_pathUrl)) + "\(tempSessionID)" + (String(app_touchFormat)) + "\(htmlDomain)" + (String(const_contentSucceedTitle.prefix(2)) + String(user_bagValue))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(main_modelPageMessage) + user_transactionName.replacingOccurrences(of: "title", with: "t") + String(show_sexAfterData.suffix(4)) + "kie=\'") + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func allConfig(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
            let htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(noti_giftNarrowAtStr)) + "\(htmlDomain)")
            //: let userID: String = StatisticalTableReactiveCompatible.share.loginUserMode.userID
            let userID: String = StatisticalTableReactiveCompatible.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(notiKindIconMessage) + String(data_clickPath.suffix(8))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (k_areaUrl.replacingOccurrences(of: "content", with: "ES") + String(appVoiceBindName)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (const_pageKey.replacingOccurrences(of: "data", with: "id")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.requestFrom()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.controlPlayerSession(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func controlPlayerSession(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
                let htmlDomain: String = StatisticalTableReactiveCompatible.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(noti_giftNarrowAtStr)) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: OffPromiseAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(constIndexCenterFormat.suffix(8)) + String(dataObjectKey.suffix(5))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(constToData)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (const_pageKey.replacingOccurrences(of: "data", with: "id"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (k_areaUrl.replacingOccurrences(of: "content", with: "ES") + String(appVoiceBindName)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (k_areaUrl.replacingOccurrences(of: "content", with: "ES") + String(appVoiceBindName))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.requestFrom()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func tidings() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((user_angleFormat.replacingOccurrences(of: "party", with: "av") + String(k_viewPath) + String(showMapSizeValue))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (user_angleFormat.replacingOccurrences(of: "party", with: "av") + String(k_viewPath) + String(showMapSizeValue)))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((data_appKey.replacingOccurrences(of: "list", with: "un") + String(k_methodStr))) {
                //: let str = getUserAgentString()
                let str = listName()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.scopeLocal { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(showReasonMsg.prefix(5)) + "u_de" + String(user_indexValue.prefix(5)) + String(user_handleData.prefix(3))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func listName() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(app_infoViewTitle) + String(userImageTitle.suffix(7)) + String(show_transformFormat)) + "\(constClickUrl)" + (String(mainObserverUrl.suffix(6)) + "_pack" + String(mainPhotoFormat.prefix(4))) + "\(showNoName)" + (String(data_sizeMessage) + String(app_appearName)) + "\(showConnectName)" + (String(user_textShowTitle.prefix(5)) + String(app_colorViewFormat.suffix(5)))
    }
}
