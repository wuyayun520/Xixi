
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let data_giftStr:[Character] = ["S","O","C","K","E","T","_","A","D","D","R","_","K"]
fileprivate let noti_rawKey:[Character] = ["E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let user_sizeMessage:[UInt8] = [0xa4,0xb8,0xb4,0xbc,0xb2,0xa3,0xa8,0xa1,0xbe,0xb3,0xb2,0xb8,0xb4,0xbf,0xb6,0xa3,0xa8,0xa5,0xb2,0xa6,0xa2,0xb2,0xa4,0xa3,0xb4,0xb6,0xbb,0xbb,0xa8,0xb9,0xb8,0xa3,0xbe,0xb1,0xbe,0xb4,0xb6,0xa3,0xbe,0xb8,0xb9]

private func bindValid(party num: UInt8) -> UInt8 {
    return num ^ 247
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let app_valueTextKey:[UInt8] = [0xa6,0xb1,0xb7,0xbd,0xa2,0xb1,0xab,0xb2,0xb8,0xbb,0xb5,0xa0,0xa7,0xb7,0xa6,0xb1,0xb1,0xba,0xab,0xba,0xbb,0xa0,0xbd,0xb2,0xbd,0xb7,0xb5,0xa0,0xbd,0xbb,0xba]

/*: "isCalling" :*/
fileprivate let constTitleFormat:String = "I"
fileprivate let const_userName:String = "color view gift forsCalling"
 
/*: "SOCKET 连接成功." :*/
fileprivate let noti_modelFormat:[Character] = ["S","O","C"]
fileprivate let dataTitleName:[Character] = ["K","E","T"," ","连","接","成","功","."]

/*: "SOCKET 连接失败, error: :*/
fileprivate let constPathData:String = "true data state colorSOCKET "
fileprivate let userItemDataTextName:String = "连mode败, "

/*: ." :*/
fileprivate let const_makeLabelValue:[Character] = ["."]

/*: "SOCKET 失败 :*/
fileprivate let constTargetText:[Character] = ["S","O","C","K","E","T"]
fileprivate let constToName:[Character] = [" ","失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let data_signKey:[Character] = ["次",","," ","切","\u{6362}","过","S","o","c"]
fileprivate let k_valueToolContent:String = "land time all ifket域"
fileprivate let appAssistantMsg:String = "名range尝"

/*: "SOCKET 已关闭, code: :*/
fileprivate let constViewFormat:[Character] = ["S","O","C","K","E","T"," ","已","\u{5173}","闭",","]
fileprivate let userPhoneSucceedGroupMsg:[Character] = [" ","c","o","d","e",":"]

/*: , reason: :*/
fileprivate let showSearchModelPath:String = ", reaname wait"

/*: "ws:// :*/
fileprivate let show_equalTableId:String = "add count mainws://"

/*: /?sid= :*/
fileprivate let user_cellStr:String = "send error/?sid="

/*: &uid= :*/
fileprivate let user_valueLabelStr:String = "view cap&uid="

/*: &platform=iphone&version= :*/
fileprivate let app_mainName:[Character] = ["&","p","l","a","t","f","o","r","m","=","i","p","h","o","n","e"]
fileprivate let kMakeId:String = "app to bank more&version="

/*: &packageId= :*/
fileprivate let main_dataStr:[Character] = ["&","p","a"]
fileprivate let notiResultUserKey:String = "ckageId=view print gesture app"

/*: "1v1server. :*/
fileprivate let constCloudSourcePath:String = "1v1serveintimate let var name"
fileprivate let data_succeedCountName:String = "play request model model varr."

/*: .com:7766" :*/
fileprivate let constLeadingStr:[Character] = ["."]
fileprivate let kBackFormat:String = "com:7766add window"

/*: "43.129.185.194:7767" :*/
fileprivate let show_cellRequestId:[Character] = ["4","3",".","1","2","9",".","1","8","5",".","1","9","4",":","7","7","6"]
fileprivate let constOfValue:[Character] = ["7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let dataVideoFormat:[UInt8] = [0x2,0xfe,0xf2,0xfa,0xf4,0x3,0xcf,0x96,0x6c,0x40,0x96,0x6a,0x4b,0x21,0x14,0x10,0x12,0x17,0x10,0x11,0x18,0x1b,0x18,0x23,0x28,0xf2,0x17,0x10,0x1d,0x16,0x14,0x13,0x96,0x4a,0x40,0x94,0x3f,0x5b,0x9e,0x6b,0x3b,0x95,0x45,0x5c,0x96,0x6c,0x40,0x94,0x34,0x62,0x98,0x46,0x5c,0xdd]

fileprivate func equalModel(layer num: UInt8) -> UInt8 {
    let value = Int(num) - 175
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cmd" :*/
fileprivate let noti_viewMsg:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let dataTitlePath:[Character] = ["p","i","n","g"]

/*: "callHeartbeat" :*/
fileprivate let constCellTitle:String = "callHfalse label section move"
fileprivate let k_defineTitle:String = "eareffecteat"

/*: "recievedDate" :*/
fileprivate let noti_tabValue:[Character] = ["r","e","c","i","e","v","e","d"]
fileprivate let noti_contentTopId:String = "Datebackground center view format top"

/*: "liveHeartBeat" :*/
fileprivate let userViewAddUrl:String = "liveHealabel label"
fileprivate let k_modelStr:String = "send pushrtBeat"

/*: "🔗 关闭 socket" :*/
fileprivate let showThroughStr:[Character] = ["\u{0001f517}"," ","关","闭"," ","s","o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let notiMakePath:String = "The ufatal from self gift equal"
fileprivate let dataAppPlayTitle:String = "ogs outrecord let color add layer"

/*: "SOCKET  :*/
fileprivate let const_sectionMsg:String = "will varSOCKET "

/*: 重连. 重试次数： :*/
fileprivate let appToText:String = "重连. 重\u{8bd5}次数："

/*: , 地址： :*/
fileprivate let constAddModelTitle:String = ", 地址："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let const_rangeMsg:[Character] = ["S","O","C","K","E","T"," ","连","\u{63a5}","正","常"," ","\u{4e0d}","\u{9700}","要","重","\u{65b0}","连"]
fileprivate let k_textViewUrl:String = "info."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let showBottomMsg:String = "SOCKET 正guard self"
fileprivate let data_contentName:String = "add连接 不"

/*: "&resume=1" :*/
fileprivate let mainLabTitle:String = "top image let more&resu"

/*: "&lang= :*/
fileprivate let kViewText:String = "from let sex&lang="

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let data_videoName:String = "rangeCKET"
fileprivate let show_cancelMessage:String = " \u{5f00}始连接"
fileprivate let k_modeText:[Character] = [","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let appManagerText:String = "across open error view constraintonNe"
fileprivate let data_byNorId:String = "aiconled"

/*: "errno" :*/
fileprivate let dataTablePath:[Character] = ["e","r","r","n","o"]

/*: "data" :*/
fileprivate let user_viewFormat:String = "DATA"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let app_labFormat:String = "SOCKEcount"
fileprivate let noti_nameTimeTitle:String = "asset languagecmd:"
fileprivate let notiPhoneData:String = "to background self ping."

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let kBlindValue:[UInt8] = [0x20,0x3c,0x30,0x38,0x36,0x27,0x53,0x7,0x1,0x7,0x10,0x97,0xcc,0xee,0x95,0xc7,0xc8,0x10,0x1e,0x17,0x49,0x53,0x10,0x12,0x1f,0x1f,0x3b,0x16,0x12,0x1,0x7,0x11,0x16,0x12,0x7,0x5d]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let main_managerShowMsg:[UInt8] = [0x2e,0x74,0x61,0x65,0x42,0x74,0x72,0x61,0x65,0x48,0x65,0x76,0x69,0x6c,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x65,0x76,0x69,0x6c,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "onP" :*/
fileprivate let k_leadingTitle:String = "equal labonP"

/*: "msg" :*/
fileprivate let constMaleUrl:String = "memptyg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let kShadowKey:[Character] = ["🔗"," ","处","理","消","息","：","c","m","d","N","a","m"]
fileprivate let dataScriptCollectionId:String = "size model top typee:"

/*: , errNo: :*/
fileprivate let data_norPath:[Character] = [","," ","e","r","r","N","o",":"]

/*: , msg: :*/
fileprivate let const_dataBeanStr:String = "press not bind view make, msg:"

/*: , data: :*/
fileprivate let userSexIconKeyName:String = ", "
fileprivate let constEqualFormat:String = "data:icon gift height"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let notiSubPath:String = "SOCKshare"
fileprivate let appTopKey:String = "息：cmdNself mete make of"
fileprivate let kCountMessage:String = "video model sizeame:"

/*: "onReceivedGift" :*/
fileprivate let userLabId:[Character] = ["o","n","R","e","c","e","i","v","e","d","G","i","f","t"]

/*: "onVideoMatch" :*/
fileprivate let appCellGoldStr:[Character] = ["o","n","V","i","d","e"]
fileprivate let constTaskId:String = "count"
fileprivate let notiTitleId:[Character] = ["M","a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let main_beginId:[Character] = ["o","n","R","e","q","u","e","s","t","C"]
fileprivate let appEnableContent:String = "alive"

/*: "onCancelCall" :*/
fileprivate let showOpenModelFalseFormat:String = "touch modelonCan"

/*: "onRefuseCall" :*/
fileprivate let constImageName:String = "add infoonRefu"
fileprivate let mainRowRequestUrl:[Character] = ["l"]

/*: "onAcceptCall" :*/
fileprivate let userDataMessage:[Character] = ["o","n","A","c","c","e","p"]
fileprivate let const_viewStr:[Character] = ["t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let notiStatusMessage:[Character] = ["o","n","S","t","o","p","C","a"]
fileprivate let const_dropPlayerKey:String = "toto"

/*: "freeCallTimes" :*/
fileprivate let main_titleData:String = "freeCsource share style status he"
fileprivate let mainTapValue:[Character] = ["s"]

/*: "mfCoin" :*/
fileprivate let showChangeMessage:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let kLayerId:String = "onMissCelse object image layer layer"
fileprivate let data_topUrl:String = "textin"

/*: "acceptCall" :*/
fileprivate let const_buttonData:String = "apointpointe"
fileprivate let appLayerPath:String = "have"

/*: "stopCall" :*/
fileprivate let app_tabStr:String = "stopCaname any list transform position"
fileprivate let k_shareKey:[Character] = ["l","l"]

/*: "requestCall" :*/
fileprivate let notiLineGalleryInText:String = "colorques"

/*: "[Invite you to a video call...]" :*/
fileprivate let showRequestMessage:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "type" :*/
fileprivate let noti_valueViewPath:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let data_messageKey:[UInt8] = [0x43,0x31,0x56,0x5e,0x51,0x5c,0x4d,0x8,0x61,0x57,0x5d,0x8,0x5c,0x57,0x8,0x49,0x8,0x5e,0x57,0x51,0x4b,0x4d,0x8,0x4b,0x49,0x54,0x54,0x16,0x16,0x16,0x45]

fileprivate func magnitudeAction(voice num: UInt8) -> UInt8 {
    let value = Int(num) - 232
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let main_digitalMessage:String = "infoid"

/*: "nickname" :*/
fileprivate let k_colorStr:String = "nblock"
fileprivate let kRemoveToData:[Character] = ["c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let user_windowData:String = "headPicvar change true content"

/*: "onCallResume" :*/
fileprivate let noti_modelName:String = "extension main model foronCall"
fileprivate let dataModeMessage:[Character] = ["m","e"]

/*: "cancelCall" :*/
fileprivate let const_phonePath:String = "toantoe"
fileprivate let mainSizeId:[Character] = ["l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let userViewModelTitle:String = "value m if rate sharerefuseC"
fileprivate let main_frameMsg:[Character] = ["a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let show_giftNameData:String = "onCalmodel event of end height"
fileprivate let notiColorTitle:String = "anormalion"

/*: "onLiveWarning" :*/
fileprivate let kAtData:String = "in let pointonLi"

/*: "reason" :*/
fileprivate let show_touchManagerFormat:[Character] = ["r"]
fileprivate let appSectionTitle:String = "easkeyn"

/*: "time" :*/
fileprivate let kFirstShrinkFormat:String = "titexte"

/*: "onStopLive" :*/
fileprivate let userInsideMsg:[Character] = ["o","n","S","t","o","p","L","i","v"]
fileprivate let showEqualId:String = "agent"

/*: "onSendProgramGift" :*/
fileprivate let constBlindId:String = "onSevertical else class string model"
fileprivate let dataMainName:String = "ramGiftas item all let content"

/*: "onGameFs" :*/
fileprivate let showFinishMessage:[Character] = ["o","n","G","a","m"]
fileprivate let notiSucceedId:String = "eFslab self birthday all object"

/*: "broadStatus" :*/
fileprivate let notiBottomId:[Character] = ["b","r","o","a","d","S"]
fileprivate let appCancelFormat:[Character] = ["t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let mainByName:String = "ad item error typeonCal"
fileprivate let constCountValue:[Character] = ["l","C","h","a","t","M","s","g"]

/*: "onNotificationPopupWin" :*/
fileprivate let main_topData:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i","o","n","P","o","p"]
fileprivate let kIconInputKey:[Character] = ["u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let kTableValue:[Character] = ["o","n","U","s"]
fileprivate let dataInfoName:String = "erBlockview lab true"

/*: "freeCallWin" :*/
fileprivate let user_useStr:[Character] = ["f","r","e","e","C","a","l"]
fileprivate let k_centerMakeValue:String = "lWinlayer var name"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MakeSocketDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let const_tagFormat = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let mainPlayFormat = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let constStoragePath = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let kKeyId = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let showStatusStr = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let const_intervalMessage = (String(data_giftStr) + String(noti_rawKey))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let notiTimeId = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let dataSendViewMessage = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let k_imageMessage = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let userNameMessage = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let showTagScreenMsg = NSNotification.Name(rawValue: String(bytes: user_sizeMessage.map{bindValid(party: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let k_giftFormat = NSNotification.Name(rawValue: String(bytes: app_valueTextKey.map{$0^244}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol PopObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func toCurrent(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol TableObjectProtocol: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func isVideo(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func christenMakeSituation(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol TitleObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func lineUpRemove(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func socketOrCollection(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func addDown(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func commit(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol BeanObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func reject(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func viewObject(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func managerData(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class MakeSocketDelegate: NSObject {
    //: enum SocketRetryType: String {
    enum DateType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currInfoVC: AnyObject?
    var currInfoVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CapacityLanguageManager.shared.direction == .rightToLeft ? 0 : appUseMessage,
        //: y: StatusBarHeight+190
        y: user_keyId + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CapacityLanguageManager.shared.direction == .rightToLeft ? 0 : appUseMessage,
        //: y: StatusBarHeight+190
        y: user_keyId + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CapacityLanguageManager.shared.direction == .rightToLeft ? 0 : appUseMessage,
        //: y: StatusBarHeight+500
        y: user_keyId + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: PopObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: TableObjectProtocol? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: TitleObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: BeanObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isInfo = false
    @objc dynamic var isInfo = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = MakeSocketDelegate()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(rawDataNote(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(backgroundSkin),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (constTitleFormat.lowercased() + String(const_userName.suffix(8))))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ViewKingfisherCompatible.shared as! ViewKingfisherCompatible).userDown()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isInfo")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ViewKingfisherCompatible.shared as! ViewKingfisherCompatible).userDown()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension MakeSocketDelegate: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        FeedLogTool.matchVoice(msg: (String(noti_modelFormat) + String(dataTitleName)))
        //: sendPing()
        collideWith()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: main_screenContent,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: const_intervalMessage)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        modelView()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        FeedLogTool.matchVoice(msg: (String(constPathData.suffix(7)) + userItemDataTextName.replacingOccurrences(of: "mode", with: "接失") + "error:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = StatisticalTableReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = StatisticalTableReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == kKeyId {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = restart(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            FeedLogTool.matchVoice(msg: (String(constTargetText) + String(constToName)) + "\(kKeyId)" + (String(data_signKey) + String(k_valueToolContent.suffix(4)) + appAssistantMsg.replacingOccurrences(of: "range", with: "重新") + "试，地址\u{ff1a}") + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.atAppear(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.contribution(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        FeedLogTool.matchVoice(msg: (String(constViewFormat) + String(userPhoneSucceedGroupMsg)) + "\(code)" + (String(showSearchModelPath.prefix(5)) + "son:") + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.option(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == userNameMessage {
            //: return
            return
        }
        //: if let status = StatisticalTableReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = StatisticalTableReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.atAppear(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func restart(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = StatisticalTableReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = StatisticalTableReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = crenelate()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: const_intervalMessage)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = StatisticalTableReactiveCompatible.share.loginUid
        let uid = StatisticalTableReactiveCompatible.share.loginUid
        //: let sid = StatisticalTableReactiveCompatible.share.loginSessionId
        let sid = StatisticalTableReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(user_cellStr.suffix(6))) + "\(sid)" + (String(user_valueLabelStr.suffix(5))) + "\(uid)" + (String(app_mainName) + String(kMakeId.suffix(9))) + "\(constClickUrl)" + (String(main_dataStr) + String(notiResultUserKey.prefix(8))) + "\(showNoName)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func crenelate() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return noti_halfId ? [(String(constCloudSourcePath.prefix(8)) + String(data_succeedCountName.suffix(2))) + "\(notiKeyStr)" + (String(constLeadingStr) + String(kBackFormat.prefix(8)))] : [(String(show_cellRequestId) + String(constOfValue))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension MakeSocketDelegate {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func rawDataNote(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            FeedLogTool.matchVoice(msg: String(bytes: dataVideoFormat.map{equalModel(layer: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.option(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.atAppear(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func backgroundSkin() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.atAppear(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.addressStarBind()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension MakeSocketDelegate {
    /// socket保活心跳包
    //: private func sendPing() {
    private func collideWith() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: const_tagFormat, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.buildExecute(info: [(String(noti_viewMsg)): (String(dataTitlePath))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func impingeDoing() {
        //: endTalkingPing()
        pastMethod()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: mainPlayFormat, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isInfo || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currInfoVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currInfoVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isInfo || (self.currentViewController()?.isKind(of: AddTvObserverDelegate.self) == false && self.currentViewController()?.isKind(of: DropViewDelegate.self) == false) && (self.currInfoVC?.isKind(of: AddTvObserverDelegate.self) == false && self.currInfoVC?.isKind(of: DropViewDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.buildExecute(info: [(String(noti_viewMsg)): (String(constCellTitle.prefix(5)) + k_defineTitle.replacingOccurrences(of: "effect", with: "tb"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func pastMethod() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func addressStarBind() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(noti_tabValue) + String(noti_contentTopId.prefix(4)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < constLimitViewKey {
                //: StatisticalTableReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                StatisticalTableReactiveCompatible.share.recieveInsideTabulation(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension MakeSocketDelegate {
    /// 开启直播心跳包
    //: func startLivePing() {
    func redSub() {
        //: endLivePing()
        pushList()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: constStoragePath, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.buildExecute(info: [(String(noti_viewMsg)): (String(userViewAddUrl.prefix(7)) + String(k_modelStr.suffix(6)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func pushList() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension MakeSocketDelegate {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func modelView() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = ExitMixView()
        //: let keyWindow = MixInReactiveCompatible.getWindow()
        let keyWindow = MixInReactiveCompatible.nameOf()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isInfo
        self.containerView?.isHidden = !self.isInfo
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func contentMiniskirt() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard ActionReactiveCompatible.editShared().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard DrawSaloonReactiveCompatible.signShared().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension MakeSocketDelegate {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func option(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(showThroughStr)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: userNameMessage, reason: (String(notiMakePath.prefix(5)) + "ser l" + String(dataAppPlayTitle.prefix(7))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        pastMethod()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            contentMiniskirt()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func liveSocket() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = MakeSocketDelegate.shared.restart(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.atAppear(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func atAppear(timeInterval: TimeInterval, type: DateType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            FeedLogTool.matchVoice(msg: (String(const_sectionMsg.suffix(7))) + "\(type.rawValue)" + (appToText) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.hitchWithSocket(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func hitchWithSocket(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                FeedLogTool.matchVoice(msg: (String(const_rangeMsg) + k_textViewUrl.replacingOccurrences(of: "info", with: "接")))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                FeedLogTool.matchVoice(msg: (String(showBottomMsg.prefix(8)) + data_contentName.replacingOccurrences(of: "add", with: "在") + "\u{9700}要\u{91cd}新\u{8fde}\u{63a5}."))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isInfo {
        if self.isInfo {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(mainLabTitle.suffix(5)) + "me=1"))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(kViewText.suffix(6))) + "\(OffPromiseAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        option(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = showStatusStr
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        FeedLogTool.matchVoice(msg: (data_videoName.replacingOccurrences(of: "range", with: "SO") + show_cancelMessage + String(k_modeText)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func buildExecute(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if StatisticalTableReactiveCompatible.share.reachability?.connection == nil || StatisticalTableReactiveCompatible.share.reachability?.connection == .unavailable {
        if StatisticalTableReactiveCompatible.share.reachability?.connection == nil || StatisticalTableReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(noti_viewMsg)): (String(appManagerText.suffix(4)) + "tDis" + data_byNorId.replacingOccurrences(of: "icon", with: "b")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (String(dataTablePath)): "\(k_imageMessage)",
                                       //: "data": [:]]
                                       (user_viewFormat.lowercased()): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            contribution(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(noti_viewMsg))] as? String == (String(dataTitlePath)) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                FeedLogTool.matchVoice(msg: (app_labFormat.replacingOccurrences(of: "count", with: "T") + " 连接保活" + String(noti_nameTimeTitle.suffix(4)) + String(notiPhoneData.suffix(6))))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(noti_viewMsg))] as? String == (String(constCellTitle.prefix(5)) + k_defineTitle.replacingOccurrences(of: "effect", with: "tb")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                FeedLogTool.matchVoice(msg: String(bytes: kBlindValue.map{$0^115}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(noti_viewMsg))] as? String == (String(userViewAddUrl.prefix(7)) + String(k_modelStr.suffix(6))) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                FeedLogTool.matchVoice(msg: String(bytes: main_managerShowMsg.reversed(), encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(noti_viewMsg))] as? String == (String(dataTitlePath)) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(noti_viewMsg)): (String(k_leadingTitle.suffix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (String(dataTablePath)): "\(notiTimeId)",
                                           //: "data": [:]]
                                           (user_viewFormat.lowercased()): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                contribution(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.atAppear(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func contribution(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(noti_viewMsg))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(String(dataTablePath))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(constMaleUrl.replacingOccurrences(of: "empty", with: "s"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(user_viewFormat.lowercased())].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(kShadowKey) + String(dataScriptCollectionId.suffix(2))) + "\(cmdName)" + (String(data_norPath)) + "\(errorNo)" + (String(const_dataBeanStr.suffix(6))) + "\(msg)" + (userSexIconKeyName.capitalized + String(constEqualFormat.prefix(5))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        before(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            FeedLogTool.matchVoice(msg: (notiSubPath.replacingOccurrences(of: "share", with: "ET") + " 接收异常消" + String(appTopKey.prefix(6)) + String(kCountMessage.suffix(4))) + "\(cmdName)" + (String(data_norPath)) + "\(errorNo)" + (String(const_dataBeanStr.suffix(6))) + "\(msg)" + (userSexIconKeyName.capitalized + String(constEqualFormat.prefix(5))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !StatisticalTableReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !StatisticalTableReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                InputWindowReactiveCompatible.shared.relieve()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.toCurrent(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(userLabId)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: appViewMessage, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(appCellGoldStr) + constTaskId.replacingOccurrences(of: "count", with: "o") + String(notiTitleId)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.isVideo(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(main_beginId) + appEnableContent.replacingOccurrences(of: "live", with: "ll")) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.christenMakeSituation(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(showOpenModelFalseFormat.suffix(5)) + "celCall") { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(constImageName.suffix(6)) + "seCal" + String(mainRowRequestUrl)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(userDataMessage) + String(const_viewStr)) { // 接收方发送acceptCall回执
            //: self.isInfo = true
            self.isInfo = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            impingeDoing()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.viewObject(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(notiStatusMessage) + const_dropPlayerKey.replacingOccurrences(of: "to", with: "l")) { // 接收方发送stopCall回执
            //: self.isInfo = false
            self.isInfo = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            pastMethod()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(main_titleData.prefix(5)) + "allTime" + String(mainTapValue))] as? Int {
                //: StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = json[(user_viewFormat.lowercased())][(String(showChangeMessage))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: data_intervalId, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ViewKingfisherCompatible.shared as! ViewKingfisherCompatible).nativeData()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(kLayerId.prefix(7)) + data_topUrl.replacingOccurrences(of: "text", with: "o")) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.commit(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (const_buttonData.replacingOccurrences(of: "point", with: "c") + "ptCal" + appLayerPath.replacingOccurrences(of: "have", with: "l")) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isInfo = true
            self.isInfo = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            impingeDoing()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.lineUpRemove(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(app_tabStr.prefix(6)) + String(k_shareKey)) { // 接收方/拨打方 挂断了通话
            //: self.isInfo = false
            self.isInfo = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            pastMethod()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.socketOrCollection(data: data)
            // 更新余额
            //: StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            StatisticalTableReactiveCompatible.share.loginUserMode.mf_coin = json[(user_viewFormat.lowercased())][(String(showChangeMessage))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            commutative(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: data_intervalId, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ViewKingfisherCompatible.shared as! ViewKingfisherCompatible).nativeData()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (notiLineGalleryInText.replacingOccurrences(of: "color", with: "re") + "tCall") { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: showTagScreenMsg, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: StatisticalTableReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                StatisticalTableReactiveCompatible.share.recieveInsideTabulation(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(noti_tabValue) + String(noti_contentTopId.prefix(4)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: showRequestMessage.reversed(), encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(user_viewFormat.lowercased())][(String(noti_valueViewPath))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: data_messageKey.map{magnitudeAction(voice: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                DataReactiveCompatible.header(uid: String(json[(user_viewFormat.lowercased())][(main_digitalMessage.replacingOccurrences(of: "info", with: "u"))].intValue),
                                         //: title: data["nickname"] as? String,
                                         title: data[(k_colorStr.replacingOccurrences(of: "block", with: "i") + String(kRemoveToData))] as? String,
                                         //: body: body,
                                         body: body,
                                         //: imageUrl: data["headPic"] as? String)
                                         imageUrl: data[(String(user_windowData.prefix(7)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                DataReactiveCompatible.shared.up()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(noti_modelName.suffix(6)) + "Resu" + String(dataModeMessage)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isInfo = true
            self.isInfo = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            impingeDoing()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(noti_modelName.suffix(6)) + "Resu" + String(dataModeMessage))] = "1"
            //: StatisticalTableReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            StatisticalTableReactiveCompatible.share.makeInfo(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (ViewKingfisherCompatible.shared as! ViewKingfisherCompatible).userDown()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (const_phonePath.replacingOccurrences(of: "to", with: "c") + String(mainSizeId)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isInfo = false
            self.isInfo = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            pastMethod()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.addDown(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                DataReactiveCompatible.row(identifier: String(json[(user_viewFormat.lowercased())][(main_digitalMessage.replacingOccurrences(of: "info", with: "u"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                DataReactiveCompatible.shared.change()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(userViewModelTitle.suffix(7)) + String(main_frameMsg)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isInfo = false
            self.isInfo = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            pastMethod()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.reject(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(show_giftNameData.prefix(5)) + "lMotiv" + notiColorTitle.replacingOccurrences(of: "normal", with: "t")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.managerData?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(kAtData.suffix(4)) + "veWarning") { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            ActionReactiveCompatible.sideTime(content: msg, reason: json[(user_viewFormat.lowercased())][(String(show_touchManagerFormat) + appSectionTitle.replacingOccurrences(of: "key", with: "o"))].stringValue, time: json[(user_viewFormat.lowercased())][(kFirstShrinkFormat.replacingOccurrences(of: "text", with: "m"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(userInsideMsg) + showEqualId.replacingOccurrences(of: "agent", with: "e")) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ActionReactiveCompatible.editShared().justTime()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(constBlindId.prefix(4)) + "ndProg" + String(dataMainName.prefix(7))) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.taproom(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(showFinishMessage) + String(notiSucceedId.prefix(3))) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(notiBottomId) + String(appCancelFormat))] as? Int ?? -1
            //: if StatisticalTableReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if StatisticalTableReactiveCompatible.share.appStatus == ScanPropertyProtocol.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: k_giftFormat, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: appViewMessage, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: k_giftFormat, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(mainByName.suffix(5)) + String(constCountValue)) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            TextVideoThen.infix().toMsg(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(main_topData) + String(kIconInputKey)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            InputWindowReactiveCompatible.shared.cease(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(kTableValue) + String(dataInfoName.prefix(7))) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            ActionReactiveCompatible.editShared().justTime()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            DrawSaloonReactiveCompatible.signShared().nowClickItem()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: notiCornerRecordUrl, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension MakeSocketDelegate {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func before(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (notiLineGalleryInText.replacingOccurrences(of: "color", with: "re") + "tCall") || cmdName == (String(main_beginId) + appEnableContent.replacingOccurrences(of: "live", with: "ll")) {
            //: if TalkingLiveManager.shared().isLive == true {
            if ActionReactiveCompatible.editShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                noti_numberManagerUrl = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                ActionReactiveCompatible.variantCompletion(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                ActionReactiveCompatible.editShared().justTime()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard noti_numberManagerUrl == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (const_phonePath.replacingOccurrences(of: "to", with: "c") + String(mainSizeId)) || cmdName == (String(showOpenModelFalseFormat.suffix(5)) + "celCall") ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(userViewModelTitle.suffix(7)) + String(main_frameMsg)) || cmdName == (String(constImageName.suffix(6)) + "seCal" + String(mainRowRequestUrl)) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (String(app_tabStr.prefix(6)) + String(k_shareKey)) || cmdName == (String(notiStatusMessage) + const_dropPlayerKey.replacingOccurrences(of: "to", with: "l"))
        {
            //: NEED_RESTORE_LIVE = false
            noti_numberManagerUrl = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = TableCapacityReactiveCompatible(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                ActionReactiveCompatible.editShared().alongHandler()
            }
            //: alert.show()
            alert.overModel()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func commutative(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(main_titleData.prefix(5)) + "allTime" + String(mainTapValue))] as? Int {
            //: StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            StatisticalTableReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(user_useStr) + String(k_centerMakeValue.prefix(4)))] as? Bool
        //: if freeCallWin == true && StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = CapacityView()
            //: view.show()
            view.itemCell()
            //: view.setShowData(data: data)
            view.stickIn(data: data)
        }
    }
}
