
//: Declare String Begin

/*: "mf/business/giftList" :*/
fileprivate let constStylePath:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","g","i","f","t","L","i"]
fileprivate let appBottomAddMessage:String = "slab"

/*: "type" :*/
fileprivate let data_arrayUrl:[UInt8] = [0x4b,0x46,0x4f,0x5a]

private func objectSize(me num: UInt8) -> UInt8 {
    return num ^ 63
}

/*: "mf/chatRoom/giftList" :*/
fileprivate let const_eventStr:String = "equal videomf/cha"
fileprivate let data_aboveFormat:[Character] = ["t","R","o","o","m","/","g","i","f","t","L","i","s","t"]

/*: "live/giftList" :*/
fileprivate let main_playerPath:[Character] = ["l","i","v","e","/","g","i","f","t"]
fileprivate let data_appPath:[Character] = ["L","i","s","t"]

/*: "party/giftList" :*/
fileprivate let dataShowId:String = "party/name share manager add awake"
fileprivate let dataMomentStr:[Character] = ["t"]

/*: "IM/getMessageList" :*/
fileprivate let notiOnTitle:String = "cost read typeIM/getMe"
fileprivate let data_frameKey:String = "mini top selfssageList"

/*: "mf/business/sendMsg" :*/
fileprivate let user_lastName:String = "mf/buclick with"
fileprivate let constAddKey:String = "ss/sendview image return background view"
fileprivate let kEffectText:[Character] = ["M","s","g"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let main_activityKey:String = "index user returnmf/c"
fileprivate let data_inputTargetUrl:String = "type type table nameoom/"

/*: "mf/business/sendGift" :*/
fileprivate let k_mediumPlayFormat:[Character] = ["m","f","/","b","u","s","i","n","e","s","s","/","s","e"]
fileprivate let showSizeFormat:String = "repeat remove view selfndGift"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let notiLineKey:[Character] = ["m","f","/","c","h"]
fileprivate let noti_dataContent:String = "user add self type stringatRo"
fileprivate let data_removePath:String = "view indicator broadcast tap viewdGift"

/*: "live/sendGift" :*/
fileprivate let k_emptyEqualMyTitle:String = "ldefine"
fileprivate let showMarginData:String = "not self viewve/sen"

/*: "party/sendGift" :*/
fileprivate let kLikeStr:String = "title size tabparty/se"
fileprivate let main_normalName:String = "import status leading content buttonndGift"

/*: "mf/user/getChatInfo" :*/
fileprivate let showAppName:[Character] = ["m","f","/","u","s","e","r","/","g","e","t"]
fileprivate let notiArrayStatusTitle:String = "Chinput view"
fileprivate let noti_byKey:[Character] = ["a","t","I","n","f","o"]

/*: "IM/getMessageUserInfoList" :*/
fileprivate let notiLimitValue:String = "IM/geprogress false"
fileprivate let show_skinText:String = "as in fill data modelsageUs"
fileprivate let userEqualId:[Character] = ["f","o","L","i","s","t"]

/*: "mf/crush/whoLikeMeList" :*/
fileprivate let const_startHiddenFutureKey:[Character] = ["m","f","/","c","r","u","s","h","/","w","h","o"]
fileprivate let userTopMessage:[Character] = ["L","i","k","e","M","e","L","i","s","t"]

/*: "mf/crush/ignore" :*/
fileprivate let data_playerInfoMessage:String = "err managermf/cru"
fileprivate let user_sizeStr:String = "foundation to livesh/ig"

/*: "mf/crush/like" :*/
fileprivate let dataButtonMoreMiniFormat:String = "bar pointmf/cru"

/*: "mf/user/getRelation" :*/
fileprivate let mainBottomId:[Character] = ["m","f","/","u","s","e","r","/","g"]
fileprivate let kKeyName:[Character] = ["e","t","R","e","l","a","t","i","o","n"]

/*: "user/attention" :*/
fileprivate let showEqualMessage:[UInt8] = [0x5e,0x58,0x4e,0x59,0x4,0x4a,0x5f,0x5f,0x4e,0x45,0x5f,0x42,0x44,0x45]

private func makeGreet(background num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "user/removeAttention" :*/
fileprivate let show_tagTitle:String = "name self start acrossuser/rem"
fileprivate let kStartLoadKey:[Character] = ["o","v"]
fileprivate let notiShowMessage:String = "eAtteguard include minimize let"

/*: "Can't earn points if you follow each other~" :*/
fileprivate let userAppStr:[UInt8] = [0x2e,0x4c,0x59,0x12,0x5f,0xb,0x50,0x4c,0x5d,0x59,0xb,0x5b,0x5a,0x54,0x59,0x5f,0x5e,0xb,0x54,0x51,0xb,0x64,0x5a,0x60,0xb,0x51,0x5a,0x57,0x57,0x5a,0x62,0xb,0x50,0x4c,0x4e,0x53,0xb,0x5a,0x5f,0x53,0x50,0x5d,0x69]

fileprivate func sectionTopCheck(content num: UInt8) -> UInt8 {
    let value = Int(num) - 235
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "follow Success" :*/
fileprivate let user_cameraName:String = "fsizellsize"
fileprivate let k_iconEqualLocationValue:String = "make truew Suc"

/*: "unfollow Success" :*/
fileprivate let appSharedUrl:String = "UNFOLLO"
fileprivate let kMakeShareBlockText:[Character] = ["w"," ","S","u","c","c","e","s","s"]

/*: "translate/msg" :*/
fileprivate let mainSignatureValue:String = "TRAN"
fileprivate let appEquallyShowPath:[Character] = ["s","g"]

/*: "msgId" :*/
fileprivate let notiImageUrl:[UInt8] = [0x97,0x89,0x9d,0xb3,0x9e]

private func groupMoment(make num: UInt8) -> UInt8 {
    return num ^ 250
}

/*: "targetLng" :*/
fileprivate let const_positionValue:[UInt8] = [0xaf,0xba,0xa9,0xbc,0xbe,0xaf,0x97,0xb5,0xbc]

private func imageFailure(size num: UInt8) -> UInt8 {
    return num ^ 219
}

/*: "mf/greet/getAutoGreetContent" :*/
fileprivate let constNoBeforeFormat:[Character] = ["m","f","/","g","r","e","e","t","/","g","e","t","A","u"]
fileprivate let show_headBarStr:String = "super corner type constrainttoGreetC"
fileprivate let user_liveIndexStr:String = "ontlastt"

/*: "mf/greet/getIntimatePhoto" :*/
fileprivate let mainPageFormat:String = "mf/gremonth equal"
fileprivate let mainTitleKey:String = "etInfalse error send equal"
fileprivate let main_effectStr:String = "keyma"

/*: "toUid" :*/
fileprivate let noti_iconUrl:[UInt8] = [0xab,0xb0,0x8a,0xb6,0xbb]

private func appStatus(log num: UInt8) -> UInt8 {
    return num ^ 223
}

/*: "mf/chatRoom/info" :*/
fileprivate let main_collectionName:String = "false as cellmf/c"
fileprivate let constMainKey:[Character] = ["i","n","f","o"]

/*: "mf/chatRoom/list" :*/
fileprivate let main_cookieKey:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","l","i","s","t"]

/*: "roomId" :*/
fileprivate let main_shadowId:[UInt8] = [0xc2,0xdf,0xdf,0xdd,0xf9,0xd4]

private func contestantButton(block num: UInt8) -> UInt8 {
    return num ^ 176
}

/*: "needExtraInfo" :*/
fileprivate let app_draftKey:[UInt8] = [0xd6,0xcd,0xcd,0xcc,0xad,0xe0,0xdc,0xda,0xc9,0xb1,0xd6,0xce,0xd7]

fileprivate func inFrom(on num: UInt8) -> UInt8 {
    let value = Int(num) + 152
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/chatRoom/check" :*/
fileprivate let mainViewMessage:String = "false readmf/cha"
fileprivate let showMakeMessage:String = "checto"

/*: "mf/chatRoom/joinOrQuit" :*/
fileprivate let main_awakePath:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","j","o","i"]
fileprivate let app_contentBackLabelUrl:String = "share"
fileprivate let kMakeKey:[Character] = ["O","r","Q","u","i","t"]

/*: "mf/chatRoom/members" :*/
fileprivate let kItemAppData:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","m","e","m","b","e"]
fileprivate let mainContentTitle:String = "contact"

/*: "translate/text" :*/
fileprivate let const_elementValue:String = "TRANSL"
fileprivate let main_managerId:String = "intimateexintimate"

/*: "text" :*/
fileprivate let k_labProgressName:[UInt8] = [0x8,0x19,0x4,0x8]

private func likeAdjustment(successfully num: UInt8) -> UInt8 {
    return num ^ 124
}

/*: "mf/user/queryStatus" :*/
fileprivate let data_canWantFrameKey:String = "constraint size let to cellmf/u"
fileprivate let main_modelMsg:String = "after make modelser/qu"
fileprivate let app_emptyPath:String = "tatushow"

/*: "toUids" :*/
fileprivate let show_sendMsg:[UInt8] = [0xd4,0xcf,0xf5,0xc9,0xc4,0xd3]

/*: "call/getCallTab" :*/
fileprivate let data_textId:String = "call/row observer to path exit"
fileprivate let dataValueId:String = "AB"

/*: "mf/user/remark" :*/
fileprivate let mainNumberUrl:[Character] = ["m","f","/","u","s","e","r","/","r","e","m","a"]
fileprivate let notiSourceStr:String = "rup"

/*: "content" :*/
fileprivate let constBackStr:[UInt8] = [0x37,0x3b,0x3a,0x20,0x31,0x3a,0x20]

private func appText(clear num: UInt8) -> UInt8 {
    return num ^ 84
}

/*: "top" :*/
fileprivate let data_marginFormat:[UInt8] = [0x70,0x6f,0x74]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ChatContentReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/7.
//

//: import UIKit
import UIKit

//: public class ChatContentReactiveCompatible: NSObject {
public class ChatContentReactiveCompatible: NSObject {
    //: public typealias RequestEndBlock = (_ t: Bool) -> Void
    public typealias RequestEndBlock = (_ t: Bool) -> Void
    //: typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    typealias FinishBlocks = (_ succeed: Bool, _ result: NSDictionary) -> Void
    //: public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [ViewThen]) -> Void
    public typealias RequestMsgUserListBlock = (_ succeed: Bool, _ dataArr: [ViewThen]) -> Void

    /// 私信礼物面板
    /// - Parameters:
    ///   - type: 0=默认， 1=私密照片，2=私密视频,3=节目单
    ///   - completion: 回调
    //: class func req_ChatGiftList(type: String, completion: @escaping FinishBlock) {
    class func loadItem(type: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/business/giftList"
        reqModel.requestPath = (String(constStylePath) + appBottomAddMessage.replacingOccurrences(of: "lab", with: "t"))
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: data_arrayUrl.map{objectSize(me: $0)}, encoding: .utf8)!: type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_ChatRoomGiftList(type: String, completion: @escaping FinishBlock) {
    class func countimate(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/giftList"
        reqModel.requestPath = (String(const_eventStr.suffix(6)) + String(data_aboveFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_LiveGiftList(type: String, completion: @escaping FinishBlock) {
    class func giftColor(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "live/giftList"
        reqModel.requestPath = (String(main_playerPath) + String(data_appPath))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房礼物面板
    /// - Parameters：
    ///   - completion: 回调
    //: class func req_partyGiftList(type: String, completion: @escaping FinishBlock) {
    class func communication(type _: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "party/giftList"
        reqModel.requestPath = (String(dataShowId.prefix(6)) + "giftLis" + String(dataMomentStr))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求系统消息
    /// - Parameter completion: 回调
    //: @objc class func req_sysMessageList(completion: @escaping FinishBlocks) {
    @objc class func agendaRadiogramInfoCompletion(completion: @escaping FinishBlocks) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "IM/getMessageList"
        reqModel.requestPath = (String(notiOnTitle.suffix(8)) + String(data_frameKey.suffix(9)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: completion(true, result as! NSDictionary)
                completion(true, result as! NSDictionary)
            }
        }
    }

    /// 私聊发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendPrivete(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func barAwakeContainer(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendMsg"
        reqModel.requestPath = (String(user_lastName.prefix(5)) + "sine" + String(constAddKey.prefix(7)) + String(kEffectText))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 公共聊天室发送消息
    /// - Parameters:
    ///   - param: 传参
    ///   - completion: 回调
    //: class func req_SendRoomMsg(param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func firstMiniUser(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendMsg"
        reqModel.requestPath = (String(main_activityKey.suffix(4)) + "hatR" + String(data_inputTargetUrl.suffix(4)) + "sendMsg")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftPrivete( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func receiveParam(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/business/sendGift"
        reqModel.requestPath = (String(k_mediumPlayFormat) + String(showSizeFormat.suffix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftChatRoom( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func roundOfCompletion(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/chatRoom/sendGift"
        reqModel.requestPath = (String(notiLineKey) + String(noti_dataContent.suffix(4)) + "om/sen" + String(data_removePath.suffix(5)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 直播送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_SendGiftLive( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func param(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "live/sendGift"
        reqModel.requestPath = (k_emptyEqualMyTitle.replacingOccurrences(of: "define", with: "i") + String(showMarginData.suffix(6)) + "dGift")
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 语聊房送礼物
    /// - Parameters:
    ///   - param: 参数
    ///   - completion: 回调
    //: class func req_sendPartyGift(param: [String: Any], completion: @escaping FinishBlock) {
    class func sura(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "party/sendGift"
        reqModel.requestPath = (String(kLikeStr.suffix(8)) + String(main_normalName.suffix(6)))
        //: reqModel.params = param
        reqModel.params = param
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求聊天用户信息
    /// - Parameters:
    //: class func req_refreshUserChatInfo( param: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func barZoneUpward(param: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getChatInfo"
        reqModel.requestPath = (String(showAppName) + String(notiArrayStatusTitle.prefix(2)) + String(noti_byKey))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 请求用户消息列表
    /// - Parameters:
    //: @objc class public func req_refreshMsgUserListInfo( param: Dictionary<String, Any>, completion: @escaping RequestMsgUserListBlock) {
    @objc public class func statusFemale(param: [String: Any], completion: @escaping RequestMsgUserListBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "IM/getMessageUserInfoList"
        reqModel.requestPath = (String(notiLimitValue.prefix(5)) + "tMes" + String(show_skinText.suffix(6)) + "erIn" + String(userEqualId))
        //: reqModel.params = param
        reqModel.params = param
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, _ in
            //: guard succeed else {
            guard succeed else {
                //: completion(succeed, [])
                completion(succeed, [])
                //: return
                return
            }

            //: if let datas = Array<ViewThen>.deserialize(from: result as? Array) {
            if let datas = Array<ViewThen>.deserialize(from: result as? Array) {
                //: completion(succeed, datas as! [ViewThen])
                completion(succeed, datas as! [ViewThen])
            }
        }
    }

    /// 请求用户消息列表
    //: @objc class public func req_giftEffectDataUrl(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
    @objc public class func beData(giftUrl: String, path: String, completion: @escaping RequestEndBlock) {
        //: GJ.startGiftZipRequest(giftUrl: giftUrl, path: path) { succeed, result, errorModel in
        show_pageTitle.sunnahOn(giftUrl: giftUrl, path: path) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 谁心动我 - 列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func modelWith(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/whoLikeMeList"
        reqModel.requestPath = (String(const_startHiddenFutureKey) + String(userTopMessage))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - nope拒绝操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMeIgnore(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func duringLine(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/ignore"
        reqModel.requestPath = (String(data_playerInfoMessage.suffix(6)) + String(user_sizeStr.suffix(5)) + "nore")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - like操作
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_whoLikeMelike(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func searchedEnd(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/crush/like"
        reqModel.requestPath = (String(dataButtonMoreMiniFormat.suffix(6)) + "sh/like")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_atationList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func notModel(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getRelation"
        reqModel.requestPath = (String(mainBottomId) + String(kKeyName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 关注操作
    /// - Parameters:
    /// - isAttention: 关注/取消
    ///   - completion: 回调
    //: class func req_atationTool(isAttention: Bool, params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func nearTitle(isAttention: Bool, params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: let path = isAttention==true ? "user/attention" :"user/removeAttention"
        let path = isAttention == true ? String(bytes: showEqualMessage.map{makeGreet(background: $0)}, encoding: .utf8)! : (String(show_tagTitle.suffix(8)) + String(kStartLoadKey) + String(notiShowMessage.prefix(5)) + "ntion")
        //: reqModel.requestPath = path
        reqModel.requestPath = path
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            // 统一处理toast
            //: if succeed {
            if succeed {
                //: if isAttention {
                if isAttention {
                    //: if StatisticalTableReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
                    if StatisticalTableReactiveCompatible.share.loginUserMode.sex == FalloutCompatibleValue.female.rawValue { // 女性
                        //: object.func__showStatusBarSuccessMsg(showMsg: "Can't earn points if you follow each other~".localized)
                        noti_pathValue.taproom(showMsg: String(bytes: userAppStr.map{sectionTopCheck(content: $0)}, encoding: .utf8)!.localized)
                        //: } else {
                    } else {
                        //: object.func__showStatusBarSuccessMsg(showMsg: "follow Success".localized)
                        noti_pathValue.taproom(showMsg: (user_cameraName.replacingOccurrences(of: "size", with: "o") + String(k_iconEqualLocationValue.suffix(5)) + "cess").localized)
                    }
                    //: } else {
                } else {
                    //: object.func__showStatusBarSuccessMsg(showMsg: "unfollow Success".localized)
                    noti_pathValue.taproom(showMsg: (appSharedUrl.lowercased() + String(kMakeShareBlockText)).localized)
                }

                //: } else {
            } else {
                //: object.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                noti_pathValue.alongShow(showMsg: errorModel?.errorMsg ?? "")
            }
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - msgId: 消息ID
    ///   - type=0表示私聊消息，type=1表示聊天室消息
    ///   - completion: 回调
    //: @objc class public func req_translateMsg(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
    @objc public class func ping(msgId: String, type: Int, completion: @escaping (_ succeed: Bool, _ result: NSDictionary, _ errorCode: Int) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "translate/msg"
        reqModel.requestPath = (mainSignatureValue.lowercased() + "slate/m" + String(appEquallyShowPath))
        //: reqModel.params = ["msgId": msgId, "targetLng": MixInReactiveCompatible.getSystemLangCode(), "type": "\(type)"]
        reqModel.params = [String(bytes: notiImageUrl.map{groupMoment(make: $0)}, encoding: .utf8)!: msgId, String(bytes: const_positionValue.map{imageFailure(size: $0)}, encoding: .utf8)!: MixInReactiveCompatible.withEvent(), String(bytes: data_arrayUrl.map{objectSize(me: $0)}, encoding: .utf8)!: "\(type)"]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
            completion(succeed, result as? NSDictionary ?? [:], errorModel?.errorCode ?? 0)
        }
    }

    /// 获取一键打招呼内容
    ///   - completion: 回调
    //: class func req_getAutoGreetContent(completion: @escaping FinishBlock) {
    class func changeCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getAutoGreetContent"
        reqModel.requestPath = (String(constNoBeforeFormat) + String(show_headBarStr.suffix(8)) + user_liveIndexStr.replacingOccurrences(of: "last", with: "en"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取随机私密照片
    ///   - completion: 回调
    //: class func req_getIntimatePhoto(toUid: String, completion: @escaping FinishBlock) {
    class func photoVoice(toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getIntimatePhoto"
        reqModel.requestPath = (String(mainPageFormat.prefix(6)) + "et/g" + String(mainTitleKey.prefix(4)) + main_effectStr.replacingOccurrences(of: "key", with: "ti") + "tePhoto")
        //: reqModel.params = ["toUid": toUid]
        reqModel.params = [String(bytes: noti_iconUrl.map{appStatus(log: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取聊天广场在线人数和家族信息
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomInfo(completion: @escaping FinishBlock) {
    class func actionSession(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/info"
        reqModel.requestPath = (String(main_collectionName.suffix(4)) + "hatRoom/" + String(constMainKey))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天广场列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomList(completion: @escaping FinishBlock) {
    class func mastheadModel(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/list"
        reqModel.requestPath = (String(main_cookieKey))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测聊天室
    /// - Parameters:
    ///   - roomId: 聊天室Id
    ///   - completion: 回调
    //: class func req_checkChatRoom(roomId: String, completion: @escaping FinishBlock) {
    class func toView(roomId: String, completion: @escaping FinishBlock) {
        //: let params = ["roomId": roomId, "needExtraInfo": "1"]
        let params = [String(bytes: main_shadowId.map{contestantButton(block: $0)}, encoding: .utf8)!: roomId, String(bytes: app_draftKey.map{inFrom(on: $0)}, encoding: .utf8)!: "1"]
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/check"
        reqModel.requestPath = (String(mainViewMessage.suffix(6)) + "tRoom/" + showMakeMessage.replacingOccurrences(of: "to", with: "k"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 进入或退出聊天室
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_chatRoomJoinOrQuit(params: [String: Any], completion: @escaping FinishBlock) {
    class func nameTag(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/joinOrQuit"
        reqModel.requestPath = (String(main_awakePath) + app_contentBackLabelUrl.replacingOccurrences(of: "share", with: "n") + String(kMakeKey))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 聊天室在线名单
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getChatRoomMembers(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func crushCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/chatRoom/members"
        reqModel.requestPath = (String(kItemAppData) + mainContentTitle.replacingOccurrences(of: "contact", with: "rs"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 文本翻译
    /// - Parameters:
    ///   - text: 文本
    ///   - completion: 回调
    //: class func req_translateText(text: String, completion: @escaping FinishBlock) {
    class func applicationError(text: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "translate/text"
        reqModel.requestPath = (const_elementValue.lowercased() + "ate/" + main_managerId.replacingOccurrences(of: "intimate", with: "t"))
        //: reqModel.params = ["text": text]
        reqModel.params = [String(bytes: k_labProgressName.map{likeAdjustment(successfully: $0)}, encoding: .utf8)!: text]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 批量查询消息列表用户在线状态
    /// - Parameters:
    ///   - toUids: 用户ids
    ///   - completion: 回调
    //: @objc class public func req_queryStatus(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
    @objc public class func tipPath(toUids: String, completion: @escaping (_ succeed: Bool, _ result: [[String: Any]]?) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/user/queryStatus"
        reqModel.requestPath = (String(data_canWantFrameKey.suffix(4)) + String(main_modelMsg.suffix(6)) + "eryS" + app_emptyPath.replacingOccurrences(of: "show", with: "s"))
        //: reqModel.params = ["toUids": toUids, "type": 1]
        reqModel.params = [String(bytes: show_sendMsg.map{$0^160}, encoding: .utf8)!: toUids, String(bytes: data_arrayUrl.map{objectSize(me: $0)}, encoding: .utf8)!: 1]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, _ in
            //: completion(succeed, result as? [[String: Any]] ?? [])
            completion(succeed, result as? [[String: Any]] ?? [])
        }
    }

    /// 拨打通话记录
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_getCallTab(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func disable(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "call/getCallTab"
        reqModel.requestPath = (String(data_textId.prefix(5)) + "getCallT" + dataValueId.lowercased())
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 新增/修改备注
    /// - Parameters:
    ///   - content: 备注内容
    ///   - isTop: 是否置顶，1：是，2：否
    ///   - toUid: 对方uid
    ///   - completion: 回调
    //: class func req_remark(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
    class func crown(content: String, isTop: Int, toUid: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/user/remark"
        reqModel.requestPath = (String(mainNumberUrl) + notiSourceStr.replacingOccurrences(of: "up", with: "k"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = ["content": content, "top": isTop, "toUid": toUid]
        reqModel.params = [String(bytes: constBackStr.map{appText(clear: $0)}, encoding: .utf8)!: content, String(bytes: data_marginFormat.reversed(), encoding: .utf8)!: isTop, String(bytes: noti_iconUrl.map{appStatus(log: $0)}, encoding: .utf8)!: toUid]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
