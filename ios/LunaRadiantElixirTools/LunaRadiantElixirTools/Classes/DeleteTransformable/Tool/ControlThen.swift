
//: Declare String Begin

/*: "user/updateInfo" :*/
fileprivate let user_labContent:[Character] = ["u","s"]
fileprivate let k_giftPath:String = "add edit model attacher/up"
fileprivate let k_jumpStr:String = "equal app sceneInfo"

/*: "gallery/getGalleryByUid" :*/
fileprivate let const_requestText:[Character] = ["g","a","l","l","e","r","y","/","g","e","t","G","a","l","l","e","r","y","B","y","U"]
fileprivate let show_panelVerticalKey:String = "authord"

/*: "gallery/upload" :*/
fileprivate let noti_viewText:[Character] = ["g","a"]
fileprivate let dataArrayPath:String = "llacceptry"

/*: "gallery/deletePic" :*/
fileprivate let user_crushId:[Character] = ["g","a","l","l","e","r","y","/","d","e","l","e","t","e","P","i","c"]

/*: "user/tagDelete" :*/
fileprivate let main_makeMessage:String = "user/with self"
fileprivate let appTransitionName:String = "etextete"

/*: "mf/user/tagEdit" :*/
fileprivate let main_displayMessage:[Character] = ["m","f","/","u","s","e","r","/","t","a"]
fileprivate let kScreenFormat:[Character] = ["g","E","d","i","t"]

/*: "app/feedback" :*/
fileprivate let show_viewFinishPositionText:String = "app/fuser image appear round top"
fileprivate let data_indexRemoveId:String = "EEDBACK"

/*: "IM/getMyBlackList" :*/
fileprivate let k_actionData:String = "make return info component cellIM/get"
fileprivate let notiAppearKey:[Character] = ["M","y","B","l","a","c","k","L","i","s","t"]

/*: "mf/user/getSetting" :*/
fileprivate let show_infoMsg:String = "mf/useself user color let"
fileprivate let data_managerValue:String = "r/glist show plus equal"
fileprivate let noti_showMessage:String = "etSearray loop succeed type"

/*: "mf/user/setConfig" :*/
fileprivate let app_errorStr:[Character] = ["m","f","/","u","s","e","r","/","s","e","t","C","o","n","f","i"]
fileprivate let k_asideQueryFormat:String = "object"

/*: "mf/user/settingStatus" :*/
fileprivate let app_viewTitle:String = "mf/ustab remove space format view"
fileprivate let k_contentToPath:String = "error clear user equaltingSt"

/*: "type" :*/
fileprivate let showLeadingTitle:[UInt8] = [0x35,0x38,0x31,0x24]

private func copernicanSystem(app num: UInt8) -> UInt8 {
    return num ^ 65
}

/*: "user/lockAccount" :*/
fileprivate let user_layerId:String = "user/locenter shared model background"
fileprivate let app_resultTitle:String = "kAclick in element log"
fileprivate let kIconKey:String = "ccospacet"

/*: "mf/user/editInfo" :*/
fileprivate let const_postUrl:String = "mf/usreturn data collection"
fileprivate let k_makePathMessage:String = "right status name string beantInfo"

/*: "mf/userSign/list" :*/
fileprivate let constNumberData:[Character] = ["m","f","/","u","s"]
fileprivate let constActualPath:String = "erSititle normal view submit"

/*: "mf/userSign/signIn" :*/
fileprivate let const_bottomText:String = "touchf"
fileprivate let k_messageTitle:String = "/userSmax first catch var"
fileprivate let data_buttonPath:String = "signInraw count"

/*: "mf/dating/saveVideo" :*/
fileprivate let appGenderName:String = "mf/datfor button than data"
fileprivate let main_objectMsg:String = "aveVideoitem intimate show"

/*: "mf/dating/getConfig" :*/
fileprivate let constMLineText:String = "mf/datincategory model"
fileprivate let data_pauseManagerId:String = "g/getrue be"

/*: "mf/dating/deleteVideo" :*/
fileprivate let k_blockKey:String = "mf/daindex data to succeed background"
fileprivate let appShowTitle:[Character] = ["d","e","l","e"]
fileprivate let show_modelText:String = "teVideoadd appear model color"

/*: "mf/greet/updateAudioRemark" :*/
fileprivate let app_connectionPath:[Character] = ["m","f","/","g","r","e"]
fileprivate let userIndexGiftPath:String = "white size right model onet/up"
fileprivate let notiSharePath:String = "string var track max infoioRe"

/*: "mf/greet/list" :*/
fileprivate let show_arrayName:String = "mf/grecount center to height at"
fileprivate let appUserId:String = "svideo"

/*: "mf/greet/add" :*/
fileprivate let k_modeTitle:[Character] = ["m","f","/","g","r","e","e","t"]
fileprivate let show_licenseData:String = "/additem return product"

/*: "mf/greet/del" :*/
fileprivate let k_upTitleValue:[Character] = ["m","f","/","g","r"]
fileprivate let const_jumpName:String = "error top limit extensioneet/del"

/*: "mf/greet/getSettings" :*/
fileprivate let kConfirmLanguageMessage:String = "mf/greepath path make shared view"
fileprivate let userExploreMsg:String = "shadow content size origin arrayt/ge"
fileprivate let show_formId:String = "intarget"

/*: "mf/greet/extraSetting" :*/
fileprivate let userDataStr:[Character] = ["m","f","/","g","r","e","e","t","/","e","x","t"]
fileprivate let appPathName:String = "raSewrap m tool"

/*: "game/list" :*/
fileprivate let k_fileKey:[Character] = ["g","a","m"]
fileprivate let kImageId:String = "selected app viewe/list"

/*: "category" :*/
fileprivate let const_bottomValue:[UInt8] = [0x79,0x72,0x6f,0x67,0x65,0x74,0x61,0x63]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ControlThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/9.
//

//: import UIKit
import UIKit

//: class TalkingMeRequestTool: NSObject {
class ControlThen: NSObject {
    /// 资料完善
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateUserInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func belowImage(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "user/updateInfo"
        reqModel.requestPath = (String(user_labContent) + String(k_giftPath.suffix(5)) + "date" + String(k_jumpStr.suffix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户相册
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GalleryByUid(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func gallery(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "gallery/getGalleryByUid"
        reqModel.requestPath = (String(const_requestText) + show_panelVerticalKey.replacingOccurrences(of: "author", with: "i"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UploadPic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func upCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "gallery/upload"
        reqModel.requestPath = (String(noti_viewText) + dataArrayPath.replacingOccurrences(of: "accept", with: "e") + "/upload")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除图片
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeletePic(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func doingName(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "gallery/deletePic"
        reqModel.requestPath = (String(user_crushId))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除单个用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func objectCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "user/tagDelete"
        reqModel.requestPath = (String(main_makeMessage.prefix(5)) + "tagD" + appTransitionName.replacingOccurrences(of: "text", with: "l"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 编辑用户标签
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_EditTag(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func iconAcrossCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/user/tagEdit"
        reqModel.requestPath = (String(main_displayMessage) + String(kScreenFormat))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 意见反馈
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_Feedback(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func executeSign(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "app/feedback"
        reqModel.requestPath = (String(show_viewFinishPositionText.prefix(5)) + data_indexRemoveId.lowercased())
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取黑名单列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_BlackList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func upAll(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "IM/getMyBlackList"
        reqModel.requestPath = (String(k_actionData.suffix(6)) + String(notiAppearKey))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取设置信息接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func params(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getSetting"
        reqModel.requestPath = (String(show_infoMsg.prefix(6)) + String(data_managerValue.prefix(3)) + String(noti_showMessage.prefix(4)) + "tting")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SettingChange(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func hideCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/user/setConfig"
        reqModel.requestPath = (String(app_errorStr) + k_asideQueryFormat.replacingOccurrences(of: "object", with: "g"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 女性busy状态上报接口
    /// - Parameters:
    ///   - type: 0 :不活跃， 1:活跃
    ///   - completion: 回调
    //: class func req_settingStatus(type: Int, completion: @escaping FinishBlock) {
    class func command(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/user/settingStatus"
        reqModel.requestPath = (String(app_viewTitle.prefix(5)) + "er/set" + String(k_contentToPath.suffix(6)) + "atus")
        //: reqModel.params = ["type": type]
        reqModel.params = [String(bytes: showLeadingTitle.map{copernicanSystem(app: $0)}, encoding: .utf8)!: type]
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 注销账号
    /// - Parameters:
    /// - completion: 回调
    //: class func req_LockAccount(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func dataPriority(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "user/lockAccount"
        reqModel.requestPath = (String(user_layerId.prefix(8)) + String(app_resultTitle.prefix(2)) + kIconKey.replacingOccurrences(of: "space", with: "un"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 个人中心修改私信价格、开启语音授权
    /// - Parameters:
    /// - completion: 回调
    //: class func req_EditInfo(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func perspective(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/user/editInfo"
        reqModel.requestPath = (String(const_postUrl.prefix(5)) + "er/edi" + String(k_makePathMessage.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取签到日历列表
    /// - Parameter completion: 回调
    //: class func req_getUserSignList(completion: @escaping FinishBlock) {
    class func notCorner(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/userSign/list"
        reqModel.requestPath = (String(constNumberData) + String(constActualPath.prefix(4)) + "gn/list")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户签到
    /// - Parameter completion: 回调
    //: class func req_userSignIn(completion: @escaping FinishBlock) {
    class func sunnah(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/userSign/signIn"
        reqModel.requestPath = (const_bottomText.replacingOccurrences(of: "touch", with: "m") + String(k_messageTitle.prefix(6)) + "ign/" + String(data_buttonPath.prefix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户资料视频地址上传
    /// - Parameters:
    ///   - completion: 回调
    //: class func uploadVideoUserEdit(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func isochronous(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/saveVideo"
        reqModel.requestPath = (String(appGenderName.prefix(6)) + "ing/s" + String(main_objectMsg.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func getUploadVideoInfo(completion: @escaping FinishBlock) {
    class func nearSub(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/getConfig"
        reqModel.requestPath = (String(constMLineText.prefix(8)) + String(data_pauseManagerId.prefix(4)) + "tConfig")
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除当前用户上传的视频
    /// - Parameters:
    ///   - completion: 回调
    //: class func deleteUserVideo(completion: @escaping FinishBlock) {
    class func footballer(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/dating/deleteVideo"
        reqModel.requestPath = (String(k_blockKey.prefix(5)) + "ting/" + String(appShowTitle) + String(show_modelText.prefix(7)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新语音备注
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(completion: @escaping FinishBlock) {
    class func more(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(app_connectionPath) + String(userIndexGiftPath.suffix(5)) + "dateAud" + String(notiSharePath.suffix(4)) + "mark")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-查看设置列表
    /// - Parameters: type: 1=语音， 2=文本
    ///   - completion: 回调
    //: class func req_GetGreetList(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func sharedUp(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/list"
        reqModel.requestPath = (String(show_arrayName.prefix(6)) + "et/li" + appUserId.replacingOccurrences(of: "video", with: "t"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-添加自定义文案/语音/图片
    /// - Parameters: type: 1：语音；2：文案；3：图片; 4:私密照片
    ///   - completion: 回调
    //: class func req_UploadGreetAdd(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func clip(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/add"
        reqModel.requestPath = (String(k_modeTitle) + String(show_licenseData.prefix(4)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-设置音频备注
    ///   - completion: 回调
    //: class func req_UpdateAudioRemark(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func reportDown(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/updateAudioRemark"
        reqModel.requestPath = (String(app_connectionPath) + String(userIndexGiftPath.suffix(5)) + "dateAud" + String(notiSharePath.suffix(4)) + "mark")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 自动打招呼-删除文案/音频
    ///   - completion: 回调
    //: class func req_DeleteAudio(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func chemicalAgent(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/del"
        reqModel.requestPath = (String(k_upTitleValue) + String(const_jumpName.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 招呼设置-查看设置
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_GetGreetSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func buttonMini(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/getSettings"
        reqModel.requestPath = (String(kConfirmLanguageMessage.prefix(7)) + String(userExploreMsg.suffix(4)) + "tSett" + show_formId.replacingOccurrences(of: "target", with: "gs"))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 图片打招呼-额外配置礼物信息
    /// - Parameters: unlockGiftId:解锁礼物id, isBurn:设置的阅后即焚状态
    ///   - completion: 回调
    //: class func req_UploadGreetExtraSetting(params: Dictionary<String, Any>, completion: @escaping FinishBlock) {
    class func thumb(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "mf/greet/extraSetting"
        reqModel.requestPath = (String(userDataStr) + String(appPathName.prefix(4)) + "tting")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取游戏列表
    /// - Parameters:
    ///   - category: 1=我的页面，2=私聊页面
    ///   - completion: 回调
    //: class func req_getGameList(category: Int, completion: @escaping FinishBlock) {
    class func listPhone(category: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ContentBlockReactiveCompatible()
        //: reqModel.requestPath = "game/list"
        reqModel.requestPath = (String(k_fileKey) + String(kImageId.suffix(6)))
        //: reqModel.params = ["category": category]
        reqModel.params = [String(bytes: const_bottomValue.reversed(), encoding: .utf8)!: category]
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        show_pageTitle.withCompletion(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
