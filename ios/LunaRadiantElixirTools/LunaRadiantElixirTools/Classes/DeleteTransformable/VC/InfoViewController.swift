
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appModelPath:[UInt8] = [0xb3,0xb4,0xb3,0xae,0xf2,0xb9,0xb5,0xbe,0xbf,0xa8,0xe0,0xf3,0xfa,0xb2,0xbb,0xa9,0xfa,0xb4,0xb5,0xae,0xfa,0xb8,0xbf,0xbf,0xb4,0xfa,0xb3,0xb7,0xaa,0xb6,0xbf,0xb7,0xbf,0xb4,0xae,0xbf,0xbe]

/*: "Voice greeting" :*/
fileprivate let notiUserStr:String = "else var popVoic"
fileprivate let mainManagerViewPath:String = "tindismiss"

/*: "Add" :*/
fileprivate let constLabelUrl:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let mainTitleContent:String = "tlaste"

/*: "addStatus" :*/
fileprivate let user_blockValue:[Character] = ["a","d","d"]
fileprivate let noti_editId:String = "self type stringStatus"

/*: "list" :*/
fileprivate let constStopBrowKey:String = "liviewt"

/*: "audio" :*/
fileprivate let notiStyleUrl:String = "audiblock"

/*: "length" :*/
fileprivate let main_indexDataMessage:[Character] = ["l","e","n","g","t","h"]

/*: "url" :*/
fileprivate let kToStr:String = "urlabel"

/*: "Successfully added, please wait for approval" :*/
fileprivate let appMakeGiftIndexUrl:[UInt8] = [0xbe,0x98,0x8e,0x8e,0x88,0x9e,0x9e,0x8b,0x98,0x81,0x81,0x94,0xcd,0x8c,0x89,0x89,0x88,0x89,0xc1,0xcd,0x9d,0x81,0x88,0x8c,0x9e,0x88,0xcd,0x9a,0x8c,0x84,0x99,0xcd,0x8b,0x82,0x9f,0xcd,0x8c,0x9d,0x9d,0x9f,0x82,0x9b,0x8c,0x81]

private func executeName(kit num: UInt8) -> UInt8 {
    return num ^ 237
}

/*: "Are you sure to delete this message？" :*/
fileprivate let kFirstKey:[UInt8] = [0x0,0x33,0x24,0x61,0x38,0x2e,0x34,0x61,0x32,0x34,0x33,0x24,0x61,0x35,0x2e,0x61,0x25,0x24,0x2d,0x24,0x35,0x24,0x61,0x35,0x29,0x28,0x32,0x61,0x2c,0x24,0x32,0x32,0x20,0x26,0x24,0xae,0xfd,0xde]

private func featureMoment(position num: UInt8) -> UInt8 {
    return num ^ 65
}

/*: "Cancel" :*/
fileprivate let app_indexMessage:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let noti_moveName:String = "tool"
fileprivate let userVideoName:String = "ELETE"

/*: "id" :*/
fileprivate let const_stateMsg:String = "isocial"

/*: "Please wait for the audit result" :*/
fileprivate let kKindMsg:[UInt8] = [0x1f,0x23,0x2a,0x2e,0x3c,0x2a,0x6f,0x38,0x2e,0x26,0x3b,0x6f,0x29,0x20,0x3d,0x6f,0x3b,0x27,0x2a,0x6f,0x2e,0x3a,0x2b,0x26,0x3b,0x6f,0x3d,0x2a,0x3c,0x3a,0x23,0x3b]

private func failPhoto(transform num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "The upper limit has been reached" :*/
fileprivate let mainSourceData:[UInt8] = [0x8d,0xa1,0x9e,0x59,0xae,0xa9,0xa9,0x9e,0xab,0x59,0xa5,0xa2,0xa6,0xa2,0xad,0x59,0xa1,0x9a,0xac,0x59,0x9b,0x9e,0x9e,0xa7,0x59,0xab,0x9e,0x9a,0x9c,0xa1,0x9e,0x9d]

fileprivate func scaleAdd(normal num: UInt8) -> UInt8 {
    let value = Int(num) + 199
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "filePath" :*/
fileprivate let mainDeadlineData:String = "filePalet change"
fileprivate let notiWhiteLayerName:[Character] = ["t","h"]

/*: "time" :*/
fileprivate let appFirstData:String = "timcard"

/*: "#FF506D" :*/
fileprivate let mainSizeStr:String = "#"
fileprivate let user_appViewMessage:[Character] = ["F","F","5","0","6","D"]

/*: "btn_message_delete_nor" :*/
fileprivate let user_intimateName:String = "show request phone true viewbtn_me"
fileprivate let kInfoPlaceName:String = "name row view_delet"
fileprivate let constColorBackKey:String = "e_norto height make"

/*: "Currently mute" :*/
fileprivate let const_rangeText:String = "c"
fileprivate let mainSearchedKey:String = "urrapplicationnt"

/*: "Edit Title" :*/
fileprivate let show_errorPath:String = "view not define to imageEdit "
fileprivate let const_fatalStr:[Character] = ["T","i","t","l","e"]

/*: "OK" :*/
fileprivate let notiLogDataFormat:String = "ok"

/*: "remark" :*/
fileprivate let notiAppMsg:String = "remblockk"

/*: "Click the button below to add\ncontent" :*/
fileprivate let kStyleValue:[UInt8] = [0xec,0xc3,0xc6,0xcc,0xc4,0x8f,0xdb,0xc7,0xca,0x8f,0xcd,0xda,0xdb,0xdb,0xc0,0xc1,0x8f,0xcd,0xca,0xc3,0xc0,0xd8,0x8f,0xdb,0xc0,0x8f,0xce,0xcb,0xcb,0xa5,0xcc,0xc0,0xc1,0xdb,0xca,0xc1,0xdb]

private func statusFrame(number num: UInt8) -> UInt8 {
    return num ^ 175
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let dataNameText:[UInt8] = []

private func managerTop(image num: UInt8) -> UInt8 {
    return num ^ 246
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InfoViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class InfoViewController: DropBaseViewController {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: ViewReactiveCompatible?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: FirstPlayTool?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: AudioView?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appModelPath.map{$0^218}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(notiUserStr.suffix(4)) + "e gree" + mainManagerViewPath.replacingOccurrences(of: "dismiss", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.messageText()
        //: designView()
        forefront()
        //: getAudioListData()
        pathList()
        //: setMutedDetection()
        mutedMax()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(exceptEnd(notification:)), name: showTagScreenMsg, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(no(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(stop),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: showTagScreenMsg,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.pullUpShortAce()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [ScreenHandyJSON] = {
        //: var array = [TalkingGreetModel]()
        var array = [ScreenHandyJSON]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(constLabelUrl)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.colorTitle(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.atArray(colors: UIColor.govern(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(all), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension InfoViewController {
    //: func getAudioListData() {
    func pathList() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(mainTitleContent.replacingOccurrences(of: "last", with: "yp"))] = 1
        //: ProgressHUD.show()
        BeanNameProgressHUD.nameShow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        ControlThen.sharedUp(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            BeanNameProgressHUD.doingBlock()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.gift()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(user_blockValue) + String(noti_editId.suffix(6)))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(constStopBrowKey.replacingOccurrences(of: "view", with: "s"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [ScreenHandyJSON] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<ScreenHandyJSON>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func ergodic(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(mainTitleContent.replacingOccurrences(of: "last", with: "yp"))] = 1
        //: dict["audio"] = data
        dict[(notiStyleUrl.replacingOccurrences(of: "block", with: "o"))] = data
        //: dict["length"] = Int(time)
        dict[(String(main_indexDataMessage))] = Int(time)

        //: ProgressHUD.show()
        BeanNameProgressHUD.nameShow()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        ControlThen.clip(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            BeanNameProgressHUD.doingBlock()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(kToStr.replacingOccurrences(of: "label", with: "l"))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.taproom(showMsg: String(bytes: appMakeGiftIndexUrl.map{executeName(kit: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.pathList()
                //: self.recordView?.dismiss()
                self.recordView?.makeComponent()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func alertShow(index: Int) {
        //: let config = ShowAlertConfig()
        let config = DigitizerAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.colorTitle(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        BlockThen.collectionConfig(message: String(bytes: kFirstKey.map{featureMoment(position: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(app_indexMessage)).localized, rightBtnTitle: (noti_moveName.replacingOccurrences(of: "tool", with: "D") + userVideoName.lowercased()).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            BlockThen.buttonIn()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            BlockThen.buttonIn()
            //: self.stopPlay()
            self.stop()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: ScreenHandyJSON = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            ControlThen.chemicalAgent(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.pathList()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension InfoViewController {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func exceptEnd(notification _: NSNotification) {
        //: stopPlay()
        stop()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func no(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            stop()
        }
    }

    //: @objc func addButtonClick() {
    @objc func all() {
        //: guard TalkingSocketManager.shared.isInfo == false else {
        guard MakeSocketDelegate.shared.isInfo == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.alongShow(showMsg: userClickMessage)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        UpPermissionTool.imageBlock(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.alongShow(showMsg: kScreenMsg)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.alongShow(showMsg: String(bytes: kKindMsg.map{failPhoto(transform: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.alongShow(showMsg: String(bytes: mainSourceData.map{scaleAdd(normal: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.stop()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = ViewReactiveCompatible(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.dismissBy()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(mainDeadlineData.prefix(6)) + String(notiWhiteLayerName))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.ergodic(data: data!, time: dict[(appFirstData.replacingOccurrences(of: "card", with: "e"))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension InfoViewController {
    //: func setMutedDetection() {
    func mutedMax() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        Syncretise.shared.asset()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        Syncretise.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension InfoViewController: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetAudioCell.className()
        let identifier = AudioView.className()
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: AudioView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! AudioView

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.bindLast(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.imageActivity(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.imageActivity(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            Syncretise.shared.asset()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.play(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.stop()
            //: self.editTitle(index: indexPath.section)
            self.fail(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (noti_moveName.replacingOccurrences(of: "tool", with: "D") + userVideoName.lowercased()).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.alertShow(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (mainSizeStr.capitalized + String(user_appViewMessage)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.alertShow(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.barName(name: (String(user_intimateName.suffix(6)) + "ssage" + String(kInfoPlaceName.suffix(6)) + String(constColorBackKey.prefix(5)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = VisualisationReactiveCompatible(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func play(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isInfo else {
        guard !MakeSocketDelegate.shared.isInfo else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.alongShow(showMsg: userClickMessage)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.pullUpShortAce()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? AudioView
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.imageActivity(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.alongShow(showMsg: (const_rangeText.uppercased() + mainSearchedKey.replacingOccurrences(of: "application", with: "e") + "ly mute").localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = FirstPlayTool.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.pullUpShortAce()
        //: self.player?.play(urlStr: model.content)
        self.player?.messageStr(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? AudioView
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.imageActivity(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func fail(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = InfoView(title: (String(show_errorPath.suffix(5)) + String(const_fatalStr)).localized, leftBtnTitle: (String(app_indexMessage)).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.appShow()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            BeanNameProgressHUD.nameShow()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: ScreenHandyJSON = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(notiAppMsg.replacingOccurrences(of: "block", with: "ar"))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            ControlThen.reportDown(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                BeanNameProgressHUD.doingBlock()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func stop() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.pullUpShortAce()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.imageActivity(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension InfoViewController: LineageThen {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func tightStatus(status: GolfPlayerSignedNumeric) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            stop()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            stop()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension InfoViewController {
    //: private func setupEmptyDataView() {
    private func gift() {
        //: var style = EmptyStyle()
        var style = ModeFitEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: kStyleValue.map{statusFrame(number: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .messageText()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func forefront() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: TalkingGreetAudioCell.className())
        MainTable.register(AudioView.self, forCellReuseIdentifier: AudioView.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.shouldHiddenAppear()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.statusDataMove(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: dataNameText.map{managerTop(image: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
