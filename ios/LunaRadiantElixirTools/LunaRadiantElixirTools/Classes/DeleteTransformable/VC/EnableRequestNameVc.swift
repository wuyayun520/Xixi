
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userWhenUrl:[UInt8] = [0x9b,0xa0,0x9b,0xa6,0x5a,0x95,0xa1,0x96,0x97,0xa4,0x6c,0x5b,0x52,0x9a,0x93,0xa5,0x52,0xa0,0xa1,0xa6,0x52,0x94,0x97,0x97,0xa0,0x52,0x9b,0x9f,0xa2,0x9e,0x97,0x9f,0x97,0xa0,0xa6,0x97,0x96]

fileprivate func giftTransition(by num: UInt8) -> UInt8 {
    let value = Int(num) + 206
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Text greeting" :*/
fileprivate let app_viewMsg:String = "var in corner photo rawText"
fileprivate let showInsideText:String = "infood"

/*: "Add" :*/
fileprivate let const_tableData:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let appShowPath:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let data_toolFatalTitle:String = "self title standardaddStat"
fileprivate let constPathWithMessage:[Character] = ["u","s"]

/*: "list" :*/
fileprivate let show_quoteId:[Character] = ["l","i","s","t"]

/*: "Are you sure to delete this message？" :*/
fileprivate let data_fromTableName:[UInt8] = [0xfb,0xc8,0xdf,0x9a,0xc3,0xd5,0xcf,0x9a,0xc9,0xcf,0xc8,0xdf,0x9a,0xce,0xd5,0x9a,0xde,0xdf,0xd6,0xdf,0xce,0xdf,0x9a,0xce,0xd2,0xd3,0xc9,0x9a,0xd7,0xdf,0xc9,0xc9,0xdb,0xdd,0xdf,0x55,0x6,0x25]

private func sizeView(selected num: UInt8) -> UInt8 {
    return num ^ 186
}

/*: "Cancel" :*/
fileprivate let user_colorText:[Character] = ["C"]
fileprivate let showConnectData:String = "aaddel"

/*: "Delete" :*/
fileprivate let main_dataName:[Character] = ["D","e","l","e","t"]
fileprivate let noti_detailFormat:[Character] = ["e"]

/*: "id" :*/
fileprivate let mainFromUrl:String = "ID"

/*: "The upper limit has been reached" :*/
fileprivate let main_playUrl:[UInt8] = [0x64,0x58,0x55,0x10,0x45,0x40,0x40,0x55,0x42,0x10,0x5c,0x59,0x5d,0x59,0x44,0x10,0x58,0x51,0x43,0x10,0x52,0x55,0x55,0x5e,0x10,0x42,0x55,0x51,0x53,0x58,0x55,0x54]

private func headPush(error num: UInt8) -> UInt8 {
    return num ^ 48
}

/*: "text" :*/
fileprivate let noti_backgroundValue:String = "TEXT"

/*: "Successfully added, please wait for approval" :*/
fileprivate let dataSuddenPath:[UInt8] = [0x1f,0x39,0x2f,0x2f,0x29,0x3f,0x3f,0x2a,0x39,0x20,0x20,0x35,0x6c,0x2d,0x28,0x28,0x29,0x28,0x60,0x6c,0x3c,0x20,0x29,0x2d,0x3f,0x29,0x6c,0x3b,0x2d,0x25,0x38,0x6c,0x2a,0x23,0x3e,0x6c,0x2d,0x3c,0x3c,0x3e,0x23,0x3a,0x2d,0x20]

private func sharedSourceModel(range num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "Any-Hex/Java" :*/
fileprivate let main_blockData:[Character] = ["A"]
fileprivate let appToValue:[Character] = ["n","y","-","H","e","x","/","J","a","v","a"]

/*: "#FF506D" :*/
fileprivate let const_headStr:String = "true data cell#FF506"
fileprivate let app_backTitle:String = "at"

/*: "btn_message_delete_nor" :*/
fileprivate let noti_hiddenMessage:String = "as chemical normal storagebtn_me"
fileprivate let main_labelFormat:[Character] = ["s","s","a","g","e","_","d","e"]
fileprivate let appImagePath:String = "right pathlete_nor"

/*: "Click the button below to add\ncontent" :*/
fileprivate let const_sessionText:[UInt8] = [0xd8,0x1,0xfe,0xf8,0x0,0xb5,0x9,0xfd,0xfa,0xb5,0xf7,0xa,0x9,0x9,0x4,0x3,0xb5,0xf7,0xfa,0x1,0x4,0xc,0xb5,0x9,0x4,0xb5,0xf6,0xf9,0xf9,0x9f,0xf8,0x4,0x3,0x9,0xfa,0x3,0x9]

fileprivate func voiceMake(interaction num: UInt8) -> UInt8 {
    let value = Int(num) - 149
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let main_filterPicName:[UInt8] = [0x5c,0x63,0x6e,0x65,0x2b,0x6a,0x6f,0x6f,0x62,0x65,0x6c,0x2b,0x66,0x7e,0x67,0x7f,0x62,0x7b,0x67,0x6e,0x2b,0x7f,0x6e,0x73,0x7f,0x2b,0x66,0x6e,0x78,0x78,0x6a,0x6c,0x6e,0x27,0x2b,0x7f,0x63,0x6e,0x2b,0x78,0x72,0x78,0x7f,0x2b,0x6e,0x66,0x2b,0x7c,0x62,0x67,0x67,0x2b,0x78,0x6e,0x65,0x6f,0x2b,0x7f,0x63,0x6e,0x66,0x2b,0x69,0x72,0x2b,0x7f,0x7e,0x79,0x65,0x78,0x30,0x78,0x67,0x62,0x6f,0x6e,0x2b,0x7f,0x64,0x2b,0x7f,0x63,0x6e,0x2b,0x66,0x6e,0x78,0x78,0x6a,0x6c,0x6e,0x2b,0x7f,0x64,0x2b,0x6f,0x6e,0x67,0x6e,0x7f,0x6e]

private func valueUnless(document num: UInt8) -> UInt8 {
    return num ^ 11
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EnableRequestNameVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class EnableRequestNameVc: DropBaseViewController {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: MonthViewDelegate?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userWhenUrl.map{giftTransition(by: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(app_viewMsg.suffix(4)) + " greet" + showInsideText.replacingOccurrences(of: "food", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.messageText()
        //: designView()
        reading()
        //: getAudioListData()
        showObserver()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [ScreenHandyJSON] = {
        //: var array = [TalkingGreetModel]()
        var array = [ScreenHandyJSON]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: kStatusName - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(const_tableData)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.colorTitle(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.atArray(colors: UIColor.govern(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(sawLog), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension EnableRequestNameVc {
    //: func getAudioListData() {
    func showObserver() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(String(appShowPath))] = 2
        //: ProgressHUD.show()
        BeanNameProgressHUD.nameShow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        ControlThen.sharedUp(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            BeanNameProgressHUD.doingBlock()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.crushServer()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(data_toolFatalTitle.suffix(7)) + String(constPathWithMessage))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(show_quoteId))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [ScreenHandyJSON] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<ScreenHandyJSON>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func finishTargetIndexResponse(index: Int) {
        //: let config = ShowAlertConfig()
        let config = DigitizerAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.colorTitle(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        BlockThen.collectionConfig(message: String(bytes: data_fromTableName.map{sizeView(selected: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(user_colorText) + showConnectData.replacingOccurrences(of: "add", with: "nc")).localized, rightBtnTitle: (String(main_dataName) + String(noti_detailFormat)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            BlockThen.buttonIn()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            BlockThen.buttonIn()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: ScreenHandyJSON = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            BeanNameProgressHUD.nameShow()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            ControlThen.chemicalAgent(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                BeanNameProgressHUD.doingBlock()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.showObserver()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension EnableRequestNameVc {
    //: @objc func addButtonClick() {
    @objc func sawLog() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.alongShow(showMsg: String(bytes: main_playUrl.map{headPush(error: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = MonthViewDelegate(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.permission()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.digitiserVideo(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(String(appShowPath))] = 2
            //: dict["text"] = tempStr
            dict[(noti_backgroundValue.lowercased())] = tempStr
            //: ProgressHUD.show()
            BeanNameProgressHUD.nameShow()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            ControlThen.clip(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                BeanNameProgressHUD.doingBlock()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.taproom(showMsg: String(bytes: dataSuddenPath.map{sharedSourceModel(range: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.showObserver()
                //: self.recordView?.dismiss()
                self.recordView?.enableicial()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func digitiserVideo(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(main_blockData) + String(appToValue)) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension EnableRequestNameVc: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: appUseMessage - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.colorTitle(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetTextCell.className()
        let identifier = RawView.className()
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: RawView = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! RawView

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.muffCreate(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(main_dataName) + String(noti_detailFormat)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.finishTargetIndexResponse(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(const_headStr.suffix(6)) + app_backTitle.replacingOccurrences(of: "at", with: "D")))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.finishTargetIndexResponse(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.barName(name: (String(noti_hiddenMessage.suffix(6)) + String(main_labelFormat) + String(appImagePath.suffix(8)))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = VisualisationReactiveCompatible(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension EnableRequestNameVc {
    //: private func setupEmptyDataView() {
    private func crushServer() {
        //: var style = EmptyStyle()
        var style = ModeFitEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: const_sessionText.map{voiceMake(interaction: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .messageText()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func reading() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: TalkingGreetTextCell.className())
        MainTable.register(RawView.self, forCellReuseIdentifier: RawView.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: appUseMessage, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.shouldHiddenAppear()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.statusDataMove(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: main_filterPicName.map{valueUnless(document: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
